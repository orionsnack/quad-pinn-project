# PINN 기반 쿼드콥터 바람 외란 추정 및 보정 — 캡스톤 보고서 초안

**상태**: 초안(draft). 실험 상세 근거는 전부 [EXPERIMENTS.md](EXPERIMENTS.md)의 해당 절 번호를
따라가면 확인 가능. 이 문서는 그 42개 절을 발표/보고서용으로 압축·재구성한 것.

---

## 0. 한 줄 요약

PX4 SITL(Gazebo) 환경에서, 물리 정보 신경망(PINN)으로 쿼드콥터에 작용하는 바람을
실시간 추정하고, 그 추정치를 가속도/토크 피드포워드로 비행 제어 루프에 주입해
위치·자세 유지 성능을 개선하려 시도했다. 병진(위치) 방향은 반복측정(N=10)으로
검증해도 순이익이 유지됐다(calm 제외 전 조건 양성). 회전(자세) 방향은 처음엔
"첫 순이익"을 확인했다고 결론 냈으나, 뒤늦게 N=10으로 재검증하니 그 결론이
뒤집혔다 — 현재 게인으로는 신뢰할 수 있는 순이익이 없다. 과정에서 "추정
정확도가 좋아져도 실제 제어 효과는 오히려 나빠질 수 있다"는 것, "모델이 학습
데이터의 우연한 통계적 특징(더더링된 자이로 신호)에 암묵적으로 의존할 수
있다"는 것, 그리고 "반복측정 표본이 작으면 A/B 결과의 방향조차 못 믿는다"는
것을 실측으로 발견했다.

---

## 1. 서론

### 1.1 동기

쿼드콥터는 바람이 불면 위치를 유지하기 위해 기체를 기울인다(roll/pitch). 이
반응 자체는 PX4의 내장 PID 제어기가 처리하지만, 바람이 강하거나 갑자기 바뀌면
반응이 느려 위치오차가 커진다. 만약 "지금 바람이 얼마나 부는지"를 미리
알거나 실시간으로 추정할 수 있다면, 그 정보를 제어 루프에 피드포워드로
얹어서 반응 속도를 앞당길 수 있지 않을까 — 이것이 이 프로젝트의 출발점이다.

### 1.2 목표

1. 쿼드콥터의 텔레메트리(속도, 자세)만으로 현재 바람벡터를 실시간 추정하는
   신경망을 학습한다. 순수 지도학습이 아니라 항력 방정식(물리 법칙)을 손실
   함수에 포함시켜(PINN) 일반화 성능을 높인다.
2. 그 추정치를 실제 비행 제어 루프(PX4 SITL)에 연결해, 위치 유지 성능이
   실제로 개선되는지 A/B 테스트로 검증한다.
3. 세미나에서 다룬 회전 방정식(`Jω̇ = τ_motor + τ_disturbance`)까지 확장해,
   자세(roll/pitch) 안정성도 개선할 수 있는지 검증한다.

### 1.3 스코프

- 지배방정식: 병진 항력식 `a = k|v_rel|v_rel` (RAMP-Net 등 콜로케이션+자동미분
  기반 물리손실과는 다름 — 상세 비교는 EXPERIMENTS.md 12-15절)
- 컨트롤러: PX4 내장 PID + MAVSDK 오프보드 가속도/토크 피드포워드 (MPC 아님)
- 검증 환경: PX4 SITL(Gazebo Harmonic) 시뮬레이션만 — 실기 비행 데이터 없음
- PINN 역할: 동역학 모델을 대체하는 게 아니라, 기존 PID 위에 얹는 보조 신호

---

## 2. 시스템 구조

### 2.1 전체 파이프라인

```
[데이터 수집]              [오프라인 학습]                [실비행 제어 연결]
data_collection/*.py  →  train_wind_estimator.py  →  correction_experiments/*.py
     │                         │                              │
     ▼                         ▼                              ▼
logs/*.csv              wind_estimator.pt              logs/pinn_correction_*.csv
(바람조건×yaw그리드)      (WindPINN 가중치+정규화)         (A/B 결과)
```

### 2.2 실시간 제어 루프

```
PX4 SITL 텔레메트리(속도·자세·자이로)
        │  (MAVSDK 수신, 20Hz)
        ▼
최근 1초(WINDOW=20스텝) 상태 버퍼
        │
        ▼
WindPINN(state) → 추정 바람벡터 (wind_n, wind_e) [+ 추정 회전 방해토크 (tau_x,y,z)]
        │
        ▼
accel = -ACCEL_GAIN * wind_est  (deadband 적용)         tau_ff = -TAU_FF_GAIN * tau_est
        │                                                       │
        ▼                                                       ▼
MAVSDK set_position_velocity_acceleration_ned            PX4 firmware mc_rate_control.cpp
(위치는 고정, accel만 추가)                                MAVLink DEBUG_VECT("TAU_FF") 채널로 주입
        │                                                       │
        └──────────────────→ PX4 내장 PID → Gazebo 물리 시뮬레이션(바람 포함) ──┘
                                    │
                        (다시 텔레메트리로 피드백)
```

**핵심**: PINN은 컨트롤러가 아니라 PID 위에 얹는 "가속도/토크 힌트"만
계산한다. 실제 위치/자세 제어는 전부 PX4 내부에서 수행된다.

### 2.3 모델 구조 (`WindPINN`)

- 입력: `WINDOW=20`(1초) × 9피처(vn, ve, roll/pitch를 yaw로 분해한 4값,
  wx/wy/wz 자이로) = 180차원, flatten
- `Linear(180→64) → ReLU → Linear(64→64) → ReLU → Linear(64→5)`
- 출력 5채널: 풍속(wind_vx, wind_vy) 2채널 + 회전 방해토크(tau_x, tau_y, tau_z) 3채널
- 손실: `L = L_data + 0.05 * L_physics_translation + L_physics_rotation`
  - `L_physics_translation`: `MSE(k|v_rel|v_rel, 유한차분 가속도)` — 학습 가능 항력계수 k
  - `L_physics_rotation`: 텔레메트리 물리잔차(`Jω̇ - τ_motor`)를 신경망 출력이
    설명하도록 하는 항 — 회전은 별도 라벨 없이 물리잔차 자체가 학습 신호

(모델 출력이 원래 2채널이었다가 5채널로 확장된 경위는 5절 "회전 확장" 참고)

---

## 3. 방법론적 진행 순서 (실패 → 재설계 → 성공의 흐름)

이 프로젝트는 첫 시도가 대부분 실패하고, 그 실패의 원인을 진단해 재설계하는
패턴이 반복됐다. 아래는 그 흐름을 압축한 것 — 괄호 안은 EXPERIMENTS.md 절 번호.

### 3.1 병진(위치) 보정: 3번의 구조 변경

1. **1차(실패, 12-3)**: 추정 바람만큼 position setpoint를 반대로 밀어줌 →
   setpoint 자체가 흔들리며 폐루프 발산(roll 수십도).
2. **2차(성공, 12-4)**: 목표 위치는 고정, **가속도만 추가**로 바꿈
   (`set_position_velocity_acceleration_ned`). PID가 위치오차를 이미 트림으로
   거의 다 잡고 있었기 때문에, 위치 자체를 흔드는 건 잘못된 지렛대였다는 게
   원인이었음. 강풍 온셋 피크 위치오차 0.47m→0.21m(-55%).
3. **3차(gust 확장, 12-6→12-18)**: 처음엔 gust(변동바람) 조건에서 MAE가
   1.8m/s로 튀어 "모델 구조 자체의 한계"로 오판했으나, 실제 원인은 학습
   데이터의 yaw(기수 방향) 범위가 너무 좁았던 것 — yaw 그리드로 재수집하자
   0.36m/s로 **5배 개선**됨. "구조적 한계"라는 결론 자체가 데이터 문제를
   구조 문제로 오진단한 사례.

### 3.2 회전(자세) 보정: 두 번의 발산과 그 원인

1. **오프라인 학습은 성공**(15세션, 2700조건 yaw 그리드, 물리잔차
   0.763±0.238) — 문제는 **실시간 폐루프에서 발산**(무풍에서도 21°+).
2. **원인 진단(12-16/17)**: 회전 물리손실이 "모델 출력이 물리방정식을
   만족하는가"만 검사할 뿐, 인과관계(무엇이 원인이고 무엇이 결과인지)를
   강제하지 않음. 학습 데이터가 전부 무보정 상태였기 때문에, 신경망이
   "자기 입력(자이로)을 미분해서 그대로 돌려주는" 지름길을 학습해버렸고,
   실시간으로 그 출력이 다시 자이로에 영향을 주면서 양성 피드백 루프가 형성됨.
   게인 스윕이나 저역통과 필터로는 못 고침(필터는 오히려 더 심하게 발산).
3. **해결(더더링)**: 데이터 수집 중에 실제로 무작위 토크를 주입(OU 프로세스)해
   "자기 입력 지름길"을 데이터로 봉쇄. 재수집·재학습 후 발산 완전히 해소,
   게인 재스윕(1.0→0.2)으로 **첫 순이익**을 확보한 것처럼 보였다(default
   +2.8~3.8%, strong +3.2%) — 다만 이 결론은 1회 측정 수준이었고, N=10
   재검증(3.9절)에서 뒤집혔다.

### 3.3 재현성 인프라: "5/5 개선"이 실은 노이즈였다

2026-08-19, 코드 셀프 리뷰(12-21) 과정에서 지금까지의 A/B 결과가 전부
**1회 측정**이었다는 게 지적됨. 반복측정 인프라(`repeat_sweep.sh`)를 새로
구축해 5회 반복해보니, 고정바람 A/B의 "5/5 개선" 결과가 **최대 20배의
표준편차**에 묻혀 재현이 잘 안 됨(12-22). 원인 추적 결과 조건 전환 시
안정화 대기시간(`CALM_SETTLE_S`/`PHASE_GAP_S`)이 3초/2초로 너무 짧았던
것으로 확정(12-23) — 5초/4초로 늘리자 표준편차가 대폭 줄고 방향성도 유지됨.

### 3.4 "추정이 정확해져도 제어 효과는 나빠질 수 있다" (가장 중요한 발견)

2026-08-20, 바람이 "고정↔변동"으로 전환되는 구간을 조사하려다 예상 못 한
문제에 부딪힘: 새로 수집한 데이터의 추정 오차가 배포 모델의 공인 성능보다
2.5~6배 나쁘게 나옴(12-27). PX4 버전 드리프트 가설을 세우고 실제로 구커밋으로
되돌려 재검증까지 했으나 기각됨. **진짜 원인(12-28)**: 배포 모델이 학습 중
쓰던 **더더링(무작위 토크 주입)에 의한 자이로 신호 통계**에 암묵적으로
의존하고 있었음 — 실비행에서는 더더링을 쓰지 않으므로 자이로 신호 분포가
학습 때와 달라 추정이 깨진 것.

더더링을 실비행 중에도 켜보는 임시방편은 시도했으나 실패(물리적 외란 자체가
비행을 더 나쁘게 만듦, 12-29). **최종 해결**: 더더링 데이터와 비더더링
데이터를 섞어서 재학습(12-30) — 비더더링 대조군 MAE가 1.37~1.58m/s →
**0.18~0.91m/s로 대폭 개선**됨.

그런데 여기서 두 번째 반전: 추정 정확도가 훨씬 좋아졌는데도, **실비행 A/B
개선율은 오히려 작아짐**(default +41.9%→-2.2%, strong +53.1%→+5.2%). 원인은
`ACCEL_GAIN` 등 보정 파라미터가 기존(부정확했던) 모델의 출력 스케일에
맞춰 튜닝돼 있었기 때문 — **추정 정확도와 "이 특정 게인으로 만드는 제어
효과 크기"는 별개 문제**라는 게 이 프로젝트에서 얻은 핵심 교훈 중 하나.

### 3.5 재튜닝 도중 발견한 스크립트 버그가 결과를 왜곡시킴

새 모델 기준으로 `ACCEL_GAIN`/`WIND_DEADBAND_MPS`를 재스윕하다, roll이
11°까지 튀는 등 물리적으로 말이 안 되는 결과가 나옴(12-31). 원인은 모델이나
게인이 아니라 **스윕 스크립트 자체의 버그**: "고정" 바람 조건인데도 매초
`gz topic pub`을 불필요하게 재발행하고 있었고, 그 재발행 자체가 실제 물리적
충격(1Hz 미니 돌풍)을 만들어내고 있었음. 고친 뒤 재측정하니 결과가 훨씬
안정적이고 해석 가능해짐 — `ACCEL_GAIN`을 0.15→0.05로 최종 확정.

같은 버그가 `wind_transition_sweep.py`에도 있었던 걸 확인·수정하고
재수집하니(12-32), 애초에 조사하려던 "전환 구간 특성" 질문에도 깨끗한 답이
나옴: gust→fixed 전환 시 ~1.5~2초간 일시적으로 나빠졌다 회복(WINDOW 크기로
설명됨), fixed→gust 방향은 스크립트 설계상 "경과시간"이 gust 사인파 위상과
뒤섞여 있어 분리 불가.

### 3.6 gust 재도전 — N=5로는 방향조차 못 믿는다는 걸 다시, 더 강하게 확인

비더더링 gust 데이터를 480조건 목표로 추가 수집·재학습(12-34~35). 이 과정
자체에서 실측 페이스가 예상(22초/조건)의 4배(90초/조건)였던 걸 뒤늦게 발견해
수집 오케스트레이션의 세션 타임아웃이 매번 완료 전에 강제종료되던 버그를
잡았고(12-31/34), SITL을 반복 재시작하다 GPS 락이 영영 안 되는 문제도 다시
겪었다(4번째 재발 - 3.7절 참고).

재학습 결과: gust 32조건 대조군(기존 8조건은 표본이 작아 새로 마련) 기준
풍속 MAE가 1.517→1.218m/s로 **20% 개선**됐다. 그런데 SITL A/B 5회 반복에서는
`crosswind_gust`만 5회 중 4회 심하게 악화(-69.1%±82.4%, 최악 -179.5%)돼 배포를
보류했다(12-36).

**여기서 반전이 한 번 더 나온다**: crosswind_gust만 따로 10회 재검증하니
그 악화가 **전혀 재현되지 않았다**(+0.8%±6.9%, 극단값 없음, 12-37). 나머지
3개 조건(light/default/strong_gust)도 각각 10회씩 재검증한 결과, **4개
조건 전부 중립**으로 귀결됐다(12-38/39) — 원래 5회 결과에서 유일하게
확실한 이득처럼 보였던 `strong_gust`(+26.4%±22.6%)도 10회로는 +1.0%±2.1%로
사라졌다. 방향이 정반대인 두 "극단값"(crosswind의 심한 악화, strong의 확실한
개선)이 같은 5회짜리 세션에서 동시에 나왔다는 사실 자체가, 그 세션에 결과를
흔든 무언가가 있었음을 시사한다. 결국 432조건 재학습 모델은 **오프라인
정확도는 개선됐지만 실비행 제어 효과는 4개 조건 어디서도 신뢰할 수 있게
나타나지 않아 배포하지 않기로 했다** — 3.4절의 교훈("추정 정확도와 제어
효과는 별개 문제")이 이번엔 통계적 유의성 문제로 한 번 더 재현된 셈이다.

### 3.7 "빌드 손상"이라는 오진단이 네 번 반복된 이유

세션형 수집/반복측정 스크립트가 SITL을 재시작할 때 `kill -9`를 반복하는
구조라, 이번 프로젝트 내내 "GPS 락이 영영 안 되는" 증상이 여러 차례
나타났다(12-16, 30, 34, 36). 매번 `make distclean` + 완전 재빌드(10~25분)로
"해결"해왔는데, 사실 이건 오진단이었다 — 진짜 원인은 컴파일된 코드가 아니라
`rootfs/parameters.bson`(런타임 설정 파일)이 `kill -9` 도중 덜 쓰인 채
손상되는 것이었고, 재빌드가 "해결"된 것처럼 보인 건 빌드 폴더를 통째로
지우면서 이 파일도 같이 삭제·재생성됐기 때문일 뿐이었다. 진짜 해결책은
`mv rootfs/parameters.bson{,.bak}` 후 재시작 — 사실상 즉시 끝난다. 이 근본
원인은 프로젝트 초반에 이미 대조실험(PX4 소스를 되돌려도 재현됨)으로 밝혀져
있었는데, 문서를 정리하는 과정에서 그 결론이 누락되면서 이후 세 번 더
"재빌드로 해결"을 반복했다 — **한 번 밝힌 근본 원인도 기록에서 빠지면
똑같이 재발한다**는 걸 실제로 겪은 사례. 예방책으로 MAVSDK
`Action.shutdown()`(진짜 정상 종료 명령, OS 시그널이 아님)을 세션 재시작
전 단계로 추가했다.

### 3.8 물리적으로 계산한 값이 항상 "쓸 수 있는" 값은 아니다

`TAU_FF_GAIN=0.2`는 원래 스윕으로 찾은 경험값이었는데, 이번엔 반대 방향에서
접근했다 — 더더링 데이터(학습 중 이미 알려진 크기의 토크를 주입하며 로터
반응을 같이 로깅해뒀던 것)로 "정규화 토크 1.0이 실제 몇 N·m인지"를 회귀로
직접 계산했다(12-40). 결과는 축마다 3.90~6.30(기존 0.2의 약 20배)로, 회귀
신뢰구간도 매우 좁아 계산 자체는 믿을 만했다. 그런데 SITL에서 그 근처
값(1.0, 계산값의 1/4~1/6)을 조심스럽게 시도해보니 이미 확실히 발산했다
(자세오차 -95.0%). 물리적으로 옳은 변환 배율이라도, 그걸 그대로 게인에
쓰면 **모델 추정치의 노이즈까지 같은 배율로 증폭**돼 피드포워드가 새로운
외란이 돼버린다 — "정확한 물리 상수"와 "노이즈 있는 실시간 추정치에 안전하게
쓸 수 있는 게인"은 다른 질문이라는 걸 보여준 사례.

### 3.9 병진/회전 5조건을 N=10으로 재검증 — 회전의 "첫 순이익"이 뒤집힘

gust에서 N=5→N=10으로 결론이 뒤집힌 걸 겪은 뒤(3.6절), 같은 검증을 병진/회전
5조건 A/B에도 적용했다(12-41) — 지금까지 4.2/4.3절 수치 대부분이 사실상 1회
측정이었기 때문. 병진은 방향이 유지됐다(strong의 "+38.2%"는 과대추정이었고
실제로는 +3.0%±5.1%, calm 제외 전 조건 순이익). **회전은 방향 자체가
뒤집혔다**: default -7.9%±3.5%, strong -3.6%±3.4%, light -6.5%±6.8% — 전부
표준편차가 작아 노이즈로 치부하기 어렵고, crosswind만 미약하게 양성
(+3.3%±4.0%, CI가 0에 걸침). 3.2절의 "첫 순이익"은 재현되지 않는다.

원인으로 의심되는 건 `TAU_FF_GAIN=0.2`가 12-17절(12-30 혼합 재학습 **이전**
구모델 기준)에서 확정된 뒤 한 번도 재스윕되지 않았다는 점 — 병진의
`ACCEL_GAIN`은 3.5절에서 이미 같은 이유로 재스윕(0.15→0.05)했지만, 회전
게인은 그 작업이 누락된 채로 남아 있었다. 3.4절의 교훈("게인이 특정 모델의
출력 스케일에 맞춰 튜닝돼 있으면, 모델이 바뀌면 재튜닝이 필요하다")이 회전
축에는 아직 적용되지 않았던 셈.

**재스윕 결과(12-42)**: 이 가설을 확인하려 `TAU_FF_GAIN`을 현재 모델 기준으로
재스윕했다 — {0.05, 0.10, 0.15, 0.20, 0.30}을 default+strong에서 N=5로 코스
스윕한 결과, **5개 값 전부 두 조건 동시 양성이 없었다**(0.30만 확실히
나쁨, 나머지는 전부 0 근처 노이즈). 가장 유망해 보인 0.10을 5조건 전체 N=10으로
검증하니, gain=0.20 대비 **5개 조건 전부 방향이 개선**됐다(light/strong은
음수→거의 중립, crosswind는 순이익 확대) — `ACCEL_GAIN`이 재학습 후 더 낮은
값을 필요로 했던 것과 같은 패턴이 실제로 존재함을 확인했다. 하지만 `default`는
gain=0.10에서도 여전히 확실하게 음수였다(-3.1%±2.8%, CI가 0에 안 걸림) — 스윕한
범위 안에 "5조건 전부 배포해도 되는" 값은 없었다. **회전 피드포워드는 게인
튜닝만으로는 해결되지 않는 것으로 잠정 결론, 현재 미해결 상태로 배포하지
않는다.**

---

## 4. 핵심 결과 요약

### 4.1 바람 추정 정확도 (풍속 MAE)

| 데이터 종류 | MAE | 비고 |
|---|---|---|
| 고정바람(초기, 좁은 yaw) | 0.37~0.5 m/s | |
| gust 포함(초기, 좁은 yaw) | 1.8 m/s | "구조적 한계"로 오판했었음 |
| gust 포함(yaw 그리드 재수집 후) | **0.355±0.018 m/s** | 5배 개선 |
| yaw 일반화(학습 그리드 밖 각도) | 0.229~0.241 m/s | |
| 비더더링 고정바람(혼합 재학습 전) | 1.37~1.58 m/s | |
| 비더더링 고정바람(혼합 재학습 후) | **0.18~0.43 m/s** | 3.7~7.5배 개선 |
| 비더더링 gust(혼합 재학습 후, 8조건 대조군) | 0.909 m/s | 부분 개선 (gust 비더더링 데이터 미포함, 표본 작아 신뢰도 낮음) |
| 비더더링 gust(432조건 재학습, 32조건 대조군) | **1.518→1.218 m/s** | 20% 개선 — 그러나 4.2절 A/B에서 제어 효과로 안 이어짐 |

### 4.2 병진(위치) 피드포워드 보정 A/B

| 조건 | 개선율 |
|---|---|
| 5조건(무풍/약/보통/강/옆바람), yaw그리드 모델 | 5/5 개선 (+20.1%~+77.7%) |
| gust 4조건, yaw그리드 모델 | 4/4 개선 (+9.4%~+60.5%) |
| 비더더링 혼합 재학습 모델, 재튜닝 전(gain=0.15) | default -2.2%(중립), strong +5.2%, crosswind +17.2% |
| 비더더링 혼합 재학습 모델, 재튜닝 후(gain=0.05) | default_fixed +6.6%, strong_fixed +38.2% |
| 432조건 재학습 모델, gust 4조건 (5회 반복, 최초 결과) | light +미측정, default +미측정, crosswind **-69.1%±82.4%**, strong **+26.4%±22.6%** — 방향 상반된 두 극단값 |
| 432조건 재학습 모델, gust 4조건 (10회 반복, 재검증 후) | light -4.7%±17.2%, default +1.6%±10.9%, crosswind +0.8%±6.9%, strong +1.0%±2.1% — **4/4 중립, 배포 안 함** |
| 현재 배포 모델, 고정바람 5조건 (10회 반복, 재검증 후) | calm -9.2%±37.6%(노이즈), light +10.4%±10.6%, default +1.9%±4.7%, strong +3.0%±5.1%, crosswind +14.3%±4.7% — **calm 제외 4/4 순이익 유지** |

### 4.3 회전(자세) 피드포워드 보정 A/B

| 단계 | 결과 |
|---|---|
| 더더링 도입 전 | 실시간 폐루프 발산(무풍에서도 21°+) |
| 더더링 도입 + 게인 재스윕(gain=0.2) | default +2.8~3.8%, strong +3.2% (첫 순이익, 1회 측정) |
| 5조건 확장 검증 (1회 측정) | 3/5 개선, 병진 4/5와 유사한 패턴 |
| yaw 180도 회전 검증 (1회 측정) | strong +5.0% |
| **5조건, 10회 반복 재검증 (gain=0.20, 12-41)** | calm -6.7%±40.3%(노이즈), light -6.5%±6.8%, default **-7.9%±3.5%**, strong **-3.6%±3.4%**, crosswind +3.3%±4.0% — **"첫 순이익" 재현 안 됨, 3개 조건에서 오히려 자세오차 증가** |
| gain 코스 스윕 (5값×2조건×N=5, 12-42) | 0.05~0.30 전부 default·strong 동시 양성 없음, 0.30만 확실히 나쁨 — 최선(least-bad) 후보는 0.10 |
| **5조건, 10회 반복 재검증 (gain=0.10, 12-42)** | calm -5.8%±13.5%(노이즈), light +0.4%±5.0%, default **-3.1%±2.8%**, strong -0.6%±2.3%, crosswind +4.9%±3.4% — gain=0.20 대비 **5/5 조건 방향 개선**되었으나 default는 여전히 확실한 손해, **배포 가능한 값을 찾지 못함** |

### 4.4 재현성 인프라가 바꾼 것

- `CALM_SETTLE_S`/`PHASE_GAP_S` 3s/2s → 5s/4s: A/B 반복측정 표준편차 **최대
  20배 감소**
- `gz topic pub` discovery 타이밍 이슈: 3회 재전송으로 완화 (25회 측정 중
  1회 실패 관측)
- `set_wind()` 반복 재발행 버그: 파라미터 스윕/전환구간 조사 두 곳 모두에서
  발견·수정, 물리적으로 불가능해 보이던 결과의 진짜 원인이었음
- **N=5 → N=10 재검증**: gust A/B 5회 반복에서 나온 두 "극단값"(crosswind
  심한 악화, strong 확실한 개선)이 10회 반복에서 둘 다 사라짐 — 반복측정
  인프라를 만든 것만으로는 부족하고, 반복 횟수 자체도 충분히 커야 함을 재확인

### 4.5 `TAU_FF_GAIN` 물리 단위 캘리브레이션

더더링 로그(주입 토크 기지값 vs. 로터 실측 반응)를 회귀해서 얻은 N·m↔정규화
변환 배율:

| 축 | 회귀 계수 (N·m/정규화단위) | 환산 게인 | 배포 게인(0.2) 대비 |
|---|---|---|---|
| roll | 0.256 ± 0.005 | ≈3.90 | 약 19.5배 |
| pitch | 0.159 ± 0.004 | ≈6.30 | 약 31.5배 |
| yaw | 0.238 ± 0.001 | ≈4.20 | 약 21배 |

SITL에서 gain=1.0(계산값의 1/4~1/6, 그래도 배포값의 5배)만 시험해봐도 이미
발산(자세오차 -95.0%, 4.35°→8.49°) — 배포 게인 0.2 유지.

---

## 5. 방법론적 교훈 (이 프로젝트에서 가장 재사용 가치가 높은 부분)

1. **"이상한 결과"는 대개 모델이 아니라 측정/인프라 문제다.** roll이 11°까지
   튀는 "불안정"은 실제로는 스윕 스크립트가 매초 불필요하게 바람을 재발행한
   부작용이었다. gust MAE 1.8m/s "구조적 한계"는 실제로는 yaw 범위가 좁은
   데이터 문제였다. 두 경우 다 "모델을 바꿔야 하나"로 오판할 뻔했다.
2. **A/B 테스트는 반복측정 없이는 방향조차 믿을 수 없다.** 1회 측정 "5/5
   개선"이 5회 반복하니 노이즈에 묻혔다 — 안정화 대기시간이 근본 원인이었다.
3. **물리손실이 있어도 인과관계를 보장하지 않는다.** 회전 물리손실은
   "출력이 방정식을 만족하는가"만 검사했지 "무엇이 원인인가"는 강제하지
   않아, 모델이 자기 입력을 미분해 돌려주는 지름길을 학습했다. 데이터
   수집 단계에서 실제로 무작위 외란(더더링)을 주입해야 이 지름길을
   봉쇄할 수 있었다.
4. **추정 정확도(MAE)와 제어 효과는 서로 다른 축이다.** 더 정확한 모델이
   반드시 더 나은 제어 결과를 만들지 않는다 — 제어 파라미터(게인/deadband)가
   특정 모델의 출력 스케일에 맞춰 튜닝돼 있다면, 모델이 바뀌면 재튜닝이
   필요하다.
5. **코드 중복은 버그 수정을 두 배로 만든다.** 4개 correction 스크립트에
   복붙돼있던 `set_wind()`의 버그를 한 파일에서 고치고 다른 파일에 있는
   같은 버그를 뒤늦게 따로 찾아야 했다 — 이후 공유 모듈로 정리(12-33).
6. **N=1~5는 A/B 방향조차 못 믿을 만큼 작은 표본일 수 있다.** gust에서는
   같은 5회짜리 세션에서 방향이 정반대인 두 "확실한" 결과가 동시에 나왔다가
   둘 다 10회로는 재현되지 않았다. 더 극단적인 사례는 회전 피드포워드다 —
   "첫 순이익"(default +2.8~3.8%, strong +3.2%)이라 결론 내렸던 게 사실은
   1회 측정이었고, N=10으로 재검증하니 **부호 자체가 반대**로 나왔다(default
   -7.9%±3.5%, strong -3.6%±3.4%, 표준편차가 작아 노이즈로 치부하기도 어려움,
   3.9절). 반복측정 인프라가 있다는 것과 반복 횟수가 충분하다는 것, 그리고
   "반복측정을 한 번이라도 했다"는 것과 "N이 충분했다"는 것은 전부 별개
   문제다.
7. **물리적으로 유도한 파라미터가 실전에 안전하다는 보장은 없다.** 노이즈가
   있는 실시간 추정치에 게인을 곱하면 그 노이즈도 같이 증폭된다 — "물리적으로
   옳은 값"과 "노이즈 있는 신호에 안전하게 쓸 수 있는 값"은 다른 질문이다.
8. **한 번 밝힌 근본 원인도 문서에서 빠지면 그대로 재발한다.** parameters.bson
   손상 문제는 프로젝트 초반에 이미 대조실험으로 근본 원인이 확인돼 있었지만,
   문서 정리 과정에서 그 설명이 누락되며 이후 같은 오진단("빌드가 손상됐다")과
   불필요한 재빌드(10~25분×수 회)를 반복했다.

---

## 6. 한계

- **SITL(시뮬레이션) 검증만 수행됨.** 실기(하드웨어) 비행 데이터 없음.
  Gazebo의 물리 모델(질량/관성/모터상수)이 실기와 다를 수 있음.
- **컨트롤러는 PX4 내장 PID 그대로.** MPC 등 PINN 추정치를 더 직접적으로
  활용하는 제어 구조는 시도 안 함.
- **회전 피드포워드는 현재 이 프로젝트 안에서 배포 가능한 설정이 없음(미해결).**
  N=10 재검증(3.9절)에서 "첫 순이익"(gain=0.2) 결론이 1회 측정의 착시였음이
  드러났고, 이후 gain 0.05~0.30을 재스윕(12-42)해도 5조건 전부에서 신뢰할 수
  있는 순이익을 내는 값을 찾지 못했다 — 가장 나은 0.10도 default 조건은
  여전히 확실한 손해(-3.1%±2.8%). 물리 변환배율(축당 3.90~6.30)도 회귀로
  구했으나 그 값 근처(1.0)에서도 SITL이 즉시 발산. 게인 조정만으로는 안 풀리는
  문제로 보이며, 추정치 자체의 노이즈를 줄이는(스무딩 등) 접근이 필요할 것으로
  추정(7절 참고).
- **432조건으로 비더더링 gust를 재학습해도 실비행 제어 효과가 안 나타남.**
  오프라인 정확도는 20% 개선(1.518→1.218m/s)됐지만 4개 gust 조건 전부
  10회 반복 A/B에서 중립 — 배포하지 않고 원본(고정바람만 비더더링 개선된)
  모델 유지 중.
- **`wind_at()` gust 위상이 매 구간 고정**이라 "전환 후 경과시간"과 "gust
  사인파 위상"이 뒤섞여, fixed→gust 방향의 전환 특성은 분리해서 답하지 못함.

## 7. 향후 과제

1. **회전 피드포워드 추정치의 노이즈를 줄이는 접근 탐색(스무딩 등).** 게인
   재스윕(12-42)으로는 5조건 전부를 만족하는 값을 못 찾았으므로, 게인을 더
   건드리기보다 추정치 자체를 다듬는 게 먼저일 가능성이 큼 — 그 다음에야
   물리 배율(3.90~6.30)에 더 가까운 게인이 안전해질 여지가 생김. 혹은 0.05
   아래로 더 낮춰보는 미세 스윕도 남아 있음(12-42 "다음에 볼 것").
2. 실제 하드웨어(Jetson + 컴패니언 컴퓨터) 이관 — 펌웨어 재빌드 → 통신/추론
   latency·모터 재캘리브레이션 확인 → 계류비행(OFF→낮은 gain ON) → 자유비행
   점진 확대
3. 캡스톤 보고서 그래프/표 시각화 (텍스트 서사는 이 문서로 완료)
4. correction_experiments 나머지 코드 중복(텔레메트리 모니터/오프보드 송신
   보일러플레이트) 정리

---

## 8. 결론

물리 정보 신경망으로 추정한 바람벡터를, PX4 내장 PID 위에 가속도/토크
피드포워드로 얹는 접근은 **병진(위치) 방향에서는** N=10 반복측정을 통과하는
일관된 순이익을 만들어냈다. **회전(자세) 방향은 초기엔 같은 성공처럼
보였지만, 반복측정 규모를 충분히 키우자 그 결론이 뒤집혔다** — 지금 게인으로는
신뢰할 수 있는 이득이 없고, 오히려 여러 조건에서 손해였다. 이 프로젝트에서
얻은 더 중요한 결과는 숫자 그 자체보다 **"왜 처음엔 안 됐는가", 그리고
"처음엔 됐다고 믿었던 게 정말 맞는가"를 추적하는 방법론**이었다 — 폐루프
발산의 인과관계 문제, 추정 정확도와 제어 효과의 분리, "모델이 이상하다"고
결론 내리기 전에 인프라·스크립트를 먼저 의심해야 한다는 것, 그리고
반복측정의 표본 크기 자체도 의심해야 한다는 것. 이 마지막 교훈은 반복해서
다른 형태로 재현됐다: 1회 측정 A/B가 안정화 대기시간 부족에 묻혔던 것도,
432조건 재학습 모델의 5회 반복 gust A/B가 정반대 방향의 두 극단값을 냈다가
10회로는 둘 다 사라진 것도, "첫 순이익"이라 확정했던 회전 피드포워드가
N=10에서 부호째 뒤집힌 것도, 결국 "표본이 부족하면 방향조차 믿을 수 없다"는
같은 문제의 다른 표현이었다. 이 프로젝트의 42개 실험 절(EXPERIMENTS.md) 중
상당수가 새로운 기법의 도입이 아니라 이런 오진단을 바로잡는 과정이었다는
점이 — 그리고 그 오진단 중 일부는 이 캡스톤 문서 자체에도 한동안 그대로
남아 있었다는 점이 — 어쩌면 이 캡스톤의 가장 실질적인 결과물이다.
