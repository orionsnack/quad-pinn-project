# PINN 기반 쿼드콥터 바람 외란 추정 및 보정 — 캡스톤 보고서 초안

**상태**: 초안(draft). 실험 상세 근거는 전부 [EXPERIMENTS.md](EXPERIMENTS.md)의 해당 절 번호를
따라가면 확인 가능. 이 문서는 그 40개 절을 발표/보고서용으로 압축·재구성한 것.

---

## 0. 한 줄 요약

PX4 SITL(Gazebo) 환경에서, 물리 정보 신경망(PINN)으로 쿼드콥터에 작용하는 바람을
실시간 추정하고, 그 추정치를 가속도 피드포워드로 비행 제어 루프에 주입해 위치
유지 성능을 개선했다. 병진(위치) 방향은 5개 바람 조건에서 일관된 순이익을
확인했고, 회전(자세) 방향까지 확장해 첫 순이익을 얻었다. 과정에서 "추정
정확도가 좋아져도 실제 제어 효과는 오히려 나빠질 수 있다"는 것과, "모델이
학습 데이터의 우연한 통계적 특징(더더링된 자이로 신호)에 암묵적으로 의존할 수
있다"는 것을 실측으로 발견·해결했다.

---

## 1. 서론

### 1.1 동기

쿼드콥터는 바람이 불면 위치를 유지하기 위해 기체를 기울인다(roll/pitch). 이
반응 자체는 PX4의 내장 PID 제어기가 처리하지만, 바람이 강하거나 갑자기 바뀌면
반응이 느려 위치오차가 커진다. 만약 "지금 바람이 얼마나 부는지"를 미리
알거나 실시간으로 추정할 수 있다면, 그 정보를 제어 루프에 피드포워드로
얹어서 반응 속도를 앞당길 수 있지 않을까 — 이것이 이 프로젝트의 출발점이다.

### 1.2 목표

1. 쿼드콥터의 텔레메트리(속도, 자세)만으로 현재 바람벡터를 실시간 추정하는
   신경망을 학습한다. 순수 지도학습이 아니라 항력 방정식(물리 법칙)을 손실
   함수에 포함시켜(PINN) 일반화 성능을 높인다.
2. 그 추정치를 실제 비행 제어 루프(PX4 SITL)에 연결해, 위치 유지 성능이
   실제로 개선되는지 A/B 테스트로 검증한다.
3. 세미나에서 다룬 회전 방정식(`Jω̇ = τ_motor + τ_disturbance`)까지 확장해,
   자세(roll/pitch) 안정성도 개선할 수 있는지 검증한다.

### 1.3 스코프

- 지배방정식: 병진 항력식 `a = k|v_rel|v_rel` (RAMP-Net 등 콜로케이션+자동미분
  기반 물리손실과는 다름 — 상세 비교는 EXPERIMENTS.md 12-15절)
- 컨트롤러: PX4 내장 PID + MAVSDK 오프보드 가속도/토크 피드포워드 (MPC 아님)
- 검증 환경: PX4 SITL(Gazebo Harmonic) 시뮬레이션만 — 실기 비행 데이터 없음
- PINN 역할: 동역학 모델을 대체하는 게 아니라, 기존 PID 위에 얹는 보조 신호

---

## 2. 시스템 구조

### 2.1 전체 파이프라인

```
[데이터 수집]              [오프라인 학습]                [실비행 제어 연결]
data_collection/*.py  →  train_wind_estimator.py  →  correction_experiments/*.py
     │                         │                              │
     ▼                         ▼                              ▼
logs/*.csv              wind_estimator.pt              logs/pinn_correction_*.csv
(바람조건×yaw그리드)      (WindPINN 가중치+정규화)         (A/B 결과)
```

### 2.2 실시간 제어 루프

```
PX4 SITL 텔레메트리(속도·자세·자이로)
        │  (MAVSDK 수신, 20Hz)
        ▼
최근 1초(WINDOW=20스텝) 상태 버퍼
        │
        ▼
WindPINN(state) → 추정 바람벡터 (wind_n, wind_e) [+ 추정 회전 방해토크 (tau_x,y,z)]
        │
        ▼
accel = -ACCEL_GAIN * wind_est  (deadband 적용)         tau_ff = -TAU_FF_GAIN * tau_est
        │                                                       │
        ▼                                                       ▼
MAVSDK set_position_velocity_acceleration_ned            PX4 firmware mc_rate_control.cpp
(위치는 고정, accel만 추가)                                MAVLink DEBUG_VECT("TAU_FF") 채널로 주입
        │                                                       │
        └──────────────────→ PX4 내장 PID → Gazebo 물리 시뮬레이션(바람 포함) ──┘
                                    │
                        (다시 텔레메트리로 피드백)
```

**핵심**: PINN은 컨트롤러가 아니라 PID 위에 얹는 "가속도/토크 힌트"만
계산한다. 실제 위치/자세 제어는 전부 PX4 내부에서 수행된다.

### 2.3 모델 구조 (`WindPINN`)

- 입력: `WINDOW=20`(1초) × 9피처(vn, ve, roll/pitch를 yaw로 분해한 4값,
  wx/wy/wz 자이로) = 180차원, flatten
- `Linear(180→64) → ReLU → Linear(64→64) → ReLU → Linear(64→5)`
- 출력 5채널: 풍속(wind_vx, wind_vy) 2채널 + 회전 방해토크(tau_x, tau_y, tau_z) 3채널
- 손실: `L = L_data + 0.05 * L_physics_translation + L_physics_rotation`
  - `L_physics_translation`: `MSE(k|v_rel|v_rel, 유한차분 가속도)` — 학습 가능 항력계수 k
  - `L_physics_rotation`: 텔레메트리 물리잔차(`Jω̇ - τ_motor`)를 신경망 출력이
    설명하도록 하는 항 — 회전은 별도 라벨 없이 물리잔차 자체가 학습 신호

(모델 출력이 원래 2채널이었다가 5채널로 확장된 경위는 5절 "회전 확장" 참고)

---

## 3. 방법론적 진행 순서 (실패 → 재설계 → 성공의 흐름)

이 프로젝트는 첫 시도가 대부분 실패하고, 그 실패의 원인을 진단해 재설계하는
패턴이 반복됐다. 아래는 그 흐름을 압축한 것 — 괄호 안은 EXPERIMENTS.md 절 번호.

### 3.1 병진(위치) 보정: 3번의 구조 변경

1. **1차(실패, 12-3)**: 추정 바람만큼 position setpoint를 반대로 밀어줌 →
   setpoint 자체가 흔들리며 폐루프 발산(roll 수십도).
2. **2차(성공, 12-4)**: 목표 위치는 고정, **가속도만 추가**로 바꿈
   (`set_position_velocity_acceleration_ned`). PID가 위치오차를 이미 트림으로
   거의 다 잡고 있었기 때문에, 위치 자체를 흔드는 건 잘못된 지렛대였다는 게
   원인이었음. 강풍 온셋 피크 위치오차 0.47m→0.21m(-55%).
3. **3차(gust 확장, 12-6→12-18)**: 처음엔 gust(변동바람) 조건에서 MAE가
   1.8m/s로 튀어 "모델 구조 자체의 한계"로 오판했으나, 실제 원인은 학습
   데이터의 yaw(기수 방향) 범위가 너무 좁았던 것 — yaw 그리드로 재수집하자
   0.36m/s로 **5배 개선**됨. "구조적 한계"라는 결론 자체가 데이터 문제를
   구조 문제로 오진단한 사례.

### 3.2 회전(자세) 보정: 두 번의 발산과 그 원인

1. **오프라인 학습은 성공**(15세션, 2700조건 yaw 그리드, 물리잔차
   0.763±0.238) — 문제는 **실시간 폐루프에서 발산**(무풍에서도 21°+).
2. **원인 진단(12-16/17)**: 회전 물리손실이 "모델 출력이 물리방정식을
   만족하는가"만 검사할 뿐, 인과관계(무엇이 원인이고 무엇이 결과인지)를
   강제하지 않음. 학습 데이터가 전부 무보정 상태였기 때문에, 신경망이
   "자기 입력(자이로)을 미분해서 그대로 돌려주는" 지름길을 학습해버렸고,
   실시간으로 그 출력이 다시 자이로에 영향을 주면서 양성 피드백 루프가 형성됨.
   게인 스윕이나 저역통과 필터로는 못 고침(필터는 오히려 더 심하게 발산).
3. **해결(더더링)**: 데이터 수집 중에 실제로 무작위 토크를 주입(OU 프로세스)해
   "자기 입력 지름길"을 데이터로 봉쇄. 재수집·재학습 후 발산 완전히 해소,
   게인 재스윕(1.0→0.2)으로 **첫 순이익** 확보(default +2.8~3.8%, strong +3.2%).

### 3.3 재현성 인프라: "5/5 개선"이 실은 노이즈였다

2026-08-19, 코드 셀프 리뷰(12-21) 과정에서 지금까지의 A/B 결과가 전부
**1회 측정**이었다는 게 지적됨. 반복측정 인프라(`repeat_sweep.sh`)를 새로
구축해 5회 반복해보니, 고정바람 A/B의 "5/5 개선" 결과가 **최대 20배의
표준편차**에 묻혀 재현이 잘 안 됨(12-22). 원인 추적 결과 조건 전환 시
안정화 대기시간(`CALM_SETTLE_S`/`PHASE_GAP_S`)이 3초/2초로 너무 짧았던
것으로 확정(12-23) — 5초/4초로 늘리자 표준편차가 대폭 줄고 방향성도 유지됨.

### 3.4 "추정이 정확해져도 제어 효과는 나빠질 수 있다" (가장 중요한 발견)

2026-08-20, 바람이 "고정↔변동"으로 전환되는 구간을 조사하려다 예상 못 한
문제에 부딪힘: 새로 수집한 데이터의 추정 오차가 배포 모델의 공인 성능보다
2.5~6배 나쁘게 나옴(12-27). PX4 버전 드리프트 가설을 세우고 실제로 구커밋으로
되돌려 재검증까지 했으나 기각됨. **진짜 원인(12-28)**: 배포 모델이 학습 중
쓰던 **더더링(무작위 토크 주입)에 의한 자이로 신호 통계**에 암묵적으로
의존하고 있었음 — 실비행에서는 더더링을 쓰지 않으므로 자이로 신호 분포가
학습 때와 달라 추정이 깨진 것.

더더링을 실비행 중에도 켜보는 임시방편은 시도했으나 실패(물리적 외란 자체가
비행을 더 나쁘게 만듦, 12-29). **최종 해결**: 더더링 데이터와 비더더링
데이터를 섞어서 재학습(12-30) — 비더더링 대조군 MAE가 1.37~1.58m/s →
**0.18~0.91m/s로 대폭 개선**됨.

그런데 여기서 두 번째 반전: 추정 정확도가 훨씬 좋아졌는데도, **실비행 A/B
개선율은 오히려 작아짐**(default +41.9%→-2.2%, strong +53.1%→+5.2%). 원인은
`ACCEL_GAIN` 등 보정 파라미터가 기존(부정확했던) 모델의 출력 스케일에
맞춰 튜닝돼 있었기 때문 — **추정 정확도와 "이 특정 게인으로 만드는 제어
효과 크기"는 별개 문제**라는 게 이 프로젝트에서 얻은 핵심 교훈 중 하나.

### 3.5 재튜닝 도중 발견한 스크립트 버그가 결과를 왜곡시킴

새 모델 기준으로 `ACCEL_GAIN`/`WIND_DEADBAND_MPS`를 재스윕하다, roll이
11°까지 튀는 등 물리적으로 말이 안 되는 결과가 나옴(12-31). 원인은 모델이나
게인이 아니라 **스윕 스크립트 자체의 버그**: "고정" 바람 조건인데도 매초
`gz topic pub`을 불필요하게 재발행하고 있었고, 그 재발행 자체가 실제 물리적
충격(1Hz 미니 돌풍)을 만들어내고 있었음. 고친 뒤 재측정하니 결과가 훨씬
안정적이고 해석 가능해짐 — `ACCEL_GAIN`을 0.15→0.05로 최종 확정.

같은 버그가 `wind_transition_sweep.py`에도 있었던 걸 확인·수정하고
재수집하니(12-32), 애초에 조사하려던 "전환 구간 특성" 질문에도 깨끗한 답이
나옴: gust→fixed 전환 시 ~1.5~2초간 일시적으로 나빠졌다 회복(WINDOW 크기로
설명됨), fixed→gust 방향은 스크립트 설계상 "경과시간"이 gust 사인파 위상과
뒤섞여 있어 분리 불가.

---

## 4. 핵심 결과 요약

### 4.1 바람 추정 정확도 (풍속 MAE)

| 데이터 종류 | MAE | 비고 |
|---|---|---|
| 고정바람(초기, 좁은 yaw) | 0.37~0.5 m/s | |
| gust 포함(초기, 좁은 yaw) | 1.8 m/s | "구조적 한계"로 오판했었음 |
| gust 포함(yaw 그리드 재수집 후) | **0.355±0.018 m/s** | 5배 개선 |
| yaw 일반화(학습 그리드 밖 각도) | 0.229~0.241 m/s | |
| 비더더링 고정바람(혼합 재학습 전) | 1.37~1.58 m/s | |
| 비더더링 고정바람(혼합 재학습 후) | **0.18~0.43 m/s** | 3.7~7.5배 개선 |
| 비더더링 gust(혼합 재학습 후) | 0.909 m/s | 부분 개선 (gust 비더더링 데이터 미포함) |

### 4.2 병진(위치) 피드포워드 보정 A/B

| 조건 | 개선율 |
|---|---|
| 5조건(무풍/약/보통/강/옆바람), yaw그리드 모델 | 5/5 개선 (+20.1%~+77.7%) |
| gust 4조건, yaw그리드 모델 | 4/4 개선 (+9.4%~+60.5%) |
| 비더더링 혼합 재학습 모델, 재튜닝 전(gain=0.15) | default -2.2%(중립), strong +5.2%, crosswind +17.2% |
| 비더더링 혼합 재학습 모델, 재튜닝 후(gain=0.05) | default_fixed +6.6%, strong_fixed +38.2% |

### 4.3 회전(자세) 피드포워드 보정 A/B

| 단계 | 결과 |
|---|---|
| 더더링 도입 전 | 실시간 폐루프 발산(무풍에서도 21°+) |
| 더더링 도입 + 게인 재스윕(gain=0.2) | default +2.8~3.8%, strong +3.2% (첫 순이익) |
| 5조건 확장 검증 | 3/5 개선, 병진 4/5와 유사한 패턴 |
| yaw 180도 회전 검증 | strong +5.0% — 우연이 아님을 확인 |

### 4.4 재현성 인프라가 바꾼 것

- `CALM_SETTLE_S`/`PHASE_GAP_S` 3s/2s → 5s/4s: A/B 반복측정 표준편차 **최대
  20배 감소**
- `gz topic pub` discovery 타이밍 이슈: 3회 재전송으로 완화 (25회 측정 중
  1회 실패 관측)
- `set_wind()` 반복 재발행 버그: 파라미터 스윕/전환구간 조사 두 곳 모두에서
  발견·수정, 물리적으로 불가능해 보이던 결과의 진짜 원인이었음

---

## 5. 방법론적 교훈 (이 프로젝트에서 가장 재사용 가치가 높은 부분)

1. **"이상한 결과"는 대개 모델이 아니라 측정/인프라 문제다.** roll이 11°까지
   튀는 "불안정"은 실제로는 스윕 스크립트가 매초 불필요하게 바람을 재발행한
   부작용이었다. gust MAE 1.8m/s "구조적 한계"는 실제로는 yaw 범위가 좁은
   데이터 문제였다. 두 경우 다 "모델을 바꿔야 하나"로 오판할 뻔했다.
2. **A/B 테스트는 반복측정 없이는 방향조차 믿을 수 없다.** 1회 측정 "5/5
   개선"이 5회 반복하니 노이즈에 묻혔다 — 안정화 대기시간이 근본 원인이었다.
3. **물리손실이 있어도 인과관계를 보장하지 않는다.** 회전 물리손실은
   "출력이 방정식을 만족하는가"만 검사했지 "무엇이 원인인가"는 강제하지
   않아, 모델이 자기 입력을 미분해 돌려주는 지름길을 학습했다. 데이터
   수집 단계에서 실제로 무작위 외란(더더링)을 주입해야 이 지름길을
   봉쇄할 수 있었다.
4. **추정 정확도(MAE)와 제어 효과는 서로 다른 축이다.** 더 정확한 모델이
   반드시 더 나은 제어 결과를 만들지 않는다 — 제어 파라미터(게인/deadband)가
   특정 모델의 출력 스케일에 맞춰 튜닝돼 있다면, 모델이 바뀌면 재튜닝이
   필요하다.
5. **코드 중복은 버그 수정을 두 배로 만든다.** 4개 correction 스크립트에
   복붙돼있던 `set_wind()`의 버그를 한 파일에서 고치고 다른 파일에 있는
   같은 버그를 뒤늦게 따로 찾아야 했다 — 이후 공유 모듈로 정리(12-33).

---

## 6. 한계

- **SITL(시뮬레이션) 검증만 수행됨.** 실기(하드웨어) 비행 데이터 없음.
  Gazebo의 물리 모델(질량/관성/모터상수)이 실기와 다를 수 있음.
- **컨트롤러는 PX4 내장 PID 그대로.** MPC 등 PINN 추정치를 더 직접적으로
  활용하는 제어 구조는 시도 안 함.
- **회전 피드포워드 게인(`TAU_FF_GAIN`)이 물리 단위로 정밀 캘리브레이션되지
  않음.** 1차 근사치(0.2)로, 안정성·순이익은 검증됐으나 N·m 단위 정밀
  캘리브레이션은 아직임.
- **gust 조건의 비더더링 추정 정확도가 아직 완전히 개선되지 않음**
  (0.909m/s, 고정바람의 7.5배 개선에 비해 1.7배에 그침 — 비더더링 gust
  데이터가 학습셋에 없었기 때문, 2026-08-21 밤 사이 수집 진행 중).
- **`wind_at()` gust 위상이 매 구간 고정**이라 "전환 후 경과시간"과 "gust
  사인파 위상"이 뒤섞여, fixed→gust 방향의 전환 특성은 분리해서 답하지 못함.

## 7. 향후 과제

1. `TAU_FF_GAIN` 물리 단위(N·m→정규화) 정밀 캘리브레이션
2. 비더더링 gust 데이터 수집으로 gust 추정 정확도 마저 개선 (진행 중)
3. 실제 하드웨어(Jetson + 컴패니언 컴퓨터) 이관 — 펌웨어 재빌드 → 통신/추론
   latency·모터 재캘리브레이션 확인 → 계류비행(OFF→낮은 gain ON) → 자유비행
   점진 확대
4. correction_experiments 나머지 코드 중복(텔레메트리 모니터/오프보드 송신
   보일러플레이트) 정리

---

## 8. 결론

물리 정보 신경망으로 추정한 바람벡터를, PX4 내장 PID 위에 가속도/토크
피드포워드로 얹는 접근은 병진·회전 양쪽에서 일관된 순이익을 만들어냈다.
다만 그 과정에서 얻은 더 중요한 결과는 숫자 그 자체보다 **"왜 처음엔 안
됐는가"를 추적하는 방법론**이었다 — 폐루프 발산의 인과관계 문제, 반복측정
없는 A/B의 위험성, 추정 정확도와 제어 효과의 분리, 그리고 "모델이 이상하다"고
결론 내리기 전에 인프라·스크립트를 먼저 의심해야 한다는 것. 이 프로젝트의
40개 실험 절(EXPERIMENTS.md) 중 상당수가 새로운 기법의 도입이 아니라 이런
오진단을 바로잡는 과정이었다는 점이, 어쩌면 이 캡스톤의 가장 실질적인
결과물이다.
