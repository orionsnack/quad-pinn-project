# PINN 바람 추정 + 보정 실험 상세 기록

시행착오 포함 전체 실험 히스토리(설계 이유, 실패한 시도, 근거 있는 결론)를 시간
순서대로 기록한 문서입니다. 프로젝트 요약/결과 하이라이트는 [README.md](README.md),
파일별 실행 방법은 [USAGE.md](USAGE.md), 환경 구축·트러블슈팅은
[setup_guide.md](setup_guide.md) 참고.

---

## 12-1. 목표

"바람 벡터(원인)를 직접 추정"하는 방식으로 설계함 (대안: setpoint 보정량을 바로
출력하는 end-to-end 방식도 고려했으나, 추정값 자체가 해석 가능하고 물리식과 엮기
쉬워서 이쪽을 선택):

```
상태 이력(속도/자세) --[PINN]--> 추정 바람벡터 --[물리식/게인]--> 보정량 --> Offboard 송신
```

## 12-2. 데이터 & 모델

- **데이터**: `wind_random_sweep.py`로 무작위 40개 바람 조건(0~10m/s, 0~360°) 수집.
  `gz topic`으로 직접 설정한 값이라 정답 바람벡터를 100% 정확히 앎(지도학습 가능).
- **입력 feature**: 최근 10스텝(0.5초, 20Hz) 윈도우의 `[vn, ve, roll, pitch]`.
  (`pos_err`는 처음 넣었다가 뺐음 — 12-3절 참고)
- **모델**: 작은 MLP(window*feature → 64 → 64 → 2), 출력은 바람벡터
  `[wind_vx_enu, wind_vy_enu]`.
- **물리 제약**: 항력 방정식 `a_drag = k * |v_rel| * v_rel`
  (`v_rel = wind_ned - v_drone_ned`)로 계산한 예측 가속도가 실측(속도 유한차분)
  가속도와 맞도록 하는 항을 loss에 추가. `k`는 학습 가능 파라미터로 둬서 데이터로부터
  스스로 찾게 함 → 최종 `k ≈ 0.089~0.096`.
  > 주의: Gazebo world wind는 ENU, 드론 로컬좌표는 PX4 NED라서 물리 잔차 계산 시
  > 축을 바꿔줘야 함(`wind_pinn_model.py`의 `physics_residual()` 참고 — 방정식/모델/
  > 하이퍼파라미터는 전부 이 파일에 모여있음).
- **학습 결과**(미학습 조건 8개 검증): 풍속 MAE **0.37~0.50 m/s**(평균 풍속 4.6m/s
  대비). validation MAE 최저 시점 체크포인트를 저장.
- **모델 파일**: `offline_training/wind_estimator.pt`(가중치 + 정규화 통계 + window/
  features 설정까지 저장돼 추론 스크립트가 그대로 로드).

## 12-3. 1차 시도(실패): position setpoint 보정 → 폐루프 발산

**설계**: 추정 바람에 `-GAIN`을 곱해 position setpoint에 더함("바람 반대방향으로
목표를 밀어 PID가 더 일찍 반응하게 유도").

**결과**: roll -18.6°, 위치오차 18.6m까지 발산.

**원인**: 학습 데이터에서 `pos_err`는 항상 "순수 바람으로 인한 오차"였는데, 보정을
켜면 그 일부가 **"보정 자신이 setpoint를 흔들어 생긴 추종 지연"**이 됨 → 모델이
"바람이 더 세졌다"고 오인 → 보정 확대 → 지연 확대 → 양성 피드백 발산. 게다가
PID가 이미 자세 트림만으로 정상상태 위치오차를 5cm 이내로 잡고 있어서, position
setpoint를 흔드는 방식 자체가 이 문제에 안 맞는 지렛대였음. feature 제거/클램핑/
스무딩을 다 적용해도 baseline(0.43m)을 못 넘어 **position 보정 방식 포기**.

## 12-4. 2차 시도(성공): 가속도 피드포워드로 구조 변경

**설계**: `set_position_velocity_acceleration_ned()`로 position/velocity setpoint는
**처음 값 그대로 고정**, 추정 바람 반대방향으로 **가속도만 추가** 실어 보냄. "어디로
갈지"는 안 흔들리고 "가는 길을 옆에서 밀어주는" 역할만 해서 폐루프 발산이 구조적으로
안 생김.

```python
accel_n = clip(-ACCEL_GAIN * wind_estimate_north, -MAX_ACCEL, MAX_ACCEL)
accel_e = clip(-ACCEL_GAIN * wind_estimate_east,  -MAX_ACCEL, MAX_ACCEL)
```

`ACCEL_GAIN=0.15`, `MAX_ACCEL_MPS2=2.0`은 실측 트림으로 역산한 `g*tan(tilt)`
(약 0.5~3 m/s²) 범위를 못 벗어나게 보수적으로 잡은 값(엄밀히 유도한 값은 아님).

**단일 조건(강풍 8.5m/s) 결과**: 바람 온셋 시 피크 위치오차 0.469m → 0.212m
(**약 55% 감소**), 발산 없음.

## 12-5. 다중 조건 검증 & deadband

`pinn_wind_correction_sweep.py`로 5개 조건 OFF/ON 비교(조건당 15초).

**1차**(deadband 없음): 4/5 개선, calm만 -67.5%(절대값 0.043→0.072m로 작지만
추정 잡음만으로 손해). **deadband 추가**(`apply_deadband()`: 추정 풍속이
`WIND_DEADBAND_MPS`=1.0 이하면 보정 0, 그 이상은 문턱값 뺀 만큼만 부드럽게 적용)
후 재검증:

| 조건 | 풍속(m/s) | OFF | ON | 개선율 |
|---|---|---|---|---|
| calm | 0.00 | 0.067m | 0.061m | +8.2% |
| light | 2.24 | 0.124m | 0.081m | +34.6% |
| default | 5.39 | 0.336m | 0.189m | +43.8% |
| strong | 8.54 | 0.585m | 0.375m | +35.9% |
| crosswind | 6.00 | 0.542m | 0.156m | +71.3% |

**5/5 조건 전부 개선**. 바람이 강할수록/순수 옆바람에 가까울수록 효과 큰 경향.
남은 한계: `ACCEL_GAIN`/`WIND_DEADBAND_MPS`가 손으로 정한 값, gust는 미검증
(12-6절), 전부 SITL 단일 프로세스 구조(실 하드웨어 이관 시 재검증 필요).

## 12-6. gust 확장 시도 - 당시엔 "구조적 한계"로 결론 (2026-08-18: 12-18절에서 뒤집힘)

`wind_gust_sweep.py`로 사인파 바람 데이터를 추가 수집해 기존 고정바람 데이터와
합쳐 재학습 시도.

**시행착오**: val_MAE가 0.37~0.5 → 1.82m/s로 악화. 모델 확대(hidden 128)·weight
decay 다 무효. 강풍 데이터 보강도 역효과. 풍속-오차 상관계수 0.578로 "고풍속만
어렵다"로도 설명 안 됨. **깨달은 것**: 조건 100개 안팎에서 단일 80/20 분할 평가는
분할에 따라 결과가 크게 흔들려 신뢰 불가.

**해결**: `train_wind_estimator.py --kfold N` 추가(N등분 반복 검증 + 평균±표준편차,
마지막엔 항상 단일 분할로 배포 모델도 저장).

**5-fold 결과**(고정바람 75+gust 27=102조건): wind_vx MAE 1.930±0.437,
wind_vy MAE 2.146±0.437, **speed MAE 1.794±0.520 m/s**. 5회 평균도 ~1.8m/s대라
"운 나쁜 분할"이 아니라 진짜 어려워진 것으로 판단.

**당시 결론**: 0.37~0.5m/s → 1.8m/s(평균풍속 7m/s 기준 약 26%)로 악화, 여러 방향
시도해도 못 뚫어 "지금 구조(작은 MLP+1초 히스토리)의 한계"로 잠정 정리. **이 결론은
나중에 틀린 것으로 확인됨 — 실제 원인은 좁은 yaw 범위 데이터였음(12-18절)**.

## 12-7. gust 조건 PINN 보정 A/B

**배경**: 12-5(고정바람 5/5 개선)는 전부 트라이얼 내내 바람이 고정인 조건. gust가
섞이면 추정 오차 자체가 1.8m/s로 나빠지는데(12-6), 이 상황에서도 보정이 순이익인지
미확인.

**방법**: `pinn_wind_correction_gust_sweep.py`. 12-5와 같은 4방향(calm 제외 —
base_speed=0이라 방향 미정의), `speed(t)=base*(1+0.6*sin(2πt/6))`(진폭 ±60%,
주기 6초), 트라이얼 18초. 나머지 파라미터는 12-5와 동일.

| 조건 | base(m/s) | OFF | ON | 개선율 |
|---|---|---|---|---|
| light_gust | 2.24 | 0.148m | 0.156m | -5.3% |
| default_gust | 5.39 | 0.346m | 0.230m | +33.6% |
| strong_gust | 8.54 | 0.601m | 0.459m | +23.5% |
| crosswind_gust | 6.00 | 0.459m | 0.323m | +29.7% |

**4개 중 3개 개선**. `light_gust`만 소폭 악화 — base 2.24m/s±60%면 순간풍속이
0.9~3.6m/s를 오가며 `WIND_DEADBAND_MPS`(1.0)를 계속 넘나들어 잡음이 완전히
걸러지지 않은 것으로 추정(→ 12-13절에서 새 모델로는 해소 확인).

## 12-8. `ACCEL_GAIN`/`WIND_DEADBAND_MPS` 튜닝 시도

**1차 프로브**(`pinn_correction_param_tuning.py`, one-factor-at-a-time):

| 스윕 | 값 | 결과 |
|---|---|---|
| deadband(light_fixed) | 0.5/1.0/1.5/2.0 | +49.8%/+56.0%/+34.2%/+56.8% |
| deadband(light_gust) | 0.5/1.0/1.5/2.0 | +33.7%/+20.5%/**-5.5%**/+28.8% |
| gain(default_fixed) | 0.05/0.10/0.15/0.20/0.30 | +35.0%/+41.0%/+58.3%/**+62.8%**/-11.8% |
| gain(strong_fixed) | 0.05/0.10/0.15/0.20/0.30 | +23.2%/+56.1%/+47.5%/**+69.9%**/+67.5% |

`deadband=0.5, gain=0.20`이 제일 좋아 보였음(0.30은 roll 불안정).

**2차 재검증 중 SITL 오염 발견**: calm OFF 베이스라인이 <7cm에서 0.580m로 튐 —
70분 넘게 재시작 없이 연속 실행된 게 원인으로 의심(미검증 가설). 재시작 후 재검증.

**3차 깨끗한 재검증**:

| 조합 | calm | light | default | strong | crosswind | 개선 조건 |
|---|---|---|---|---|---|---|
| 0.15/1.0(원래값) | +8.2% | +34.6% | +43.8% | +35.9% | +71.3% | **5/5** |
| 0.20/0.5 | **-98.0%** | +15.1% | +55.0% | +48.2% | +43.5% | 4/5 |
| 0.20/1.0 | -15.7% | **-27.6%** | +30.3% | +42.6% | +30.6% | 3/5 |

**결론**: gain↑/deadband↓는 강풍엔 좋지만 무풍/약풍엔 나빠지는 반대 트레이드오프 —
전역 상수로는 동시 최적화 불가. **원래 값(0.15/1.0)만 5/5**라 유지. 다음 단계는
상수 재탐색이 아니라 적응형 구조(→ 12-20절에서 시도, 실패로 결론).

> 2026-08-10: 이 결론은 구버전 체크포인트(`best_val_mae=2.83m/s`, 12-9절) 기준.
> yaw 그리드 새 모델로는 1차 프로브만 재실행(12-14절), 2차/3차는 미실행.

## 12-9. 배포 체크포인트가 "운 나쁜 학습"이었던 문제

커밋된 `wind_estimator.pt`가 `best_val_mae=2.83m/s`로 12-6절 5-fold 평균(1.79±0.52)
보다 뚜렷이 나쁨. 원인: 배포 학습은 train/val 분할 시드는 고정이지만 가중치 초기화/
미니배치 순서는 안 고정 — 재학습마다 결과가 다름. 재학습 두 번 해보니 val_MAE
2.030/2.034로 재현되고 5-fold 범위 안에 들어와 `wind_estimator.pt` 갱신.
(12-7/12-8절 결과는 전부 이 나쁜 체크포인트 기준 — 갱신 모델로는 미재검증)

## 12-10. yaw 일반화: 1차 수정(회전공식) 실패, 데이터 기반으로 재설계

**배경**: `roll_deg`/`pitch_deg`는 기체 좌표계라 yaw에 암묵적으로 묶이는데, 지금까지
학습 데이터가 전부 SITL 스폰 방향(75~93도) 근처에만 몰려있었음.

**1차 시도(실패, 확실)**: roll/pitch가 yaw에 대해 대칭 회전한다고 가정하고
`body_tilt_to_inertial()`을 유도해 재학습 → `wind_yaw_generalization_test.py`로
yaw=0도(학습 범위 밖) 검증 시 **wind_vx MAE 7.49, wind_vy MAE 8.78**(정상 2.0~2.3
대비 3~4배)로 완전 실패.

**왜 실패했는지는 불확실**(yaw 표본 2종류뿐이라 회귀 신뢰도 낮음) — "대칭 가정
방식은 실패했다"만 확실한 결론으로 취급, 원인 규명 없이 데이터 기반 재설계로 전환.

**참고**: 이 실패는 진짜 데이터 설계 문제(좁은 yaw 범위)가 맞음 — 12-9절의 체크포인트
문제(랜덤 초기화)나 SITL 드리프트 가설과는 별개 원인.

**2차 시도(데이터 기반)**: `roll_deg*cos(yaw)`, `roll_deg*sin(yaw)`,
`pitch_deg*cos(yaw)`, `pitch_deg*sin(yaw)` 4개 항(`yaw_decompose()`)을 feature로
주고 결합 방식은 신경망이 배우게 함 — 단, 학습 데이터의 yaw 자체가 다양해야 함:
- `wind_random_sweep.py` 확장: yaw 그리드(`--yaw-offset`)로 회전하며 수집
- `run_yaw_collection_sessions.sh` 신설: 세션을 짧게 쪼개 SITL 재시작하며 반복,
  세션마다 yaw offset을 밀어 합쳤을 때 촘촘한 그리드가 되도록 설계(예: 12방향×15
  세션×offset 2도 = 180지점/2도 간격). 세션을 짧게 쪼갠 건 SITL 드리프트 가설
  때문만은 아니고, 세션 하나가 죽어도 데이터 유실이 적다는 이점도 있어서.
- CSV는 조건마다 flush(장시간 무인 수집 중 죽어도 유실 최소화).

15세션·2700조건(yaw 0~358도 2도 간격) 수집 완료 → 결과는 12-11절.

## 12-11. yaw 그리드 재학습: 일반화 성공

15세션 2700조건으로 재학습(feature는 12-10절 `yaw_decompose()` 6개 그대로).
`best_epoch=28`(과적합 지연됨), `best_val_mae=0.469m/s` — 좁은 yaw 학습(12-2절,
0.37~0.50) 수준을 **전체 yaw 범위에서** 달성.

**일반화 검증**(`--yaw 91 --n 10`, 학습 그리드에 없는 각도):

| 지표 | 12-10절 1차 시도(yaw=0) | 새 모델(yaw=91) |
|---|---|---|
| wind_vx MAE | 7.49 m/s | **0.229 m/s** |
| wind_vy MAE | 8.78 m/s | **0.241 m/s** |

학습 그리드에 없는 각도인데 오히려 학습 val_MAE(0.469)보다 좋음 — **yaw 일반화
문제 해결 확인**.

## 12-12. 새 모델로 PINN 보정 A/B 재검증

12-5와 동일 조건·파라미터로 재실행:

| 조건 | 12-5(구모델) | 새 모델 |
|---|---|---|
| calm | +8.2% | -0.3%(노이즈 수준, 4cm 이내) |
| light | +34.6% | +41.7% |
| default | +43.8% | +40.3% |
| strong | +35.9% | **+49.2%** |
| crosswind | **+71.3%** | +66.7% |

4/5(구모델은 5/5) — calm만 사실상 "차이 없음"(설계상 deadband로 무풍엔 보정이 거의
안 걸림), 나머지는 확실히 유지. yaw 재학습이 보정 효과를 안 깨뜨림을 재확인.

## 12-13. 새 모델로 gust A/B 재검증 — light_gust 손해 사라짐

12-7과 동일 조건 재실행:

| 조건 | 12-7(구모델) | 새 모델 |
|---|---|---|
| light_gust | **-5.3%** | **+0.5%**(노이즈 수준) |
| default_gust | +33.6% | +11.1% |
| strong_gust | +23.5% | +26.9% |
| crosswind_gust | +29.7% | **+53.9%** |

**4/4**(구모델 3/4). light_gust 손해가 사라짐 — deadband 자체보다 **구모델의 추정
정확도(좁은 yaw 잡음)**가 원인의 상당 부분이었던 것으로 보임. default_gust 개선폭
축소(33.6%→11.1%)는 원인 미확인(1회 측정 변동성 가능성).

## 12-14. 새 모델로 파라미터 튜닝 1차 프로브 재검증

12-8 1차 프로브만 재실행(2차/3차는 미실행). 첫 시도는 timeout 짧게 잡아 강제종료 →
남은 mavsdk_server 좀비로 `arm() COMMAND_DENIED` 겪음 → SITL 완전 재시작 후 재실행.

| 스윕 | 값 | 12-8(구모델) | 새 모델 |
|---|---|---|---|
| deadband(light_fixed) | 0.5/1.0/1.5/2.0 | +49.8%/+56.0%/+34.2%/+56.8% | +45.8%/+7.1%/-11.8%/-9.0% |
| deadband(light_gust) | 0.5/1.0/1.5/2.0 | +33.7%/+20.5%/-5.5%/+28.8% | +1.3%/-13.9%/+6.7%/-28.1% |
| gain(default_fixed) | 0.05~0.30 | +35.0%/+41.0%/+58.3%/+62.8%/-11.8% | +52.5%/+48.5%/+16.4%/-18.8%/-26.0% |
| gain(strong_fixed) | 0.05~0.30 | +23.2%/+56.1%/+47.5%/+69.9%/+67.5% | -2.1%/+54.9%/+34.2%/+41.7%/+18.1% |

**해석**: 절대 수치는 크게 다르지만(1회 측정 노이즈 — 12-13절 같은 조합이 +0.5%→
이번 -13.9%로 재현 안 됨을 확인) **경향은 유지**: deadband는 낮을수록 약풍에 유리,
gain은 default_fixed에서 올릴수록 나빠지고(0.20 이상 손해로 반전, 구모델과 정반대)
strong_fixed에선 계속 좋음 — **"gain↑는 강풍엔 좋고 약~보통엔 나쁘다"는 12-8절
트레이드오프가 새 모델에서 오히려 더 뚜렷해짐**.

**결론**: 원래 값(0.15/1.0) 유지 결정 불변, 값 재탐색 대신 적응형 구조가 다음 단계.

## 12-15. RAMP-Net 논문 비교 검토, 향후 loss 설계 방향

**배경**: RAMP-Net(Sanyal & Roy, ICRA 2023)을 참고자료로 검토. PINN을 MPC 내부
동역학 예측모델로 써서 RK45 대비 20배 이상 빠른 실시간 비선형 MPC를 구현한 논문 —
콜로케이션 AD 물리손실 + 파라메트릭 불확실성 주입(Eq.9-10) + 데이터손실을 결합.

| | 지금 프로젝트 | RAMP-Net |
|---|---|---|
| 컨트롤러 | PX4 PID(가속도 피드포워드) | 직접 구현 비선형 MPC(ACADOS+CasADi) |
| PINN 역할 | 바람벡터 추정(보조 신호) | 동역학 모델 자체 대체 |
| PINN 입력 | vn,ve,roll,pitch | 상태 13차원 + 제어입력 4개 |
| 물리손실 | 유한차분 vs 항력식(AD 없음) | 콜로케이션+AD 도함수 |
| 지배방정식 | 병진 항력식 | 전체 비선형 동역학(Eq.8) |

**결정한 스코프**: 완전 재현(자체 MPC, PX4 우회, 베이스라인 비교)은 규모를 벗어남.
지배방정식·컨트롤러는 유지, **손실함수 설계만 부분 차용**.

**완전 재현 시 예상 기간**(스코프 결정 근거, 미실행): A~E(상태/입력 교체, AD
콜로케이션 등) 2~3주, F(MPC 자체 구현+실시간 폐루프) 1.5~3개월, G(베이스라인 4개+
평가 인프라) 1~1.5개월 → **합계 약 3~5개월**. F는 코드 속도가 아니라 폐루프
불안정성 디버깅 자체가 병목(12-3절 발산 사례가 훨씬 작은 스케일로 이미 보여준
패턴) — 이 추정이 스코프 축소 근거.

**차용 후보 3가지**(우선순위순, 미구현):
1. **파라메트릭 불확실성 주입**(Eq.9-10) — 고정 `k`에 학습 중 노이즈를 섞어 물리손실
   계산, gust MAE 벽 완화 기대(1순위)
2. **콜로케이션 포인트 추가**(Eq.6) — 정답 없는 `(wind, v_drone)` 조합에도 물리식
   만족 여부를 검사하는 순수 물리 포인트 추가
3. **데이터/물리 손실 비율 스윕**(Fig.4) — `LAMBDA_PHYSICS`(0.05)가 실제 최적인지
   근거 확보

참고: gust 문제(당초 ①번 동기)는 12-18절에서 데이터만으로 이미 해결됨 — ①~③은
이제 독립적인 손실함수 개선 아이디어로 남음.

---

## 12-16. 회전 방해토크(τ_disturbance) 확장: 오프라인 성공, 실시간 폐루프는 실패

**배경**: 세미나 원 과제(`Jω̇=τ_motor+τ_disturbance`를 PX4 PID에 피드포워드)를
병진과 결합. 회전은 별도 라벨이 없음 — `τ_disturbance=J·ω̇(실측)-τ_motor(계산)`이
텔레메트리에서 바로 나오므로 물리잔차 자체가 학습 신호.

**1단계 — 데이터 확장**: 자이로(20Hz)·액추에이터(`set_rate_actuator_output_status()`
명시 호출 필요 — 기본 스트림 rate 0) 로깅 추가. 액추에이터 단위를 0-1 정규화로
가정했다가 실측 검증(추력-질량 역산 오차 0.05~0.42%)으로 raw rad/s임을 확인, 잘못된
가정 제거.

**2단계 — 15세션 재수집 + 결합학습**: 회전 컬럼 포함 데이터 재수집(2026-08-12~15,
WSL 재부팅으로 세션 7→8 중단 후 재개). 세션 8에서 GPS 확인 반복 실패 —
`ESTIMATOR_CONST_POS_MODE` 고착(raw GPS는 정상인데 EKF2가 영구히 GPS 폴백).
`parameters.bson` 오염이 원인(반복 `pkill -9` 추정), 백업/재생성으로 해결. 처음엔
"클린 재빌드가 해결책"으로 오진단했으나(재빌드가 부수적으로 parameters.bson도
지웠던 것뿐), 코드를 원본으로 되돌려도 증상이 재현되는 대조실험으로 최종 확인
(setup_guide.md 참고).

결합학습(`LAMBDA_ROT=0.05`) + 5-fold:

| 지표 | 5-fold 평균±표준편차 | 단일분할(배포) |
|---|---|---|
| wind_vx MAE | 0.402±0.051 | 0.379 |
| wind_vy MAE | 0.407±0.031 | 0.430 |
| speed MAE | 0.247±0.017 | 0.232 |
| rot_omega_dot_residual | 0.763±0.238 | 0.818 |

병진은 fold간 편차 작음(±13%), 회전은 큼(±31%). best_epoch 2~13으로 매우 이름 —
`EPOCHS=400` 과함. **여기까지 오프라인 검증 성공**.

**3단계 — 실시간 폐루프 연결**: MAVSDK offboard엔 병진 같은 "각가속도 피드포워드"
슬롯이 없음(위치/속도는 yaw 각도만, 가속도는 xyz뿐 yaw 없음). 대신 **PX4 펌웨어
수정**: `mc_rate_control.cpp`의 `torque_setpoint` 계산 직후 MAVLink
`DEBUG_VECT("TAU_FF", x, y, z)`(기존 `debug_vect` uORB 토픽 재사용, 새 메시지
불필요)로 받은 값을 정규화 토크에 더하는 패치, 200ms 무갱신 시 자동 0 페이드아웃.
MAVSDK `mavlink_direct` 플러그인으로 Python에서 전송하는 구조 완성.

**캘리브레이션**: roll축 0.05/0.10/0.15 인가 → 실측 roll rate 0.0403/0.0858/0.1397
rad/s로 거의 선형 — 채널 작동 확인.

**A/B 1차(실패, 폐루프 발산)**: 3조건(calm/default/strong) OFF/ON. **3/3 악화**,
무풍조차 0.996°→21.296°로 발산. CSV 시계열: t=1.5s 작은 편차가 수 초 안에 클램프
(±0.25)까지 커짐. **원인**: 입력 feature `wx/wy/wz`(각속도)를 주입한 토크
피드포워드가 직접 바꿔서 **양성 피드백 루프** 형성. 오프라인 데이터가 전부 "보정
없는" 상태라 이 문제가 안 보였음. 병진(`vn/ve/roll/pitch`)은 이 되먹임이 없어
안전했음.

**수정**: `FEATURES`에서 `wx/wy/wz` 제거 후 재학습(omega_dot 학습 라벨은 CSV에서
계속 계산 — 모델 *입력*에서만 뺌). 회전 잔차 0.818→1.575로 악화, tau_dist 크기
0.059→0.023N·m(절반 이하) — 안정성 대가로 정확도 손실.

**A/B 2차(발산 해결, 순이익은 여전히 없음)**:

| 조건 | OFF | ON | 개선율 |
|---|---|---|---|
| calm | 0.405° | 1.484° | -266.8% |
| default | 4.197° | 6.401° | -52.5% |
| strong | 6.780° | 11.478° | -69.3% |

발산은 사라졌지만 **3/3 여전히 손해** — 각속도 제외로 정확도가 너무 떨어져
"방해 상쇄"보다 "잡음 추가" 수준. 회전은 병진과 달리 수백ms 단위 빠른 동역학이라
1초 윈도우 MLP로는 타이밍을 못 따라가는 것으로 추정.

**`TAU_FF_GAIN` 스윕**(default 고정, gain만 조절): 0.3→-16.6%, 0.6→-30.4%,
1.0→-52.5% — 거의 선형으로 gain 비례 손해, gain→0에서 손해도 0 수렴 = **순이익
나는 구간이 없음**. 튜닝으로 해결 불가, 추정치 자체가 잡음에 가깝다는 뜻으로 확정.
**결론 남**.

**LPF 각속도 재도입 시도(실패, 더 나빠짐)**: `ema_filter()`(시상수 0.5초) 추가,
`FEATURES`를 `wx_lpf/wy_lpf/wz_lpf`로 교체. 재학습 결과는 고무적(회전 잔차
0.841, 원본과 거의 동일, tau_dist 크기도 원본 수준)이었으나, SITL A/B(default,
gain=1.0)에서 **오히려 더 심하게 발산**(38.669° vs 각속도 완전제외의 6.401°).
**원인**: 저역통과 필터는 고주파 성분은 줄이지만 모든 주파수에 위상지연을 추가함
— 위상지연이 위상마진을 깎아, 필터의 되먹임 억제 효과보다 "한 박자 늦은 보정이
진동을 부추기는" 효과가 더 컸음(고전 제어이론의 전형적 실패 패턴). **결론**: 입력
신호 가공(무필터→발산, 완전제외→순이익없음, LPF→더 심한 발산) 3가지 다 실패,
안전한 버전(각속도 완전 제외)으로 롤백.

---

## 12-17. 근본 원인 재진단 + 토크 더더링(dithering)으로 발산 해결 (2026-08-15~16)

**"짧은 윈도우 서브모델" 재검토**: 사용자 질문을 계기로 12-16절 말미 제안을
다시 따져봄. 윈도우를 짧게 줄여도 "지금 이 순간"의 각속도는 여전히 입력에
포함되므로 되먹임 경로 자체는 안 끊김 — 오히려 평균화가 줄어 더 민감해질 위험.
이 후보는 정확도 개선 후보이지 안정성 해결책이 아니었음(잘못 분류됐었음).

**진짜 원인**: 회전 물리손실은 `tau_dist_pred`가 `J*omega_dot=tau_motor+
tau_dist_pred`를 만족하는지만 봄 — `tau_motor`(액추에이터)는 손실 계산에만 쓰이고
모델 입력엔 없음. 지금까지 모든 데이터가 "무보정 상태"라 관측된 각속도 변화 전부가
바람 때문이었고, 네트워크는 물리손실을 만족시키는 가장 쉬운 방법으로 **"자기 입력
(wx/wy/wz)을 유한차분해서 그대로 돌려주는" 지름길**을 학습해버림. 이게 자기 출력의
메아리를 보는 구조라는 게, 양성 피드백(원본)과 LPF 위상지연 악화 둘 다 정확히
설명함.

**해결책**: 데이터 수집 중 실시간으로 진짜 방해토크와 무관한 무작위 토크를 rate
controller에 주입(더더링). `tau_motor`는 `actuator_output_status`(인위적 토크
효과가 이미 반영된 실측값)로 계산하므로 라벨 공식은 그대로 정확함 — 지름길을
쓰면 더더링 구간에서 오차가 커지므로 진짜 외부 방해와 상관관계 있는 신호를 쓰도록
유도됨. **안전한 이유**: 이 더더링은 개루프(모델 추정치·텔레메트리에서 파생되지
않는 순수 무작위 OU 신호)라 자기 출력이 자기 입력에 되먹임되는 구조 자체가 없음.

**구현**: `wind_random_dither_sweep.py`(OU 프로세스, `sigma=0.10 tau_s=0.3s
clamp=±0.15`, DEBUG_VECT로 20Hz 주입) + `run_yaw_collection_dither_sessions.sh`.
스모크 테스트 후 80조건 검증 수집.

**소규모(80조건) 결과**: 오프라인 지표는 나빠짐(회전잔차 0.82→3.59, 데이터
33분의 1이라 당연)이지만 **SITL A/B 발산이 완전히 사라짐**:

| 조건 | 각속도 제외(12-16) | 더더링(80조건) |
|---|---|---|
| calm | -266.8% | -291.5% |
| default | -52.5% | **-3.5%** |
| strong | -30.0% | -30.0% |

default가 거의 중립까지 좁혀짐 — 방향이 맞다는 신호, 규모 확대 결정.

**전체 규모(15세션, 2700조건) 수집**: 무보정과 동일 규모로 재수집. 세션 2~6에서
`ESTIMATOR_CONST_POS_MODE` 고착 5연속 재발 — 리셋해도 2세션 만에 재발, 무보정
버전보다 훨씬 빠름(더더링 스크립트가 세션마다 DEBUG_VECT 20Hz 송신 후 kill -9로
종료되는 구조 때문으로 추정). `restart_sitl()`이 **매 세션 재시작마다 예방적으로
parameters.bson 리셋**하도록 수정(비용 거의 없음) → 세션 4~15 전부 1회 시도로 성공.

**전체 데이터(2780조건) 재학습 + 최종 A/B**:

| 조건 | 각속도 제외 | 더더링(80조건) | 더더링(2700+조건) |
|---|---|---|---|
| calm | -266.8% | -291.5% | -97.5% |
| default | -52.5% | -3.5% | **-2.7%** |
| strong | -30.0% | -30.0% | **-5.8%** |

발산 없음, default/strong 사실상 중립(초기 대비 손해 10배 이상 감소) — calm은
절대오차 0.4도로 노이즈 수준. 풍속 추정도 개선(speed MAE 0.417→0.281m/s). 회전
물리잔차는 데이터 33배에도 크게 안 좋아짐(3.59→3.06) — 더더링이 본질적으로 어려운
과제라는 뜻.

**`TAU_FF_GAIN` 재스윕(첫 순이익 달성)**: 더더링 이전엔 "gain 낮출수록 손해도
선형으로 0에 수렴"해서 튜닝이 막힌 방향이었지만, 그건 추정치 품질 자체가 나빴던
이전 모델 얘기 — 더더링으로 품질이 좋아진 이 모델로 재시도. default에서 0.5
(-4.1%) → 0.2(**+2.8%**, 재현 시 **+3.8%**)로 처음 부호가 바뀜. gain=0.2로 전체
재검증:

| 조건 | gain=1.0 | gain=0.2 |
|---|---|---|
| calm | -97.5% | -4.5%(절대오차 0.036°, 노이즈) |
| default | -2.7% | **+2.8~3.8%** |
| strong | -5.8% | **+3.2%** |

**2/3 조건 실제 개선, calm은 중립 — 회전 피드포워드 첫 순이익.** 기본 gain을
0.2로 갱신. (참고: gain=1.0에서 한 번 -958.3%가 나온 적 있는데, CSV 확인 결과
OFF phase 내내 roll/pitch가 0에 가까워 `gz topic pub`이 조용히 실패해 바람이 안
걸렸던 것 — 재실행으로 정상값 확보. 원시 시계열 재확인 습관이 여기서도 유효.)

**5조건 확장 검증**(calm/light/default/strong/crosswind로 확대):

| 조건 | 풍속 | OFF | ON | 개선율 |
|---|---|---|---|---|
| calm | 0.0 | 0.418° | 0.520° | -24.4% |
| light | 2.2 | 1.892° | 1.972° | -4.2% |
| default | 5.4 | 4.658° | 4.402° | **+5.5%** |
| strong | 8.5 | 7.255° | 7.012° | **+3.3%** |
| crosswind | 6.0 | 5.348° | 5.297° | **+1.0%** |

**5개 중 3개 개선** — 병진 A/B(12-5, 4/5)와 거의 같은 패턴(약풍은 잡음이 신호보다
커서 미세 손해, 절대오차 0.08~0.10°로 작음). gain=0.2가 우연이 아니라는 근거.

**yaw 일반화 검증**: CLI로 목표 yaw를 받아 회전·안정화하는 기능 추가. 스폰 yaw
(90~97도)에서 180도로 회전시켜 재검증:

| 조건 | 스폰 yaw | yaw=180 |
|---|---|---|
| default | +5.5% | +0.5% |
| strong | +3.3% | **+5.0%**(오히려 더 좋음) |

두 조건 다 다른 yaw에서도 개선 방향 유지 — 회전 모델은 body-frame 각속도(yaw
무종속)만 쓰므로 애초에 yaw 의존성이 구조적으로 없어야 한다는 설계 의도가 실측
확인됨.

**결론**: 세미나 원 과제가 실제로 작동하고 도움이 됨. 남은 것(우선순위 낮음):
`TAU_FF_GAIN` 물리 단위 정밀 캘리브레이션 정도.

---

## 12-18. gust 정확도 "구조적 한계" 결론 뒤집힘 — yaw 그리드로 5배 개선

**배경**: 12-6절의 "gust는 모델 구조 한계"라는 결론은 yaw 그리드 수정(12-10/11)
이전, 좁은 yaw 범위 gust 데이터 기준이었음 — yaw 그리드로 재수집해본 적이 없었음
(`wind_gust_sweep.py`는 12-10절부터 yaw 그리드를 지원했지만 미사용). 병진·회전
둘 다 yaw 다양성 부족이 원인이었던 전례로 재검토.

**실험**: `wind_gust_sweep.py` 기본값(12yaw×5조건=60에피소드, ~25분) 재수집 +
기존 더더링 고정바람(2780조건)과 합쳐 `--kfold 5` 재학습(12-6과 동일 방법론).

| 지표 | 12-6(좁은 yaw) | 이번(yaw 그리드) |
|---|---|---|
| speed MAE | **1.794±0.520** | **0.355±0.018**(5배 개선) |
| 회전 물리잔차 | (미실험) | 3.023±0.016 |

fold별 표준편차도 훨씬 작아짐(±0.520→±0.018) — 운이 아니라 진짜 개선. 회전 성능은
gust 없이 학습했을 때(3.06)와 거의 동일해 유지됨.

**배포 전 SITL 재검증**: 새 체크포인트로 회전 피드포워드(gain=0.2) 재확인 —
default +2.1%, strong +1.7%(더더링 전용 체크포인트의 +2.8~3.8%/+3.2%보다 약하지만
여전히 양수). 문제없어 배포.

**결론**: 12-6절 결론은 **틀렸음** — 병진 고정바람·회전·gust 셋 다 진짜 원인은
"모델 한계"가 아니라 "좁은 yaw 범위 데이터"였음. 뭔가 안 되면 모델 구조보다 데이터
다양성부터 의심할 것.

## 12-19. gust A/B 3차 재검증 — 전 조건 대폭 개선

12-18절 체크포인트로 `pinn_wind_correction_gust_sweep.py` 재실행(12-7/13과 동일
조건·파라미터):

| 조건 | 12-7(구모델) | 12-13(고정바람만 yaw그리드) | 12-19(gust도 yaw그리드) |
|---|---|---|---|
| light_gust | -5.3% | +0.5%(노이즈) | **+9.4%** |
| default_gust | +33.6% | +11.1% | **+53.8%** |
| strong_gust | +23.5% | +26.9% | **+51.8%** |
| crosswind_gust | +29.7% | +53.9% | **+60.5%** |

**4/4, 세 버전 중 가장 큰 폭**. light_gust가 확실한 개선 구간으로, default_gust
개선폭 축소(12-13절, 원인 미확인)도 완전히 회복 — 그 미확인 원인도 결국 gust
추정 부정확(좁은 yaw)이었을 가능성이 높음.

**결론**: 지금 배포된 체크포인트는 병진(고정바람+gust)·회전 4개 실험 전부 검증
완료 상태.

## 12-20. fixed-wind A/B 4차 재검증(5/5) + 적응형 게인 시도(실패)

**fixed-wind 재검증**: 적응형 게인 시험 전, 현재 체크포인트(고정바람+gust 둘 다
yaw 그리드)로 고정 `ACCEL_GAIN=0.15` 5조건 A/B를 아직 안 돌렸다는 걸 깨닫고 먼저
확인. **5/5 전부 개선**(calm +46.3%, light +20.1%, default +33.0%, strong
+56.1%, crosswind +77.7%) — 12-12절 4/5보다 좋아짐. gust 데이터를 섞은 재학습이
병진 고정바람 추정에도 정칙화 효과를 낸 것으로 보임(메커니즘 미확인).

**적응형 게인 시도**: 12-8절 트레이드오프를 풀어보려 추정 풍속에 비례해
`ACCEL_GAIN`을 0.10(약풍)~0.25(강풍) 선형으로 키우는 `adaptive_gain()` 추가
(`--adaptive` 플래그, 기본은 고정값 유지).

- 1차(스무딩 없음): 5/5→**3/5**(light -32.9%, default -8.1%). accel 명령
  step-diff가 고정 게인 대비 2배 이상(0.012→0.028) — 풍속 추정 잡음이 게인을
  거쳐 명령에 2중으로 전달됨.
- 2차(게인에 1초 스무딩 추가): 더 나빠짐 — **1/5**(calm -293.1%, light -60.2%,
  default -7.4%, strong -16.0%). 12-16절 LPF 실패와 정확히 같은 패턴 — 필터의
  위상지연이 잡음 증폭보다 더 나쁘게 작용.

**결론**: 적응형 게인은 스무딩 유무 상관없이 고정 상수보다 나쁨 — "게인을 노이즈
있는 추정치의 함수로 만드는 것" 자체가 문제(회전의 각속도 재도입 시행착오와 같은
계열). 12-8절 "단일 상수가 최선" 결론 재확인, 이 방향은 종료. 코드는
`USE_ADAPTIVE_GAIN` 기본 False로 안전하게 유지.

## 12-21. 코드/시뮬레이션 셀프 리뷰 — 보강 후보 (2026-08-18)

하드웨어 이관 전, 지금까지 쌓인 시뮬레이션 코드 자체를 훑어서 찾은 보강 후보
(실험 결과가 아니라 코드/인프라 품질 문제).

**심각도 높음**:
1. **PX4 펌웨어 패치가 diff로 저장 안 됨** — 회전 피드포워드의 핵심인
   `mc_rate_control.cpp` 수정(12-16절)이 `PX4-Autopilot` 워킹트리에만 있고, 이
   프로젝트 저장소엔 patch 파일이 없음(문서엔 산문 설명만 있음). PX4를 재클론하거나
   `make distclean` 하면 이 변경이 통째로 사라짐 — `git -C ~/MyProjects/PX4-Autopilot
   diff > rotation_ff.patch`로 뽑아서 커밋해둘 것.
2. **`correction_experiments/` 3개 스크립트 간 코드 중복** — `set_wind`,
   `WindCorrector`, `EmaSmoother`, `apply_deadband`가
   `pinn_wind_correction_sweep.py`/`pinn_wind_correction_gust_sweep.py`/
   `pinn_correction_param_tuning.py`에 거의 동일하게 복붙돼 있음. deadband 로직 하나
   고치려면 3곳 다 손대야 함 — 공통 모듈(`correction_common.py`)로 뽑아낼 것.
3. **`set_wind()`가 `gz topic pub` 실패를 조용히 무시** — `proc.wait()`만 하고
   리턴코드 미확인. 12-17절에서 이것 때문에 OFF 구간 내내 바람이 안 걸려
   `-958.3%`라는 결과가 실제로 한 번 나왔음(CSV를 손으로 까보고서야 발견) — 근본
   원인은 아직 안 고쳐짐, 리턴코드 확인 후 실패 시 예외 발생시킬 것.

**심각도 중간**:
4. **SITL A/B 검증이 전부 1회 측정, 반복/신뢰구간 없음** — 학습 쪽엔 `--kfold`가
   있는데 SITL A/B 스크립트들엔 반복측정 개념이 없음. 12-14/12-17절에서 같은
   조합·같은 코드로도 결과가 크게 안 재현되는 걸 실제로 여러 번 겪었으면서도 인프라로
   안 고쳐짐 — 조건당 N회 반복 후 평균±표준편차 내는 구조로 바꿀 것.
5. **`LAMBDA_PHYSICS`/`LAMBDA_ROT` 둘 다 0.05로 손 고정, 스윕 안 함**
   (`wind_pinn_model.py`) — `LAMBDA_ROT`는 주석에도 "손실 스케일이 다를 수 있어
   재조정 필요"라고 스스로 적어놓고 미실행(RAMP-Net 후보 3번과 동일 항목).
6. **순수함수 유닛테스트 전무** — `physics_residual`/`motor_torque`/
   `yaw_decompose`(좌표축 변환·부호처럼 실제로 몇 번 버그 났던 지점)에 회귀 테스트가
   하나도 없음. 지금까지는 SITL 실측값으로 손으로만 검증(모터 추력 0.05% 오차 확인
   등) — 방향은 맞지만 회귀 방지용으로는 안 남음.

**심각도 낮음(구조적 한계, 당장은 아님)**:
7. **모델이 여전히 "윈도우를 펼친 MLP"** — gust는 결국 yaw 데이터 부족 문제였지만,
   "MLP가 시간 흐름을 구조적으로 모른다"는 진단(12-16절, 회전이 1초 윈도우로 타이밍을
   못 따라감) 자체는 유효함. RNN/temporal conv는 후보로만 남고 미시도.
8. **실험 결과에 재현성 메타데이터(git commit hash, 모델 버전) 미기록** — CSV/
   체크포인트에 어느 코드 버전 산출물인지 기록이 없어 EXPERIMENTS.md가 사람이 수동
   추적하는 유일한 수단. 12-9절 "배포 체크포인트가 문서 기술과 다른 버전이었던" 사고가
   이미 한 번 있었음.

## 12-22. 반복측정 인프라 구축 — 고정바람 A/B "5/5 개선" 결과가 노이즈에 묻힘 (2026-08-19)

**배경**: 12-21절 4번 항목(SITL A/B가 전부 1회 측정)을 실제로 메우려고 반복측정
도구를 만듦. `PX4-Autopilot`이 이 머신에 없길래(빌드 폴더까지 통째로 없었음) 처음엔
"Claude Code가 별개의 머신에서 도는구나"로 오판하고 새로 클론·빌드했는데, 나중에
`~/.bash_history`에서 `rm -rf PX4-Autopilot/` + `conda env remove -n px4sim` 흔적을
발견함 — **원본 WSL(같은 머신)에서 예전에 정리 차원에서 지운 걸 다시 받은 것**이었음.
결과적으로 이번 재클론·재빌드가 "완전 클린 리빌드"였던 셈이라, sudo 없이 빌드
성공(시스템 패키지는 지워지지 않고 남아있었음)·MAVSDK 연결/GPS 락도 즉시 정상이었던
건 **`b604b7a`(12-16절 실시간 폐루프 절 참고)에서 진단했던 "확실한 해법은 완전
클린 리빌드"를 그대로 재현/확인해준 결과**로 해석하는 게 맞음.

**도구**: `sim_scripts/correction_experiments/repeat_sweep.sh` — 대상 스크립트를
N회 반복하며 매회 SITL을 완전히 새로 재시작(`run_yaw_collection_sessions.sh`의
`restart_sitl()`과 동일 로직 + 12-17절에서 배운 `parameters.bson` 예방적 리셋 포함),
`aggregate_repeats.py`로 조건별 개선율의 평균±표준편차를 낸다.

> 개발 중 버그 하나 발견/수정: `pkill -f "$TARGET_SCRIPT"`가 이 스크립트를 실행한
> 상위 프로세스(이 환경에서는 커맨드라인 텍스트에 스크립트 파일명이 그대로 노출됨)를
> 패턴 매칭으로 잘못 죽이는 사고가 실제로 남 — 파일명 하나만 단독으로 `pkill -f`
> 패턴에 쓰지 말고 반드시 더 구체적인 패턴(`bin/python -u <파일명>`)을 쓰도록 수정.

**방법**: `pinn_wind_correction_sweep.py`(고정바람 5조건, 배포 모델·`ACCEL_GAIN=0.15`/
`WIND_DEADBAND_MPS=1.0` 그대로)를 5회 반복.

**결과** (조건당 N=5, 매회 독립 SITL 인스턴스):

| 조건 | OFF 평균 | ON 평균 | 개선율 평균 | 표준편차 | 범위 |
|---|---|---|---|---|---|
| calm | 0.047m | 0.041m | +1.7% | 42.1%p | -73.3%~+43.7% |
| light | 0.096m | 0.123m | **-76.2%** | 99.5%p | -197.4%~+18.5% |
| default | 0.280m | 0.258m | +8.2% | 29.6%p | -37.1%~+40.5% |
| strong | 0.334m | 0.331m | +6.6% | 32.9%p | -32.8%~+54.4% |
| crosswind | 0.319m | 0.189m | +6.1% | 86.8%p | -163.6%~+68.4% |

**해석**:
- 12-20절 "5/5 전부 개선, +20.1%~+77.7%"는 노이즈 범위 안의 우연히 좋은 1회
  draw였을 가능성이 높음. 5회 평균은 방향(부호)은 5개 조건 다 양수지만, 표준편차가
  평균보다 훨씬 커서(예: strong +6.6%±32.9%p) "이 보정이 실제로 순이익"이라고
  통계적으로 확신하기 어려움 — 특히 light는 평균 자체가 **음수(-76.2%)**.
- **핵심 단서**: 개선율이 아니라 **OFF(보정이 아예 꺼진) 베이스라인 자체**가 반복마다
  크게 다름 — light 조건 OFF peak가 0.038m~0.145m로 약 4배 차이남. OFF 트라이얼은
  PINN 추정치를 전혀 안 쓰므로, 이건 "PINN 추정 잡음"이 아니라 **SITL 트라이얼
  설계 자체의 노이즈**(직전 조건의 잔여 속도/자세가 `CALM_SETTLE_S=3s`+
  `PHASE_GAP_S=2s`로 완전히 안 걷힘, 또는 gz sim 물리엔진의 트라이얼간 비결정성)가
  근본 원인일 가능성을 시사함. 정확한 원인은 미확정.

**중요 — "다른 머신이라 그런가"라는 의심은 배제됨**: 위 배경 정정 참고 —
이 반복측정은 사용자 원본 WSL과 "별개의 머신"이 아니라 **같은 머신에서 PX4를 새로
클론·빌드한 것**이므로, "환경별 CPU 스케줄링 지터" 같은 하드웨어 차이 가설은
성립하지 않음. 즉 이 큰 분산은 하드웨어 탓으로 돌릴 여지가 없고, **SITL 트라이얼
설계(코드) 자체의 노이즈**라는 결론에 더 힘이 실림. **따라서 "PINN 보정 효과가
가짜였다"는 결론은 아직 아님 — "지금까지의 1회 측정 방법론으로는 진짜 효과 크기를
신뢰성 있게 추정할 수 없었다"는 방법론적 결론임.**

**다음에 볼 것**:
- `CALM_SETTLE_S`/`PHASE_GAP_S`를 늘려서(예: 5s/4s) OFF 베이스라인 분산이 줄어드는지
  확인 — 줄어들면 "settle 시간 부족"이 원인이었다는 근거가 됨
- 시간을 두고(다른 날, 다른 SITL 재시작 세션) 5회를 다시 돌려서 이번 분산 크기가
  재현되는지 확인 — "완전 클린 리빌드 직후"라는 특수 조건이 이번 결과에 영향을
  줬을 가능성도 배제 못 함(예: 처음 몇 세션은 캐시가 안 데워진 상태 등)

CSV: `logs/pinn_correction_sweep_20260819_21*.csv`(5개),
집계: `logs/aggregate_summary_pinn_correction_sweep_20260819_210701.csv`.

**같은 방법으로 gust 4조건도 반복측정** (`pinn_wind_correction_gust_sweep.py` 5회):

| 조건 | OFF 평균 | ON 평균 | 개선율 평균 | 표준편차 | 범위 |
|---|---|---|---|---|---|
| light_gust | 0.155m | 0.133m | +12.1% | 15.1%p | -6.3%~+31.6% |
| default_gust | 0.320m | 0.243m | +20.5% | 27.6%p | -19.9%~+54.2% |
| strong_gust | 0.486m | 0.345m | +26.3% | 20.1%p | **+0.0%~+49.8%** |
| crosswind_gust | 0.585m | 0.279m | +51.0% | 12.1%p | +30.2%~+65.8% |

gust 쪽은 고정바람보다 결과가 훨씬 안정적임 — 4개 조건 평균이 전부 양수고, 방향(부호)이
12-19절 헤드라인 결과(light_gust +9.4%, default +53.8%, strong +51.8%, crosswind
+60.5%)와 대체로 일치함. 특히 **crosswind_gust**(범위 +30.2%~+65.8%, 5회 전부 양수)와
**strong_gust**(범위 0.0%~+49.8%, 5회 중 한 번도 음수가 안 나옴)는 표준편차가 평균보다
작아 통계적으로도 "순이익"이라고 말할 수 있는 수준. 반면 default_gust는 여전히 범위가
0을 가로질러(-19.9%~+54.2%) 확신하기 어려움. **결론**: 고정바람(위)만큼 심각하게
노이즈에 묻히진 않았지만, 조건에 따라 신뢰도 차이가 큼 — "gust 보정이 4/4 순이익"이라는
기존 결론은 crosswind/strong은 유지되나 light/default는 재확인 필요로 격하해야 함.

CSV: `logs/pinn_correction_gust_sweep_20260819_21*.csv`(5개),
집계: `logs/aggregate_summary_pinn_correction_gust_sweep_20260819_213008.csv`.

## 12-23. settle-time 가설 검증 — 분산의 진짜 원인이었음 (2026-08-19)

**배경**: 12-22절에서 고정바람 A/B의 극심한 분산(light -76.2%±99.5%p)을 발견하고,
`CALM_SETTLE_S`(조건 전환 시 무풍 안정화 대기)/`PHASE_GAP_S`(OFF→ON phase 전환
대기)가 각각 3초/2초로 너무 짧아서 직전 조건의 잔여 속도가 안 걷힌 채 다음 트라이얼이
시작되는 게 원인일 수 있다고 추정함(정확한 원인은 미확정으로 남겨뒀음).

**방법**: `pinn_wind_correction_sweep.py`에 `--calm-settle-s`/`--phase-gap-s` CLI
옵션 추가(`repeat_sweep.sh`도 `--` 뒤 인자를 대상 스크립트에 그대로 전달하도록 확장),
5s/4s(기존 3s/2s의 약 1.7배)로 5회 반복.

**결과** (3s/2s vs 5s/4s, N=5 각각):

| 조건 | 3s/2s 개선율(표준편차) | 5s/4s 개선율(표준편차) | 표준편차 감소 |
|---|---|---|---|
| calm | +1.7% (42.1%p) | +7.4% (21.8%p) | 약 2배 감소 |
| light | **-76.2%** (99.5%p) | **+12.4%** (15.6%p) | 약 6배 감소, 부호도 반전 |
| default | +8.2% (29.6%p) | +41.9% (**2.9%p**) | 약 10배 감소 |
| strong | +6.6% (32.9%p) | +53.1% (**3.2%p**) | 약 10배 감소 |
| crosswind | +6.1% (86.8%p) | +56.9% (**4.2%p**) | 약 20배 감소 |

**해석**: 압도적으로 명확한 결과 — settle 시간을 늘리자 default/strong/crosswind는
표준편차가 3~4%p까지 떨어져서(개선율 평균의 1/10 수준) 통계적으로 확신 가능한
수준이 됐고, light도 부호가 음수에서 양수로 뒤집혀 12-20절 원래 방향과 일치하게 됨.
**"1회 측정 결과가 안 믿을만했던 진짜 원인은 settle 시간 부족이었다"는 게 사실상
확정됨** — 12-22절에서 "정확한 원인 미확정"으로 남겨뒀던 게 이걸로 해소됨.

**조치**: `CALM_SETTLE_S`/`PHASE_GAP_S` 기본값을 `pinn_wind_correction_sweep.py`,
`pinn_wind_correction_gust_sweep.py`, `pinn_correction_param_tuning.py`,
`pinn_rotation_correction_test.py` 전부 3s/2s → 5s/4s로 상향(코드 커밋).

**다음에 볼 것**:
- 5s/4s로도 남은 미세한 분산(calm/light 20%p대)의 잔여 원인은 뭔지(gz sim 자체
  비결정성 등) — 우선순위는 낮음, 지금 수준으로도 실용적으로는 충분히 신뢰 가능

CSV: `logs/pinn_correction_sweep_20260819_23*.csv`(5개),
집계: `logs/aggregate_summary_pinn_correction_sweep_20260819_233947.csv`.

**gust 4조건도 같은 방법으로 재검증** (5s/4s, N=5):

| 조건 | 3s/2s 개선율(표준편차) | 5s/4s 개선율(표준편차) |
|---|---|---|
| light_gust | +12.1% (15.1%p) | +14.3% (15.8%p, 개선 없음) |
| default_gust | +20.5% (27.6%p, 0 가로지름) | +54.3% (**5.2%p**, 전부 양수) |
| strong_gust | +26.3% (20.1%p) | +50.4% (**2.3%p**) |
| crosswind_gust | +51.0% (12.1%p) | +54.5% (**4.5%p**) |

고정바람과 같은 패턴 — default_gust가 범위 -19.9%~+54.2%(불확실)에서 +48.3%~+62.9%
(5회 전부 확실히 양수)로 바뀜. `light_gust`만 settle 시간을 늘려도 안 좋아짐(15%p대
유지) — 약한 바람 자체의 신호 대비 잡음비 문제로, settle-time과는 다른 원인으로 보임.

**회전 5조건도 재검증하다가 데이터 오염 1건 발견**: 5회 중 1회(`strong` 조건)에서
OFF baseline이 0.660deg로 나옴(나머지 4회는 6.7~7.2deg) — **바람이 사실상 안 걸린
채로 트라이얼이 진행된 것으로 보임**. `set_wind()`는 리턴코드 체크를 이미
추가했는데도(12-21/12-22절) `returncode=0`인데 바람이 실제로는 반영 안 되는
케이스가 남아있다는 뜻 — **리턴코드 확인만으로는 이 클래스의 실패를 100% 못
잡는다는 새로운 발견**. 이 1개 시행을 이상치로 제외하고 재계산:

| 조건 | 개선율 평균(표준편차) | 범위 | 12-17절 원본 방향 |
|---|---|---|---|
| calm | -4.6% (13.8%p) | -27.9%~+4.5% | 음수(일치) |
| light | -7.7% (9.9%p) | -21.6%~+3.5% | 음수(일치) |
| default | +0.4% (2.6%p) | -2.3%~+3.7% | 양수(약하게 일치, 노이즈 수준) |
| strong(N=4, 오염분 제외) | -0.4% (3.0%p) | -4.3%~+2.3% | 양수(노이즈 수준이라 사실상 중립) |
| crosswind | **+7.7%** (4.5%p) | **+2.5%~+14.9%**(전부 양수) | 양수(강하게 일치) |

**해석**: crosswind는 5회 전부 양수로 확실한 순이익, calm/light는 5회 대체로 음수로
원본 방향과 일치(회전 피드포워드가 약풍/무풍에선 손해라는 12-17절 결론 재확인),
default/strong은 0 근처 노이즈 수준(원본에서도 절댓값이 작았던 조건들과 일치). 전체
그림이 12-17절 원본과 정성적으로 잘 맞음 — **settle-time 상향이 회전 쪽에도
유효했고, 복구된 패치가 원본과 동일하게 작동한다는 12-24절 결론이 더 탄탄해짐.**

**남은 것**: `strong` 조건의 오염 시행 같은 "리턴코드는 정상인데 바람이 실제로
안 걸리는" 케이스의 정확한 원인 규명 — 우선순위 낮음(1/25회로 드묾), 하지만
재발하면 telemetry로 실제 풍속 반영 여부를 직접 확인하는 검증 단계를 추가할 필요.

CSV: `logs/pinn_correction_gust_sweep_20260820_00*.csv`,
`logs/pinn_rotation_correction_20260820_0*.csv`, 집계:
`logs/aggregate_summary_pinn_correction_gust_sweep_20260820_001259.csv`,
`logs/aggregate_summary_pinn_rotation_correction_20260820_004717.csv`
(회전 쪽 집계 CSV는 오염 시행 포함 원본 수치임 — 위 표는 손으로 재계산한 값).

## 12-24. PX4 펌웨어 패치 유실 발견 및 랩실 PC에서 복구, 반복측정으로 재검증 (2026-08-19)

**발견**: 12-21절 1번 항목(펌웨어 패치가 diff로 저장 안 됨)이 실제로 현실이 된 걸
확인함 — 12-22절 작업 중 새로 클론한 `PX4-Autopilot`에 회전 피드포워드 패치
(`mc_rate_control.cpp`/`.hpp`, 12-16절)가 전혀 없었음. 처음엔 "별개의 SITL
환경이라 그렇다"로 오판했으나(12-22절 정정 참고), 실제로는 **이 머신이 랩실 PC와
동일한 원본 환경이고, 예전에 `PX4-Autopilot`을 `rm -rf`로 정리하면서 같이
지워진 것**이었음.

**복구**: 다행히 원본 작업이 실제로는 **랩실 PC**(이 프로젝트가 원래 개발되던
물리적으로 다른 컴퓨터)에도 별도로 남아있었음 — 거기 로컬 브랜치
(`rotation-feedforward-patch`, 커밋 `472bcbc3cf`)에 패치가 커밋된 채로 보존돼
있었음. `git show 472bcbc3cf > rotation_ff.patch`로 추출해 `quad-pinn-project`에
커밋(`rotation_ff.patch`, 저장소 루트) → 이 머신에서 `git apply`로 재적용 →
재빌드까지 성공.

> 과정에서 겪은 잡음: 랩실 PC의 야간 DNS 문제로 `git push`가 막혀서 Windows
> PowerShell로 `\\wsl.localhost\Ubuntu-24.04\...` UNC 경로를 거쳐 우회 push함 —
> 이 과정에서 `core.fileMode` 관련 mode-bit 잡음(9P/CIFS 브릿지가 실행권한
> 비트를 못 지킴)도 겪어서 `git config core.fileMode false`로 해결.

**검증 — 5회 반복 A/B** (`pinn_rotation_correction_test.py`, `repeat_sweep.sh`로
5회, gain=0.2):

| 조건 | 개선율 평균 | 표준편차 | 범위 | 12-17절 원본(1회) |
|---|---|---|---|---|
| calm | +5.6% | 12.1%p | -12.5%~+20.0% | -24.4% |
| light | **-6.7%** | 7.7%p | **-22.1%~-2.2%**(5회 전부 음수) | -4.2% |
| default | +3.2% | 3.5%p | -2.0%~+8.1% | +5.5% |
| strong | -1.1% | 1.2%p | -2.6%~+0.4% | +3.3% |
| crosswind | **+6.7%** | 3.0%p | **+1.6%~+10.6%**(5회 전부 양수) | +1.0% |

**해석**: 고정바람(12-22절)만큼 극심한 노이즈는 안 나옴 — `light`(전부 음수)와
`crosswind`(전부 양수)는 방향이 5회 내내 일관돼서 통계적으로도 확신 가능한 수준.
두 조건 다 12-17절 원본과 부호가 일치함(light 손해, crosswind 이익). `default`도
대체로 양수. `calm`/`strong`은 0 근처에서 왔다갔다(원본에서도 절댓값이 제일
작았던 조건들). **결론: 복구된 패치가 원본과 동일한 물리적 동작을 한다는 게
반복측정으로 확인됨** — 12-21절 1번 항목의 위험이 실제로 현실화됐다가 완전히
해소된 사례.

**교훈**: 원격지(랩실 PC 등)에 별도로 남아있는 사본을 확인하지 않고 "유실됐다"고
성급히 결론내릴 뻔했음 — 여러 물리적 위치에서 작업하는 프로젝트는 "어디에 뭐가
있는지"부터 확인하는 게 우선.

CSV: `logs/pinn_rotation_correction_20260819_23*.csv`(5개),
집계: `logs/aggregate_summary_pinn_rotation_correction_20260819_231355.csv`,
패치 파일: `rotation_ff.patch`(저장소 루트).

## 12-25. `gz topic pub` 침묵 실패 재발 원인 진단 및 재전송으로 완화 (2026-08-20)

**배경**: 12-23절 회전 재검증에서 발견한 "리턴코드는 정상인데 바람이 실제로 안
걸린" 1건(25회 중 1회)의 근본 원인을 조사함.

**가설**: `gz topic pub`은 매 호출마다 새 프로세스로 뜨는 1회성 CLI임 — 메시지를
실제로 구독자(바람 플러그인)에게 전달하려면 먼저 gz-transport의 구독자 탐색
(discovery, UDP 멀티캐스트 기반이라 보통 수백ms 걸림)이 끝나야 함. 프로세스가
discovery 완료 전에 메시지를 큐에 넣고 바로 종료하면, **CLI 자체는 성공(exit 0)을
보고하지만 실제로는 아무 구독자에게도 안 갔을 수 있음** — 이게 리턴코드 체크만으로
못 잡히는 이유.

**조치**: `set_wind()`가 같은 값(멱등)을 150ms 간격으로 3번 재전송하도록 수정 —
한 번이라도 discovery 완료 후에 도착하면 됨. 8개 파일(`wind_random_sweep.py`,
`wind_gust_sweep.py`, `wind_random_dither_sweep.py`,
`wind_yaw_generalization_test.py`, `pinn_wind_correction_sweep.py`,
`pinn_wind_correction_gust_sweep.py`, `pinn_correction_param_tuning.py`,
`pinn_rotation_correction_test.py`) 전부 동일하게 수정.

**검증**: 회전 A/B 1회 실행(타임아웃 300초 안에 4/5 조건 완료, 5번째 조건 도중
시간 초과로 끊김) — 4개 조건 전부 OFF 베이스라인이 정상 범위(0.3~7.0deg, 이상치
없음)로 나옴. 다만 25회 중 1회(약 4%) 발생하던 드문 문제라, 이번 1회 시행으로
"완전히 재발 안 함"까지 확인하긴 어려움 — 재발 여부는 향후 반복측정에서 계속
지켜볼 것.

**한계**: 근본적으로 `gz topic pub`을 1회성 CLI로 매번 새로 띄우는 구조 자체가
이 문제의 원인이라, 재전송은 완화책이지 완전한 해결책은 아님. 확실히 없애려면
지속적인 gz-transport 퍼블리셔(프로세스를 안 새로 띄우고 계속 열어두는 방식)로
바꿔야 하는데, 지금 구조(매 조건 전환마다 짧게 호출)에는 과한 리팩터링이라 보류.

CSV: `logs/pinn_rotation_correction_20260820_014603.csv`(검증용 1회, 5번째
조건은 미완주).

---

## 12-26. RAMP-Net 파라메트릭 불확실성 주입 — early stopping 추가 후 밤새 스윕 착수 (2026-08-20)

**배경**: 12-15절 후보 1순위(`k_noise_sigma`, 12-22절에서 이미 구현)를 제대로 된
규모로 돌려보려 함. 전날(2026-08-19) 시도는 `EPOCHS=400`이 105분 안에 fold 1개도
못 끝내서 중단했었음 — 처음엔 "12-16절에서 이미 `best_epoch=2~13`이라 400이 과함을
알고 있었는데 그대로 뒀다"고 판단해 `EPOCHS=80`으로 낮추려 했으나, **실제 배포된
체크포인트의 메타데이터를 열어보니 `best_epoch=187`**이었음 — 12-16절 수치는 더
작은 옛날 데이터셋 얘기였고 지금(더더링+gust 전체, 2840조건) 데이터엔 안 맞는
기준이었음. `EPOCHS=80`으로 고정했으면 전부 덜 수렴된 채로 잘렸을 뻔함.

**해결책**: 고정 epoch 수를 추측하는 대신 **early stopping**을 추가함(`PATIENCE=50`,
val_MAE 개선 없이 50epoch 지나면 중단, `EPOCHS`는 안전망으로 그대로 둠) — 데이터셋/
sigma마다 실제 수렴 시점이 달라도 자동으로 맞춰짐. 소규모 데이터로 스모크
테스트(kfold=2, sigma=0.05)해서 정상 작동 확인(epoch 100에서 조기종료, best_epoch=50).

**배포 모델 보호**: `train_wind_estimator.py`는 `--kfold` 진단과 별개로 항상 단일
80/20 분할로 배포용 `wind_estimator.pt`를 재학습·덮어씀 — sigma를 여러 개 돌리면
매번 이 파일이 바뀜. `sweep_k_noise_sigma.sh` 신설: 매 sigma 실행 전 원본을
복원하고, 실행 후 결과를 `sweep_TIMESTAMP/wind_estimator_sigma_X.pt`로 따로 저장 —
스윕이 끝나도 배포된 파일은 원본 그대로 유지되고, 어떤 sigma를 실제로 쓸지는
결과 보고 사람이 따로 결정.

**실행**: sigma = 0.0/0.02/0.05/0.1 순차 실행, 각각 5-fold + 배포용 단일분할 학습.
SITL 미사용(순수 CPU 학습)이라 장시간 무인 실행에 안전함 — 밤새 무인 실행 착수,
아침에 결과 확인 예정. gust MAE 개선 여부가 핵심 판단 기준.

**결과** (2026-08-20 아침, 4개 sigma 전부 완료, 약 1시간45분 소요 — early stopping
덕에 전날 예상보다 훨씬 빨랐음): 5-fold 평균±표준편차 (더 안정적인 단일 분할이
아니라 이 수치가 진짜 비교 기준):

| sigma | wind_vx MAE | wind_vy MAE | speed MAE | 회전 잔차 |
|---|---|---|---|---|
| 0.0(베이스라인) | 0.434±0.021 | 0.440±0.023 | 0.361±0.023 | 3.026±0.015 |
| 0.02 | 0.431±0.022 | 0.441±0.019 | 0.359±0.018 | 3.031±0.020 |
| 0.05 | 0.435±0.020 | 0.441±0.018 | 0.361±0.017 | 3.029±0.015 |
| 0.1 | 0.428±0.020 | 0.446±0.023 | 0.362±0.018 | 3.020±0.024 |

**해석**: 4개 sigma 전부 서로의 표준편차 범위 안에 들어옴 — **측정 가능한 차이가
없음**. sigma=0.02가 speed MAE 기준 근소하게 가장 낮지만(0.359 vs 0.361), 차이가
표준편차(0.017~0.023)보다 훨씬 작아 노이즈 수준. **파라메트릭 불확실성 주입은 이
설정(sigma 0.02~0.1)에서 효과가 없다는 게 5-fold로 확인됨** — 이건 노이즈 때문에
결론을 못 내린 게 아니라, 실제로 효과가 없다는 통계적으로 근거 있는 null result임.

**왜 효과가 없었을지 (추정)**: 12-15절에서 이 실험의 원래 동기는 12-6절의 gust MAE
1.8m/s "벽"을 완화하는 것이었는데, 그 벽은 이미 12-18절에서 **완전히 다른 원인
(yaw 다양성 부족)** 때문이었던 걸로 밝혀져 데이터만으로 해결됨(현재 베이스라인
speed MAE 0.361이 이미 그 개선된 상태). 즉 이 실험을 시작할 때부터 원래 동기였던
문제가 이미 없어진 상태라, "고정된 k로는 급변 상황을 못 따라간다"는 가설을 검증할
남은 여지 자체가 별로 없었을 가능성이 큼.

**결론**: 배포 모델(sigma=0 기준)을 바꿀 근거 없음 — 원본 그대로 유지(안전장치대로
스윕 중 한 번도 안 바뀌었음, git diff 없음 확인됨). RAMP-Net 손실함수 설계 후보
1순위는 이걸로 "효과 없음"이라는 결론이 남 — 남은 2개(콜로케이션 포인트, 손실
비율 스윕)도 같은 이유(원래 동기가 이미 해소됨)로 우선순위가 낮아졌다고 보는 게
합리적.

CSV/로그 없음(오프라인 학습 로그만) — `offline_training/sweep_20260820_020220/`
(sigma별 로그 4개 + 체크포인트 4개, 커밋됨). 배포 `wind_estimator.pt`는 미변경.

## 12-27. 바람 "전환" 구간 조사 — 원래 질문 미해결, 성능 저하 원인은 PX4 버전이 아님으로 판명 (2026-08-20)

**배경**: 지금까지 모든 학습/평가 데이터는 한 트라이얼 내내 성격이 일정했음(계속
고정 아니면 계속 gust), `WIND_SETTLE_S`로 바람 온셋 직후 구간도 로깅에서 제외해옴 —
"일정↔변동 전환" 자체는 한 번도 데이터에 없었음. 실제 비행에선 오히려 이 전환이
흔함(잔잔하다가 돌풍, 돌풍 지나가고 다시 잔잔). 이 갭을 조사하려 함.

**방법**: `wind_transition_sweep.py` 신설 — 에피소드 하나 = 5개 구간(각 8초)을
이어붙여 고정↔gust가 4번 전환되도록 함(단일 yaw, 이 조사의 핵심이 시간축이지
yaw가 아니라서 스코프를 좁힘). `analyze_transition_error.py`로 전환 후 경과시간별
오차를 집계. 40에피소드 수집(예상 28분 → 실제 약 2시간 걸림, 원인 미상 — 아래
참고).

**1차 결과 (이상하게 나쁨)**: "전환 후 4초 이상 지난 정상상태" 구간조차
speed MAE가 fixed 1.07m/s, gust 2.32m/s로, 배포 모델의 공인 성능(0.36~0.43m/s)보다
**2.5~6배 나쁨**. 전환 직후(0~0.5초)와 정상상태(4초+) 차이는 -0.33m/s로 오히려
직후가 살짝 낫기까지 함 — "전환 직후에 잠깐 나빠졌다가 회복"이라는 원래 가설과
안 맞는 패턴.

**디버깅 (원인 규명)**: 이 나쁜 결과가 진짜 "전환 효과"인지, 뭔가 잘못된 건지
단계적으로 확인함:
1. 분석 스크립트 버그 의심 → 이미 검증된 `evaluate_checkpoint.py`로 같은 CSV
   재평가 → **동일하게 나쁨**(speed MAE 1.668m/s). 분석 스크립트 버그 아님.
2. 데이터 수집 자체가 잘못됐는지(바람이 실제로 안 걸렸는지) 의심 → steady-state
   구간에서 commanded 풍속 크기 vs 실측 roll/pitch 크기 상관계수 확인 →
   **0.95로 강한 상관** — 물리적으로 정상. 바람은 제대로 걸리고 있음.
3. 체크포인트 자체가 손상됐는지 의심(12-26절 스윕 중 실수로 안 바뀐 값이 남았는지)
   → 메타데이터 확인(`best_epoch=187`, `best_val_mae=0.431`) + `git diff` 없음
   → 체크포인트 정상.
4. **결정적 대조 실험**: "전환"이 진짜 원인이라면, 전환이 아예 없는 순수
   고정바람 데이터는 정상이어야 함. SITL을 완전히 새로 띄우고(아래 참고),
   이미 검증된 `wind_random_sweep.py`로 소규모(8조건) 순수 고정바람만 수집해서
   같은 방식으로 평가 → **speed MAE 1.367m/s로 똑같이 나쁨**. 전환이 전혀 없는
   데이터인데도 나쁘다 → **"전환" 가설은 기각됨**.

**진짜 원인으로 의심되는 것**: 4번 대조 실험 직전에 알아챈 사실 — 이 SITL이
아침 08:33부터 **3시간 가까이 끊김없이 연속 실행 중**이었음(12-8절의 "SITL을
오래 연속 실행하면 이상해질 수 있다"는 그동안 미확인이었던 가설과 정확히 같은
정황). 대조 실험은 SITL을 완전히 재시작한 새 인스턴스에서 했는데도 여전히
나빴으므로, **"이번 세션 안에서 SITL이 오래 돌아서"가 유일한 원인은 아님**.

더 유력한 후보: 이 세션의 `PX4-Autopilot`은 **오늘 GitHub에서 완전히 새로
클론한 것**(12-22/12-24절 참고 — 원본은 `rm -rf`로 지워졌었음). 배포된
`wind_estimator.pt`는 08-16~08-19에 수집된, **그때 그 PX4 빌드**의 데이터로
학습됨. 그 사이 PX4 upstream이 `main` 브랜치에서 계속 업데이트되므로, 오늘 새로
받은 클론은 물리 모델(x500 SDF의 질량/관성/모터상수) 등이 미묘하게 달라졌을
가능성이 있음 — 그러면 "같은 풍속이라도 실제 기울기 반응이 달라짐"이 생기고,
옛 데이터로 학습한 모델이 새 물리 반응을 잘못 해석하는 게 설명됨. **정확히
확인은 못 했음(PX4 두 버전을 나란히 비교하는 건 이번 조사 스코프 밖) — 가설로
남김.**

**중요한 재해석**: 오늘/어제 세션에서 한 "상대 비교"(고정바람 A/B의 3s/2s vs
5s/4s settle-time, 회전 A/B의 패치 복구 전/후, gust A/B의 새 settle-time 재검증
등)는 전부 **같은 세션의 같은 PX4 빌드 안에서 조건끼리 서로 비교**한 것이라
이 문제와 무관하게 유효함. 문제가 되는 건 **오늘 새로 수집한 데이터를 배포된
체크포인트의 "절대적인" 학습시 성능(0.36~0.43m/s)과 직접 비교하는 경우**뿐임 —
이번 12-27절의 원래 질문(전환 구간 특성)이 정확히 그런 절대비교였어서 걸림.

**추가 확인**: gust 대조군(같은 소규모, 새 SITL, `wind_gust_sweep.py` 표준 스크립트)도
평가해보니 speed MAE 1.581m/s — 고정바람 대조군(1.367m/s)과 비슷하게 나쁨. 세 번째
독립 확인(전환 데이터·고정 대조군·gust 대조군 전부 비슷하게 나쁨)으로, "전환도
아니고 fixed/gust 어느 한쪽 특유의 문제도 아니고, 세션 전반에 걸친 문제"라는
그림이 더 뚜렷해짐.

**PX4 버전 드리프트 가설 — 직접 검증해서 기각함**: 랩실 PC에서 확인한 실제 학습
시점 커밋(`e23504031c`, 회전 패치의 부모 커밋)으로 이 세션의 `PX4-Autopilot`을
`git checkout` + `git submodule update` + 패치 재적용 + 재빌드한 뒤, 완전히 같은
방식으로 소규모 고정바람 대조군을 다시 수집. **결과: speed MAE 1.557m/s — 오늘
최신 커밋(1.367m/s)과 사실상 동일하게 나쁨.** 구커밋으로 되돌려도 안 고쳐짐 →
**PX4 버전 드리프트는 원인이 아님, 가설 기각.**

이 결과로 "다른 환경이라 그렇다"는 설명도 같이 약해짐 —애초에 12-22/12-24절에서
이미 확인했듯 이 세션은 랩실 PC와 **같은 물리적 머신**이라, "하드웨어가 다르다"는
설명 자체가 성립하지 않았음. PX4 코드까지 동일하게 맞췄는데도 재현이 안 되므로,
남은 후보는 **PX4 코드 외의 무언가**(예: `parameters.bson` 관련 — 12-17절에서
"매 세션 재시작마다 예방적으로 parameters.bson 리셋"하는 로직이 추가됐는데,
이게 원본 데이터 수집 초반 세션에는 없었을 수 있음 → 오늘은 항상 순정 기본
파라미터로 부팅하는데, 원본 데이터는 그 사이 누적된 다른 캘리브레이션/튜닝
파라미터 위에서 수집됐을 가능성) 또는 아직 특정 못 한 다른 세션별 상태 차이.

**결론**:
- **PX4 버전 드리프트 가설은 기각됨** — 실제로 되돌려서 재검증했는데도 재현 안 됨.
- SITL 오래 연속 실행 가설(12-8절)도 이번 조사로는 확증/반증 둘 다 못 함(새
  SITL로 재시작해도, 구커밋으로 되돌려도 여전히 나빴음).
- 진짜 원인은 **12-28절에서 확정됨** — PX4나 환경 문제가 아니라 **데이터 생성
  방식(더더링 유무) 차이**였음. 원래 질문(전환 구간 특성)도 12-28절 결론에 따라
  재시도 방향이 정해짐.

## 12-28. 진짜 원인 확정: 모델이 더더링 특유의 자이로 신호에 의존하고 있었음 (2026-08-20)

**추가 검증**: PX4 버전을 기각한 뒤, "우연" 가설도 통계적으로 기각함 — 원본 전체
데이터(2848조건)에서 조건별 speed MAE의 실제 분포를 구하고, 거기서 8개를
무작위로 2000번 뽑아 평균의 분포를 시뮬레이션. 오늘 관측된 값(1.37~1.58)이 나올
확률은 **0.05%** — 순전히 운으로 어려운 조건만 뽑힌 게 아님이 확인됨. yaw=0도
근처가 특별히 약한 지점인지도 원본 데이터로 확인했으나 아님(전체 yaw 구간 중
0~20도는 MAE 0.368로 오히려 평균 근처).

**결정적 단서**: 오늘 대조군은 전부 `wind_random_sweep.py`(더더링 없음)로
모았는데, **배포된 모델은 전부 `wind_random_dither_*.csv`(더더링 있음)로
학습됨** — 데이터 생성 방식 자체가 달랐음. 자이로(wx/wy/wz) 표준편차를 직접
비교:

| | wx_rad_s std | 비고 |
|---|---|---|
| 원본 학습데이터(더더링) | 0.5545 rad/s | OU 프로세스(sigma=0.10, tau=0.3s)로 무작위 토크 주입 |
| 오늘 대조군(더더링 없음) | 0.0063 rad/s | 순수 비행, 약 **88배** 차이 |

모델 입력 9개 중 3개(자이로)가 학습 때와 완전히 다른 통계적 특성을 보고 있었던
것. `wind_random_dither_sweep.py`로 **같은 조건을 다시 수집**해서 평가하니:

**speed MAE = 0.297m/s** — 공인 성능(0.36~0.43)과 같거나 오히려 더 좋음. **확정.**

**원인 요약**: 12-17절에서 회전 추정의 "자기입력 미분 지름길"을 막으려고 도입한
더더링이, 데이터 수집 단계에서만 쓰이고 실제 정상 비행/보정 추론 시점에는 전혀
쓰이지 않음 — 근데 모델은 gust/rotation 결합 이후(12-16절~) FEATURES에 자이로
(wx/wy/wz)가 포함돼 있어서, **더더링이 만드는 자이로 잡음의 "크기와 패턴"
자체를 암묵적으로 학습 분포의 일부로 흡수해버림.** 더더링 없는 "깨끗한" 비행
데이터는 이 모델에게 통계적으로 낯선 입력이 되고, 그 결과 풍속 추정이 크게
틀어짐.

**중대한 파급 효과**: `correction_experiments/`의 실제 보정 스크립트들
(`pinn_wind_correction_sweep.py` 등 4개)을 확인해보니 **더더링 로직이 전혀
없음** — 실비행 중엔 항상 자연 상태(더더링 없는) 자이로를 모델에 먹임. 즉
**12-16절 이후(회전 결합 모델을 쓴 모든 시점, 12-18/19/20절 및 이번 세션의
12-22~12-25절 전부 포함)의 모든 correction A/B 실험은, 모델이 학습 때와
다른(더 나쁜) 조건에서 바람을 추정하며 진행된 것.** 그런데도 보정이 순이익으로
나온 건, deadband/EMA 스무딩이 잡음의 상당 부분을 걸러내고, 추정치의 "방향"은
그럭저럭 맞았기 때문으로 보임(물리적 tilt-풍속 상관계수 0.95 확인 참고) —
**즉 지금까지의 A/B "방향"(보정이 순이익이다) 자체는 뒤집힐 근거가 없지만,
"얼마나" 좋아지는지의 정확한 수치들은 전부 이상적이지 않은 조건에서 나온
보수적인(어쩌면 과소평가된) 값일 수 있음.**

**다음에 볼 것** (우선순위 순):
1. **실비행/보정 추론 시에도 더더링을 켜서 A/B 하나만 다시 재검증** — 이게
   맞다면 지금 보고된 개선율(예: crosswind +51~67%)보다 더 좋은 수치가 나올
   가능성. 제일 직접적이고 빠른 확인 방법
2. **더 나은 근본 해법**: 더더링 있는/없는 데이터를 섞어서 재학습 — 모델이
   "더더링 유무와 무관하게" 자이로를 올바르게 해석하도록 일반화시키는 것.
   실비행에 인위적 토크 노이즈를 계속 주입하는 건 실제 하드웨어 이관 시에는
   바람직하지 않은 선택이라, 장기적으로는 이쪽이 맞는 방향
3. 하드웨어 이관 전 반드시 확정: 지금처럼 "더더링 없이는 못 쓰는 모델"인 채로
   실비행에 올리면 안 됨
4. 원래 질문(전환 구간 특성)은 더더링 있는 데이터로 다시 수집해야 재시도 가능

**교훈**: PX4 버전이라는 "그럴듯하지만 틀린" 가설을 먼저 떠올려서 상당한 시간을
썼음 — 데이터 생성 스크립트 목록(`wind_random_dither_*` vs `wind_random_*`)을
꼼꼼히 봤다면 더 빨리 찾았을 문제. **이상한 결과가 나오면 "환경이 다른가"보다
먼저 "정확히 같은 데이터 생성 파이프라인을 쓰고 있는가"부터 확인할 것.**

CSV: `logs/wind_transition_20260820_083356_n40_seed777.csv`(전환 조사용, 결론엔
못 씀), `logs/wind_random_20260820_113006_..._seed9001.csv`/
`logs/wind_gust_20260820_113240_..._seed9002.csv`/
`logs/wind_random_20260820_122224_..._seed9101.csv`(더더링 없는 대조군 3개,
전부 나쁨 — PX4 버전과 무관하다는 증거),
`logs/wind_random_dither_20260820_123957_..._seed9202.csv`(**더더링 있는
대조군 — 정상 성능(0.297m/s)으로 원인 확정시킨 결정적 데이터**).

## 12-29. 실비행 중 더더링 켜기 시도 — 임시방편으로도 못 씀 (2026-08-20)

**배경**: 12-28절에서 확정된 원인("모델이 더더링 있는 자이로에 의존")에 대한
빠른 진단으로, `pinn_wind_correction_sweep.py`에 `wind_random_dither_sweep.py`와
동일한 OU 더더링(`--dither` 옵션 신설, 기본 꺼짐)을 추가해서, 실비행 중에도
학습 조건과 똑같이 맞춘 상태로 고정바람 5조건 A/B를 5회 반복.

**결과**:

| 조건 | 더더링 없음(12-23절) | 더더링 켬(이번) |
|---|---|---|
| calm | +7.4% | **-22.3%** |
| light | +12.4% | **-11.5%** |
| default | +41.9% | **-18.5%** |
| strong | +53.1% | +0.9%(노이즈 수준) |
| crosswind | +56.9% | **-19.4%** |

**5개 중 4개가 마이너스로 뒤집힘 — 예상과 정반대.** OFF 베이스라인 자체도
전부 크게 나빠짐(예: calm 0.047m → 0.699m).

**해석**: 더더링은 데이터 수집 시점엔 "무해한 잡음"이었지만, 그건 그때 목표가
"정확한 바람 라벨을 기록하는 것"이지 "위치를 잘 유지하는 것"이 아니었기
때문임. 이 스크립트에서는 목표가 정반대(위치오차 최소화)라서, **더더링이 만드는
지속적인 무작위 토크 자체가 그 내내 자세/위치를 흔드는 실제 물리적 교란으로
작용**함 — 바람 추정이 좋아지는 이득보다, 더더링 자체가 주는 손해가 훨씬 큼.

**결론**: **"실비행에 더더링을 계속 켜두는 건 해법이 아님"**이 명확히 확인됨 —
12-28절 말미에 이미 "임시방편"이라고 낮게 평가했던 선택지가 실측으로도
기각됨. 남은 길은 하나뿐: **더더링 있는/없는 데이터를 섞어서 재학습**해서,
모델이 실비행(더더링 없음)에서도 자이로를 올바르게 해석하도록 만드는 것.

CSV: `logs/pinn_correction_sweep_20260820_13*.csv`(5개, 더더링 켠 상태),
집계: `logs/aggregate_summary_pinn_correction_sweep_20260820_130134.csv`. 코드:
`pinn_wind_correction_sweep.py`에 `--dither`/`--dither-seed` 옵션 추가(기본
꺼짐, 기존 동작 불변).

## 12-30. 더더링+비더더링 혼합 재학습 완료 — 추정은 크게 개선, 보정 효과는 오히려 줄어듦 (2026-08-20)

**1단계 — 비더더링 데이터 수집**: `run_yaw_collection_sessions.sh`로 4세션
(yaw 12방향×방향당 10조건×4세션=480조건, yaw 0/7.5/15/22.5도 오프셋으로 30지점/
7.5도 간격) 수집. **도중 큰 장애 발생**: 첫 시도에서 세션 1~3이 전부 3회씩(총
9번) GPS 락 실패로 완전히 실패함 — 오늘 PX4 버전 가설 검증 과정에서 커밋을
여러 번 왔다갔다 체크아웃한 여파로 `build/px4_sitl_default`가 손상된 것으로
추정(12-16절에서 이미 진단됐던 것과 같은 클래스의 문제). `make distclean` +
완전 재빌드로 해결(스모크테스트로 확인 후 재수집 시작) — 재수집은 4세션 전부
1회 시도로 성공(총 소요 약 1시간49분).

**2단계 — 혼합 재학습**: 기존 더더링 데이터(17개 파일, ~2780조건) + 새 비더더링
데이터(4개 파일, 480조건) = 23개 파일, 928,400행, 3336조건으로 재학습.

| 지표 | 기존(더더링만) | 혼합 재학습 |
|---|---|---|
| speed MAE (5-fold) | 0.361±0.023 | **0.359±0.009** (품질 유지, 오히려 더 안정적) |

혼합해도 성능 저하 없음 확인. 배포용 단일분할 모델도 정상 학습(best_epoch=110,
val_MAE=0.452m/s).

**비더더링 대조군 재평가 — 극적으로 개선됨**:

| 대조군 | 기존 모델 | 혼합 재학습 모델 |
|---|---|---|
| 고정바람(seed9001) | 1.367m/s | **0.181m/s** (7.5배 개선) |
| 고정바람(구커밋, seed9101) | 1.557m/s | **0.426m/s** (3.7배 개선) |
| gust(seed9002) | 1.581m/s | **0.909m/s** (1.7배 개선, 완전하진 않음 — 비더더링 데이터에
  gust 조건을 안 넣었던 게 원인으로 보임) |

**3단계 — 실비행 A/B 재검증(더더링 없음, 실제 운영 조건)**: 고정바람 5조건을
새 모델로 5회 반복. **개선율이 오히려 작아짐**:

| 조건 | 12-23절(기존 모델) | 혼합 재학습 모델 |
|---|---|---|
| calm | +7.4% | -8.8%(노이즈 수준, 절대값 3~4cm) |
| light | +12.4% | +3.8% |
| default | +41.9% | **-2.2%(사실상 중립)** |
| strong | +53.1% | +5.2%(표준편차 2.2%p로 작음 — 약하지만 확실) |
| crosswind | +56.9% | **+17.2%**(표준편차 2.7%p — 5개 중 가장 확실히 유지됨) |

**해석 — 12-28절 말미의 추측("실제 잠재력은 더 좋을 수도")은 틀렸음**: 추정
정확도(MAE)는 극적으로 좋아졌는데, 실제 보정 효과(위치오차 감소)는 오히려
줄어듦. 그럴듯한 이유: 기존(더더링 편향) 모델의 추정치가 절대적으로는
부정확해도, 우연히 이 accel 피드포워드 구조·게인(`ACCEL_GAIN=0.15`)에서 더 큰
보정 신호를 만들어내는 방향으로 편향돼 있었을 가능성. 반대로 새 모델은 더
"차분한"(노이즈에 덜 반응하는) 추정치를 내서, 정확하지만 deadband/게인 튜닝이
새 추정치 스케일에 맞지 않게 됐을 수 있음. 즉 **추정 정확도와 "이 특정 게인으로
만드는 보정 효과 크기"는 별개 문제** — 지금 게인(0.15)은 기존(편향된) 모델
기준으로 튜닝된 값이라, 새 모델에는 재튜닝이 필요할 가능성이 있음.

**결론**:
- **모델 자체의 문제(12-28절)는 확실히 고쳐짐** — 실비행(더더링 없음)에서도
  이제 정상적으로 바람을 추정함. 이건 명백한 개선이고 되돌릴 이유 없음.
- 다만 그 결과로 **`ACCEL_GAIN`/`WIND_DEADBAND_MPS` 재튜닝이 사실상 다시
  필요해짐** — 12-8/12-14절에서 "원래 값이 최선"이라고 결론 내렸던 게 전부
  구모델(더더링 편향) 기준이었기 때문. crosswind(+17.2%)와 strong(+5.2%)은
  그래도 확실히 순이익이라 "보정이 아예 무의미해졌다"는 아님.
- 새 모델을 배포함(`wind_estimator.pt` 갱신) — 구모델은 별도 백업 파일 없이
  git 이력(이전 커밋)으로 충분히 복구 가능해서 그대로 둠.

**다음에 볼 것**:
1. `ACCEL_GAIN`/`WIND_DEADBAND_MPS`를 새 모델 기준으로 재스윕(12-8절과 같은
   방법론) — default/calm이 다시 순이익으로 돌아올지 확인
2. 비더더링 gust 데이터도 추가 수집해서 gust 추정 정확도(현재 0.909m/s) 마저
   개선
3. 재튜닝 끝나면 12-27절 원래 질문(전환 구간 특성) 재시도 가능

CSV: `logs/wind_random_20260820_18*.csv`/`_19*.csv`/`_20*.csv`(4개, 새 비더더링
데이터), `logs/pinn_correction_sweep_20260820_21*.csv`(5개, 재검증), 집계:
`logs/aggregate_summary_pinn_correction_sweep_20260820_210908.csv`. 모델:
`offline_training/wind_estimator.pt`(신규 배포, 구모델은 이전 커밋에 보존됨).

---

## 12-31. `ACCEL_GAIN`/`WIND_DEADBAND_MPS` 재스윕 — 스윕 스크립트 자체 버그 발견 후
재측정, gain 0.15→0.05로 하향 (2026-08-20)

**배경**: 12-30절에서 새(혼합 재학습) 모델 기준으로는 기존 `ACCEL_GAIN=0.15`가
최선이 아닐 수 있다는 게 확인돼, `pinn_correction_param_tuning.py`로
`ACCEL_GAIN`(0.05~0.30)·`WIND_DEADBAND_MPS`(0.5~2.0) 재스윕 시작.

**1차 시도 — 심각하게 불안정한 결과, 원인은 모델이 아니라 스윕 스크립트 버그**:
`default_fixed`(gain=0.05)에서 개선율 -363%, roll 11.3°까지 튀는 등 물리적으로
말이 안 되는 결과가 나옴(gain이 낮을수록 오히려 더 불안정한 비단조 패턴도 확인).
CSV 원시 로그를 시간축으로 까본 결과, **정확히 1초 경계마다(GUST_UPDATE_INTERVAL_S)
roll/pitch가 급변하고 그 다음 틱과 완전히 동일한 값이 찍히는 패턴**을 발견 —
텔레메트리가 그 순간 멈췄다 튀는 신호.

원인: `pinn_wind_correction_sweep.py`는 트라이얼 시작 전 바람을 1회만 설정하고
끝까지 안 건드리는데, `pinn_correction_param_tuning.py`는 "_fixed"(고정) 조건
에서도 `wind_at()`을 매초 호출해 `set_wind()`를 반복 실행하고 있었음.
`set_wind()`는 `gz topic pub` 1회성 subprocess를 3번 재전송하는 호출(12-23/12-25절)
인데, 이걸 값이 바뀌지도 않는 "고정" 바람에 매초 재발행하면서 재발행 자체가
매초 실제 물리적 충격(roll/pitch 급변)을 만들어내고 있었음. 즉 "_fixed" 조건이
사실은 **스스로 만든 1Hz 미니 돌풍이 반복 주입되는 조건**이었던 것 — `ACCEL_GAIN`/
`deadband` 문제가 아니라 스윕 스크립트 자체의 버그.

**수정**: `run_trial()` 내부에서 마지막으로 보낸 바람 값과 다를 때만 `set_wind()`를
호출하도록 변경(고정 조건은 트라이얼당 1회만 발행, gust 조건은 기존처럼 값이
실제로 바뀔 때마다 발행 - 동작 그대로 유지).

**2차 시도 — WSL 재시작으로 중단**: 수정 후 재실행 중 WSL 자체가 재시작되며 SITL과
스크립트가 전부 죽음. 죽기 직전까지의 CSV(끊기기 전 `strong_fixed` gain=0.05
직전까지 완료)를 직접 파싱해본 결과 roll/pitch가 1~6°대로 정상 범위로 돌아온 것만
확인하고, 최종 요약 전에 끊겨서 SITL·스크립트 재시작 후 처음부터 재실행.

**3차 시도 — 최종 결과 (깨끗하게 완주)**:

| 조건(deadband 스윕, gain=0.15 고정) | OFF peak | deadband=0.5 | 1.0 | 1.5 | 2.0 |
|---|---|---|---|---|---|
| light_fixed | 0.106m | -18.0% | -61.6% | -51.0% | -33.9% |
| light_gust | 0.146m | -15.0% | **+26.1%** | +2.1% | +18.5% |

| 조건(gain 스윕, deadband=1.0 고정) | OFF peak | gain=0.05 | 0.10 | 0.15 | 0.20 | 0.30 |
|---|---|---|---|---|---|---|
| default_fixed | 0.267m | **+6.6%** | -7.6% | -24.8% | -132.8%(roll 9.0°) | -97.7%(roll 9.9°) |
| strong_fixed | 0.727m | **+38.2%**(roll 8.8°) | +0.2% | +14.3%(roll 12.5°) | +26.1%(roll 12.9°) | -17.7%(roll 15.5°) |

**해석**:
- 버그가 고쳐지자 gain에 대해 default_fixed는 뚜렷하게 단조적으로 나빠짐(0.05가
  최선, 0.20/0.30에서 확실히 발산). strong_fixed는 gain=0.05가 가장 좋고 이후
  들쭉날쭉하다 0.30에서 다시 나빠짐 — 두 조건 다 **0.05가 공통으로 안전하고
  이득이 있는 지점**.
- strong_fixed의 roll이 gain이 낮아도 8~9°나 되는 건 이상 현상이 아니라
  8.5m/s 바람 자체를 버티려면 원래 그 정도 기울여야 하는 물리적으로 정상적인
  값(OFF 상태에서도 비슷한 크기일 것으로 추정 - 이번 스윕은 OFF trial의 roll을
  따로 기록하지 않아 직접 비교는 못 함).
- deadband는 light_gust에서 1.0이 여전히 최선으로 재확인됨 — 기존값 그대로 유지.
- light_fixed는 어떤 deadband를 써도 전부 손해(-18%~-62%) — 12-7절부터 알려진
  한계(약풍은 보정이 잡음만 더함) 그대로, deadband로 못 걸러짐.

**결론**:
- `ACCEL_GAIN`을 **0.15 → 0.05**로 낮춤 (`pinn_wind_correction_sweep.py`,
  `pinn_wind_correction_gust_sweep.py`, `pinn_correction_param_tuning.py`의
  `ACCEL_GAIN_DEFAULT` 전부 반영).
- `WIND_DEADBAND_MPS=1.0`은 그대로 유지.
- 적응형 게인(`ACCEL_GAIN_MIN=0.10`/`ACCEL_GAIN_MAX=0.25`, `--adaptive`)은 이번
  스윕 대상이 아니었음 - 이번 결과대로면 `ACCEL_GAIN_MIN`도 재조정이 필요할 수
  있으나 미검증 상태로 남김.
- 부수적으로, 시간대별 물리 이벤트 주입이 있는 스윕 스크립트를 새로 짤 때는
  "고정" 조건이라도 반복 재발행이 그 자체로 외란이 될 수 있다는 걸 유의할 것
  (`wind_transition_sweep.py`도 같은 패턴으로 짜여 있어 재점검 필요할 수 있음).

**다음에 볼 것**:
1. `ACCEL_GAIN_MIN`/`MAX`(적응형 게인)도 새 gain=0.05 기준으로 재검증할지 결정
2. ~~`wind_transition_sweep.py`의 fixed 구간도 매초 재발행 버그가 있는지 확인~~
   → **확인 완료, 실제로 있었음.** 291번 줄이 fixed 구간에서도 `set_wind()`를
   매초 무조건 재호출하고 있어서 값이 바뀔 때만 재발행하도록 동일하게 수정함.
   영향 범위 확인 결과: **12-28절의 핵심 결론(더더링 의존 자이로 신호)은 무관** —
   그 결론을 확정지은 결정적 대조실험(12-27절 4번, PX4버전 가설 검증)은 전부
   `wind_random_sweep.py`/`wind_gust_sweep.py`(둘 다 조건당 1회만 발행하는 안전한
   패턴)로 수집했고 거기서도 동일하게 나쁜 MAE가 재현됐었기 때문. 다만 **12-27절
   원래 질문(전환 후 경과시간별 오차) 자체는 `wind_transition_sweep.py` 원본
   데이터로만 계산됐던 거라 이 버그의 영향을 받았을 가능성이 있음** — 그 질문은
   원래도 "미해결"로 남겨뒀던 것이라 결론이 뒤집히는 건 아니지만, 재시도할 땐
   반드시 수정된 버전으로 다시 수집해야 함.
3. 재튜닝 끝났으니 12-27절 원래 질문(전환 구간 특성)을 수정된
   `wind_transition_sweep.py`로 재시도 가능(40에피소드 재수집, 이전엔 약 2시간
   소요됨)

CSV: `logs/pinn_correction_param_tuning_20260820_214657.csv`(1차, 버그가 드러난
원본 - 위 인용 수치의 출처), `logs/pinn_correction_param_tuning_20260820_224443.csv`
(3차, 최종 완주분). (2차, WSL 재시작으로 중단된 참고용 사본은 정리 차원에서 삭제함.)

---

## 12-32. 바람 전환 구간 재조사 — 원래 질문 답 나옴: fixed 방향은 회복, gust 방향은
사인파 위상과 뒤섞여 있었음 (2026-08-21)

**배경**: 12-31절에서 `wind_transition_sweep.py`의 반복 재발행 버그를 고친 뒤,
12-27절에서 미해결로 남았던 원래 질문("전환 후 경과시간별 추정 오차")을 수정된
스크립트로 재수집해 재시도.

**수집**: 첫 시도(1번째 에피소드 진행 중) 도중 **WSL이 알 수 없는 이유로 강제
재시작됨**(`uptime` 0분, journal "uncleanly shut down" 확인 - 같은 세션에서
12-31절 재검증 도중에도 한 번 더 발생, 원인 미특정). 노트북을 완전히 껐다 켠 뒤
재시도해서 크래시 없이 정상 완주(40/40, 송신 간격 임계치 초과 6/74023로 정상).

**결과 (배포 모델=혼합 재학습 모델, best_val_mae=0.452)**:

| 정상상태(전환 후 4초+) | speed MAE | vec MAE |
|---|---|---|
| fixed 구간 | **0.557 m/s** | 1.355 m/s |
| gust 구간 | 1.814 m/s | 3.085 m/s |

(버그 있던 1차 결과였던 12-27절 수치: fixed 1.07m/s, gust 2.32m/s — fixed는
2배 가까이 좋아짐. gust는 원래도 "fixed 구간에만 있던" 반복재발행 버그의 영향을
안 받는 구간이라 개선폭이 작은 게 당연함.)

**전환 방향별 경과시간 패턴** (0.5초 단위, `analyze_transition_error.py`):

- **gust → fixed 전환**: 전환 직후(0~0.5s) MAE **1.317m/s**로 튀었다가
  0.5~1s에 0.808, 1~1.5s에 0.625로 급격히 떨어지고 이후 8초 내내 0.54~0.6m/s
  범위에서 안정됨. **깨끗한 "튀었다가 회복" 패턴** — WINDOW=20샘플(1초 버퍼)이
  이전 gust 구간의 이력을 밀어내는 데 걸리는 시간(약 1.5~2초)과 맞아떨어짐.
- **fixed → gust 전환**: 뚜렷한 회복 패턴이 안 보이고, 0.93→1.94→2.04→1.29→
  0.68→1.17→1.68→1.79로 8초 내내 오르내림. **원인**: `wind_transition_sweep.py`의
  `wind_at()` 구현상 gust 구간은 항상 위상 0에서 시작하는 사인파(`period=6s`)라,
  `t_since_transition_s`가 사실상 그 사인파 자체의 위상 시계와 같음 — 즉 이
  변동은 "전환 이후 회복"이 아니라 **그냥 사인파 진폭이 바뀌는 구간마다 추정
  난이도가 원래 다른 것**이 `t_since_transition_s`에 우연히 겹쳐 보인 것.
- 참고로 pooled(둘 합산) "전환 직후 vs 정상상태" 차이는 -0.013m/s로 거의 0 —
  12-27절 1차 결과의 "-0.33m/s(직후가 오히려 나음)"이라는 이상 패턴은 사라짐.
  그 이상 패턴은 fixed 구간에 낀 반복재발행 버그가 정상상태 구간 전체를 고르게
  나쁘게 만들어 "직후 vs 정상상태" 차이를 지워버린 결과였던 것으로 보임.

**해석 및 결론**:
- **원래 질문에 답이 나옴**: 모델은 "일정 → 변동"보다 "변동 → 일정"으로 바뀔 때
  더 뚜렷하게 시간 의존적으로 반응한다 — gust 이후 fixed로 바뀌면 ~1.5~2초간
  일시적으로 나빠졌다가 회복됨. 이건 WINDOW 크기(20샘플=1초)로 설명되는 정상적인
  동작이고, 딱히 고쳐야 할 결함은 아님(실제 운용에서는 EMA 스무딩까지 얹혀 있어
  체감 영향은 더 작을 것).
- fixed→gust 방향은 "전환 특성" 질문 자체가 이 스크립트의 위상-고정 설계와
  뒤섞여 있어 깨끗하게 답할 수 없음 — gust 구간마다 위상을 무작위화하지 않는 한
  더 분리하기 어려움. 우선순위가 낮아 추가 조사는 보류.
- 이 조사는 처음부터 "학습 파이프라인에 편입 목적 아님(연구용)"으로 설계됐던
  것이라(스크립트 docstring 참고), 여기서 배포 모델이나 파라미터를 바꿀 필요는
  없음 — 순수하게 이해를 위한 결과.

**다음에 볼 것**: 낮은 우선순위 - `wind_at()`에 gust 위상 무작위화 옵션을 추가하면
fixed→gust 방향도 깨끗하게 분리 가능하지만, 실익이 크지 않아 보류.

CSV: `logs/wind_transition_20260820_235946_n40_seed777.csv`(재수집 최종본,
40/40 완주). (크래시로 에피소드 0 도중 끊긴 1차 시도 사본은 정리 차원에서 삭제함.)

---

## 12-33. correction_experiments 코드 중복 정리 (2026-08-21)

**배경**: 12-21절(코드 셀프 리뷰) 2번 항목으로 지적됐던 것 - `set_wind()`,
`EmaSmoother`, `apply_deadband()`, `WindCorrector`/`RotationCorrector` 클래스가
`pinn_correction_param_tuning.py`/`pinn_wind_correction_sweep.py`/
`pinn_wind_correction_gust_sweep.py`/`pinn_rotation_correction_test.py` 4개
파일에 토씨 하나 안 틀리고 그대로 복붙돼 있었음. **직접적인 계기**: 12-31절에서
`set_wind()`의 반복 재발행 버그를 발견했을 때, 이게 복붙 코드였던 탓에
`pinn_correction_param_tuning.py`만 고치고 `wind_transition_sweep.py`(다른
디렉토리의 별도 스크립트, 이건 애초에 공유 대상 아님)에 있는 같은 버그를 뒤늦게
따로 찾아야 했음 - 만약 이 4개가 애초에 공유 모듈이었다면 한 번만 고치면 됐을 일.

**방법**: `sim_scripts/correction_experiments/correction_common.py` 신설, 4개
파일에서 diff로 바이트 단위까지 동일함을 먼저 확인한 뒤 옮김:
- `set_wind()`: 4개 파일 전부 완전히 동일 (diff 없음) - `world_name`을 인자로
  받도록만 바꾸고, 각 파일에서는 `functools.partial(set_wind, WORLD_NAME)`로
  기존 호출부(`set_wind(vx, vy)`) 시그니처를 그대로 유지.
- `WindCorrector`/`RotationCorrector`: `__init__`/`push_state`/`ready`는
  3개 파일에서 완전히 동일 확인, 마지막 추론 메서드만 출력 인덱싱이 다름(풍속
  2채널 vs 회전토크 3채널). `PINNCorrector`로 통합하고 원시 출력(`predict_raw()`)만
  제공 - 인덱싱은 호출부에 남겨둠(같은 체크포인트의 다른 출력 채널을 쓰는 두
  용도를 억지로 한 메서드로 합치지 않기 위함).
- `EmaSmoother`/`apply_deadband()`: 2~3개 파일에서 완전히 동일(사소한 docstring
  유무 차이만 있었음) - 그대로 이동.
- 텔레메트리 모니터/오프보드 송신기/이착륙 시퀀스 등 나머지 중복(파일당
  ~80~100줄)은 **일부러 손 안 댐** - 각 스크립트의 클로저 변수(`current_cmd`,
  `nominal` 등)와 얽혀 있어 리팩터링 리스크가 더 크고, 마침 SITL이 gust 데이터
  수집으로 몇 시간 점유돼 있어 실비행으로 검증할 방법이 없었음. 순수 유틸리티성
  코드(부작용 없고 입출력이 명확한 것)만 안전하게 옮김.

**검증**: SITL 없이 할 수 있는 만큼 - `py_compile`로 4개 파일 전부 문법 확인,
`import`로 모듈 로드 확인(임포트 시점에 실행되는 코드가 없어 이걸로 순환
임포트·이름 오류는 잡힘), AST로 미사용 import 확인(원래도 안 쓰이던
`WINDOW` import 3개 파일에서 발견 - 겸사겸사 정리). **실제 SITL 비행으로 동작
동일성까지 확인은 못 했음** - gust 수집이 끝나는 대로 스팟체크 필요.

**결과**: 4개 파일 합계 약 244줄 감소(278줄 삭제, 34줄 추가), 신규
`correction_common.py` 88줄. `set_wind`/`EmaSmoother`/`apply_deadband`/코렉터
클래스가 이제 한 곳에만 존재.

**다음에 볼 것**: gust 데이터 수집(12-34절 예정) 끝나고 SITL이 비면
`pinn_wind_correction_sweep.py` 짧게 한 번 돌려서 리팩터링 전후 동작이 같은지
스팟체크. 텔레메트리 모니터/오프보드 송신 보일러플레이트 정리는 여전히 남은 일.

**추가(2026-08-21, 12-34절 도중)**: SITL이 빈 김에 스팟체크 완료 —
`pinn_wind_correction_sweep.py` 5조건 전체 A/B를 크래시 없이 정상 실행, 3/5 개선
(calm +17.4%, default +4.2%, crosswind +6.1%, light -19.8%, strong -3.9%) —
절대 위치오차(0.03~0.48m) 스케일도 기존과 동일 범위. `correction_common.py`
리팩터링이 동작을 안 깨뜨렸음을 확인.

---

## 12-34. 비더더링 gust 데이터 수집(192조건) 성공, 재학습은 판단 보류 —
held-out 대조군이 결론 내리기엔 너무 작다는 걸 발견 (2026-08-21, 밤새 자율 진행)

**배경**: 12-30절에서 gust MAE가 1.7배만 개선(0.909m/s)된 채 남아있었음 — 원인은
비더더링 데이터 수집에 gust 조건을 안 넣었기 때문으로 추정됐었음. 사용자가 잠든
사이(약 01:00~08:30, 8시간) 자율적으로 진행하도록 승인받아, 고정바람 480조건
수집(12-30절)과 같은 방식으로 비더더링 gust 데이터를 추가 수집·재학습 시도.

**1단계 — 수집 인프라 버그 및 WSL 크래시 극복**: `run_yaw_gust_collection_sessions.sh`
신설(`run_yaw_collection_sessions.sh`의 gust판). 실행 도중 두 가지 장애를 극복:
1. **WSL이 두 차례 원인불명으로 강제 재시작됨** — 매번 SITL·수집 스크립트를
   재시작해서 이어감(사용자가 한 번은 직접 노트북을 재부팅해줌).
2. **세션 타임아웃이 실제 페이스보다 훨씬 짧게 계산되던 버그 발견** —
   `SESSION_TIMEOUT_S`가 조건당 22초를 가정해 계산됐는데, 실제로는(시스템 부하
   등으로) 조건당 ~90초가 걸려서 매 세션이 완료 전 약 1/4 지점에서 강제종료→
   처음부터 재시도를 반복하며 데이터를 계속 날리고 있었음(세션 크기를 줄여도
   타임아웃도 같이 줄어서 비율이 안 바뀌는 구조적 문제). `--per-episode-overhead-s`/
   `--session-timeout-s` 오버라이드 옵션을 추가해 실측 페이스 기준으로 재계산되게
   고치고, 규모도 8시간 예산에 맞게 480→192조건(4세션×48조건, yaw 12방향×방향당
   4개)으로 축소 — 12-18절에서 60에피소드만으로도 5배 개선을 봤던 전례가 있어
   이 정도로도 충분할 것으로 판단. 수정 후 4세션 전부 첫 시도에 성공(0 실패).

**2단계 — 재학습 시도 1 (새 gust 데이터 포함, 26개 파일)**: 기존 22개 파일(더더링
16+1, 비더더링 고정 4, 더더링 gust 1) + 새 비더더링 gust 4개 파일(192조건) =
26개 파일, 1,002,000행, 3,520조건으로 재학습.

- 5-fold pooled speed MAE: **0.434±0.027** (기존 23파일 기준 0.359±0.009보다
  나빠 보임 — 다만 이건 gust 조건 비중이 늘어난 데서 오는 자연스러운 결과일 수
  있음: gust는 원래 fixed보다 오차가 훨씬 크므로, 같은 모델이라도 pooled 평균에
  gust가 더 많이 섞이면 평균이 올라감. 회귀의 증거로 보기엔 이르다고 판단.)
- 배포용 단일분할: best_epoch=29, val_MAE=0.540m/s
- **held-out 대조군 평가(12-30절과 동일 파일)**: 고정(seed9001) 0.181→**0.279**,
  구커밋고정(seed9101) 0.426→**0.603**, gust(seed9002) 0.909→**0.986** — **셋
  다 나빠짐**, 개선하려던 gust까지 악화.

**3단계 — 진단: 새 데이터 없이도 재현이 안 됨**: 결과가 너무 이상해서, 새 gust
데이터를 빼고 기존 22개 파일만으로 다시 학습해봄(원본 23파일 구성을 최대한
재현). 배포용 단일분할 결과: best_epoch=107, val_MAE=0.433m/s — **원본
(best_epoch=110, val_MAE=0.452)과 거의 일치**, 파일 목록 재구성이 맞았다는 걸
확인. 그런데 held-out 대조군은: 고정(seed9001) 0.181→**0.189**(거의 동일, 정상)
이지만 gust(seed9002) 0.909→**1.169**(새 데이터를 아예 안 넣었는데도 더 나빠짐!).

**핵심 발견 — gust held-out 대조군(`seed9002`, 8조건)이 결론을 낼 만큼 안정적이지
않음**: `train_wind_estimator.py`의 train/val 분할은 `np.random.RandomState(0)`으로
고정 시드라 같은 데이터로는 항상 같은 결과가 나와야 하는데, 파일 구성을 조금만
바꿔도(이번 경우 gust 조건을 아예 안 넣었는데도) gust 대조군 MAE가 0.909→1.169로
28%나 튐. 원인은 이 대조군 CSV 자체가 **딱 8개 조건**뿐인 매우 작은 표본이라,
어떤 특정 8개 조건이 걸리느냐에 따라 MAE 추정치 자체의 표본오차가 크다는 것 —
모델이 실제로 좋아졌는지 나빠졌는지와 무관하게 이 정도 흔들림이 "정상"일 수
있음. **즉 지금까지 12-30절 등에서 "gust 0.909m/s"라는 단일 숫자로 모델 품질을
판단해온 방식 자체가, 그 숫자의 신뢰구간을 모른 채 비교해온 것이었다는 걸
이번에 알게 됨.**

**결론 및 조치**:
- **재학습된 모델(26파일)을 배포하지 않고 원래 커밋된 모델(734c4d6, 12-30절)로
  되돌림** — 두 재학습 시도(새 데이터 포함/미포함) 다 이 작은 대조군 기준으로는
  원본보다 나빠 보였는데, 그 차이가 진짜 모델 성능 차이인지 대조군 표본오차인지
  이번 조사로는 구분이 안 됐음. 애매한 상태로 프로덕션 모델을 바꾸지 않는 게 맞다고
  판단.
- **수집한 새 비더더링 gust 데이터(192조건, 4개 CSV)는 그대로 저장소에 보관** —
  나중에 더 견고한 방법(대조군을 더 큰 표본으로 새로 수집하거나, 5-fold를
  fixed/gust로 나눠 집계하거나, 여러 시드로 반복 학습해 평균 내는 등)으로 재시도할
  때 바로 쓸 수 있음.
- correction_common.py 리팩터링(12-33절) 스팟체크도 이 세션 SITL로 완료 — 정상
  동작 확인.

**다음에 볼 것**:
1. gust 대조군을 8조건보다 훨씬 크게(예: 40~80조건) 새로 수집해서, 앞으로의
   재학습 시도를 신뢰성 있게 비교할 수 있는 기준부터 마련
2. 그 다음에 이번에 모은 192조건 gust 데이터로 재시도(가능하면 여러 시드로
   반복해 평균·표준편차까지 같이 보고)
3. `ACCEL_GAIN_MIN`/`MAX`(적응형 게인, 12-31절에서 미검증으로 남김)도 나중에 볼 것

CSV: `logs/wind_gust_20260821_024909_..._seed9042.csv`,
`logs/wind_gust_20260821_040138_..._seed10042.csv`,
`logs/wind_gust_20260821_051500_..._seed11042.csv`,
`logs/wind_gust_20260821_062833_..._seed12042.csv` (4개, 새 비더더링 gust
192조건). 재학습 시도 산출물은 배포 안 함(로컬 `/tmp`에만 임시 보관, 저장소에는
없음).

---

## 12-35. 큰 gust 대조군 확보 + 240조건 추가 수집 — 재학습은 중단, 다음 세션에 이어서

**배경**: 12-34절에서 gust 대조군이 8조건뿐이라 재학습 판단이 안 됐던 문제를
풀기 위해, 사용자가 외출한 사이 이어서 진행: (1) 32조건짜리 새 대조군 수집,
(2) 240조건 추가 gust 학습데이터 수집(기존 192조건과 합쳐 총 432조건).

**대조군 결과 — 중요한 재보정**: 새 32조건 대조군(`seed30002`)으로 현재 배포
모델(12-30절)을 다시 평가하니 gust speed MAE = **1.517m/s** — 그동안 써온
"0.909m/s"(8조건 대조군 기준)보다 훨씬 나쁨. 8조건 대조군이 운 좋게 낙관적인
표본이었던 것으로 보임. **이제부터 gust 성능은 이 32조건 대조군을 기준으로
삼을 것.**

**수집 인프라 장애 재발**: 240조건 수집 중 세션 2~4가 전부 "GPS/홈 위치 확인"에서
멈추는 문제가 재발(12-16/12-30절과 같은 클래스). 이번엔 WSL 크래시는 아니었고
방금 SITL을 수동으로 다시 띄워보니 정상 기동돼서, `make distclean` + 완전
재빌드(~15분)로 대응 — 재빌드 후 스모크테스트로 GPS 락 확인 후 재개, 이후 4세션
전부 정상 완주(0 실패). SITL을 하루 안에 반복 재시작하면 빌드가 손상될 수 있다는
패턴이 세 번째로 확인된 셈 — 근본적으로는 세션 사이 재시작 횟수를 줄이거나,
반복 재시작이 예상되면 미리 주기적으로 재빌드하는 게 나을 수 있음(아직 안 함).

**재학습은 시작만 하고 중단**: 22개 기존 파일 + gust 8개 파일(432조건, 110만행)로
5-fold 재학습을 시작했으나, 사용자가 외출에서 돌아와 다음에 이어서 하기로 하고
중단시킴. `--kfold`는 원래 진단용이라 모델을 저장 안 하므로(최종 저장은 단일분할
학습이 다 끝난 뒤 1번만) **중단해도 잃는 것 없음** — `wind_estimator.pt`는 여전히
12-30절 원본 그대로 배포 중.

**결론**: 배포 모델 변경 없음. 데이터(240조건 신규 + 32조건 대조군)는 저장소에
보관, 다음 세션에 아래 명령으로 재학습 재개하면 됨:
```
cd offline_training
python train_wind_estimator.py \
  ../logs/wind_random_dither_20260816_*.csv ../logs/wind_random_dither_20260820_123957*.csv \
  ../logs/wind_gust_20260817_005926*.csv \
  ../logs/wind_random_20260820_18*.csv ../logs/wind_random_20260820_19*.csv ../logs/wind_random_20260820_20*.csv \
  ../logs/wind_gust_20260821_024909*.csv ../logs/wind_gust_20260821_040138*.csv \
  ../logs/wind_gust_20260821_051500*.csv ../logs/wind_gust_20260821_062833*.csv \
  ../logs/wind_gust_20260821_132831*.csv ../logs/wind_gust_20260821_201155*.csv \
  ../logs/wind_gust_20260821_214400*.csv ../logs/wind_gust_20260821_231544*.csv \
  --kfold 5
```
끝나면 `evaluate_checkpoint.py ../logs/wind_gust_20260821_123910_..._seed30002.csv`로
새 32조건 대조군 기준(1.517m/s) 대비 개선됐는지 판단.

**다음에 볼 것**: 위 재학습 재개 → 32조건 대조군으로 판단 → 개선되면 SITL A/B
스팟체크 후 배포.

CSV: `logs/wind_gust_20260821_123910_..._seed30002.csv`(32조건 신규 대조군),
`logs/wind_gust_20260821_132831/201155/214400/231544_..._seed2[1-4]042.csv`(4개,
240조건 신규 학습데이터).

---

## 12-36. 재학습 재개·오프라인 개선 확인, SITL A/B 5회 반복으로는 배포 기준 미달

**배경**: 12-35절에서 중단시킨 재학습(432조건, 5-fold)을 재개. 사용자가 자리를
비운 사이 학습 → 대조군 평가 → SITL A/B까지 이어서 진행.

**재학습 결과**: 5-fold 진단 speed MAE = 0.514±0.026m/s(전체 3760조건 기준,
안정적으로 수렴). 이어서 배포용 단일 80/20 분할 학습, epoch 68에서 조기종료,
val MAE=0.619m/s로 `wind_estimator.pt` 저장.

**32조건 대조군(seed30002) 평가 — 오프라인상 개선**: gust speed MAE
1.517m/s(기존 12-30절 배포 모델) → **1.218m/s**(신규, 약 20% 개선).

**SITL A/B 단일 스팟체크 — 결과가 애매함**: `pinn_wind_correction_gust_sweep.py`
4조건 1회 실행, 3/4 개선(light +21.7%, strong +56.8%, crosswind +4.0%)이지만
default_gust만 -40.6%로 크게 악화. 12-23절에서 이미 단일 시행의 분산이 크다는
게 확인된 바 있어 이 결과만으론 판단 불가 → `repeat_sweep.sh`로 5회 반복 결정.

**인프라 이슈 재발견 1 — SITL 빌드 손상**: 재빌드된 SITL에서 kill/재시작을 반복하다
프로세스가 `mavsdk_impl.cpp:806 Sending message failed`를 수천 줄 쏟아내며 멈추는
증상 발생(12-16/12-30/12-35절과 동일 클래스, 이번이 네 번째 재발). `make distclean`
+ 완전 재빌드(~15분)로 해결.

**인프라 이슈 재발견 2 — `repeat_sweep.sh` 기본 타임아웃(600초)이 실제보다 짧음**:
재빌드 후 재시도해도 5회 중 매번 3~4번째 조건 근처에서 exit 124(타임아웃)로 실패.
`set_wind()`만 격리해서 반복 호출하는 진단 스크립트로 확인한 결과, `gz topic pub`
1회 호출이 (docstring이 가정한 "수백ms"가 아니라) **약 3.5초** 걸림 — 조건당
set_wind 호출이 18회(1초 간격)라 조건 하나(OFF+ON)에 실제로 약 3분 소요, 4조건
전체면 600초를 넘김. 처음 성공했던 12-36절 이전의 "성공한" 단일 실행도 실제로는
12분 23초 걸렸던 것으로 뒤늦게 확인(파일 타임스탬프로 역산) — 원래 "3.6분"
추정치가 `gz topic pub` spawn 비용을 빼먹은 과소추정이었음. `--timeout 900`으로
올려서 해결. **`gust_sweep.py`류 스크립트를 `repeat_sweep.sh`로 돌릴 땐 기본
600초로는 부족 — 900초 이상 지정할 것.**

**SITL A/B 5회 반복 집계 — 배포 기준 미달로 결론**:

| 조건 | N | OFF 평균 | ON 평균 | 개선율 평균±SD | 범위 |
|---|---|---|---|---|---|
| light_gust | 5 | 0.116m | 0.123m | -8.1%±17.5% | -32.6%~+10.8% |
| default_gust | 5 | 0.286m | 0.289m | -1.7%±8.3% | -10.5%~+12.3% |
| strong_gust | 5 | 0.678m | 0.441m | **+26.4%±22.6%** | +5.2%~+54.8% |
| crosswind_gust | 5 | 0.340m | 0.590m | **-69.1%±82.4%** | -179.5%~+11.3% |

strong_gust만 5회 모두 양수로 확실히 개선. light/default는 평균이 표준편차보다
작아 "효과 없음"과 통계적으로 구분 안 됨. crosswind_gust는 5회 중 4회 마이너스
(최악 -179.5%)로 명백히 악화 — 옆바람에서는 보정이 순손해.

**결론**: 오프라인 풍속 추정 정확도는 20% 개선됐지만, 그게 실제 위치오차 보정
이득으로 이어지지 않음(특히 crosswind에서 뚜렷하게 역효과). **배포 안 함** —
`wind_estimator.pt`는 12-30절 원본으로 원상복구.

**다음에 볼 것**: 새 모델 자체는 대조군 CSV(`wind_gust_20260821_123910..._seed30002.csv`,
240조건 신규 학습데이터)와 함께 저장소에 남아있으니, 재도전한다면 `ACCEL_GAIN`/
`WIND_DEADBAND_MPS`를 새 모델 기준으로 재스윕(12-31절 방식)하거나, crosswind_gust만
따로 원인 조사(방향이 순수 East인 조건에서만 악화 — 좌표계/부호 버그 가능성도
배제 안 됨) 후 재시도.

CSV: `logs/pinn_correction_gust_sweep_20260823_*.csv`(단일 스팟체크 1개 + 반복 5개),
`logs/aggregate_summary_pinn_correction_gust_sweep_20260823_170924.csv`(5회 집계).

---

## 12-37. crosswind_gust "역효과" 재검증 — 10회 반복하니 사라짐, 그 세션의 일시적
문제였던 것으로 결론 (2026-08-23)

**배경**: 12-36절에서 crosswind_gust가 5회 중 4회 악화(-69.1%±82.4%, 최악 -179.5%)
로 나와 배포를 보류했던 것을, 진짜 crosswind 고유의 문제인지 재검증.

**1단계 — 원인 후보 조사(코드/데이터 분석만, SITL 사용 안 함)**:
- CSV에 남아있던 `true_wind_north/east_m_s`(실제 바람)와 `wind_est_north/east`
  (모델 추정)를 직접 비교 → 나빴던 2개 실행에서 추정 방향이 실제 바람과 최대
  177도까지 벌어짐(거의 정반대) 확인. 순간적인 튐이 아니라 ~1초간 지속되는 실제
  물리적 변위였음(텔레메트리 직접 확인).
- **학습 데이터의 방향 커버리지 문제는 기각**: 90도(crosswind 방향) 근처 학습
  조건이 199개로, light/default/strong이 몰려있는 20~30도 근처(187개)와 비슷하게
  충분함. `sample_wind_conditions`/`sample_gust_episodes` 둘 다 각도를 0~360도
  균등 샘플링이라 구조적 편향 없음.
- 결정적 단서: 나빴던 2번(174836, 180140)이 5회 중 정확히 **마지막 두 번**이었고,
  "정상"인 3번도 light/strong_gust의 각도오차가 원래 25~48도까지 나는 걸 보면
  이 지표 자체가 원래 꽤 노이즈가 있음 → crosswind 고유 결함이라기보다 그 세션
  후반부에 뭔가 있었을 가능성(같은 세션에서 "SITL 빌드 손상 4번째 재발"이 실제로
  기록돼 있음, 다만 시간순으로 이 5회 반복 전에 고쳤다고 되어있어 완전히 들어맞진
  않음 - 로그가 안 남아있어 확증은 못 함).

**2단계 — 직접 재검증**: `pinn_wind_correction_gust_sweep.py`에 `--condition` 옵션
추가(4조건 전부가 아니라 하나만 돌 수 있게 - 진단 속도용, 로직 변경 없음).
`repeat_sweep.sh --script pinn_wind_correction_gust_sweep.py --repeats 10 --timeout 400
-- --condition crosswind_gust`로 crosswind_gust만 10회 반복(약 20분 소요).

**결과 — 극단값이 하나도 안 나옴**:

| 조건 | N | OFF 평균 | ON 평균 | 개선율 평균±SD | 범위 |
|---|---|---|---|---|---|
| crosswind_gust(12-36절, 5회) | 5 | 0.340m | 0.590m | -69.1%±82.4% | -179.5%~+11.3% |
| crosswind_gust(재검증, 10회) | 10 | 0.330m | 0.326m | **+0.8%±6.9%** | -8.2%~+12.2% |

10회 개별 값: -4.1, +1.5, -1.4, -8.3, -7.3, +5.0, +10.1, +12.2, +5.5, -5.8 (%) — 전부
±12% 이내, 12-36절의 -179.5%/-158.9% 같은 극단값 없음.

**결론**: crosswind_gust는 "확실히 악화되는 조건"이 아니라 light_gust/default_gust와
같은 **"효과가 노이즈 수준(중립)"인 조건**으로 재분류해야 함. 12-36절의 극단적인
결과는 crosswind 자체의 문제가 아니라 그 세션 후반부(반복 4~5번째)에 있었던
일시적인 문제(정확한 원인은 로그 부재로 미확정 - SITL/텔레메트리 저하 쪽이 유력)
로 보임. **4개 gust 조건 중 확실히 순이익인 건 strong_gust뿐이고, 나머지 3개는
"손해는 아니지만 확신도 없음"** — 이전에 생각했던 "3개 조건에서 문제없고 1개
조건에서만 명확히 손해"가 아니라 "1개 조건만 확실히 이득, 나머지는 판단 보류"가
더 정확한 그림.

**배포 판단 재검토 필요**: 12-36절에서 "crosswind가 일관되게 악화된다"는 게
배포 보류의 핵심 근거였는데, 이게 사실이 아니었던 것으로 확인됨. strong_gust의
확실한 이득과 나머지 조건들의 "손해는 없음"을 감안하면 배포를 재고할 여지가
있음 — 다만 light/default/crosswind 3개 조건도 각각 10회씩 재검증해서 정말
"중립"이 맞는지(혹시 strong처럼 숨은 이득이 있는지) 확인한 뒤 결정하는 게 안전.

**다음에 볼 것**: light_gust/default_gust도 같은 방식(조건별 10회 반복)으로
재검증 → 4개 조건 전부 신뢰할 수 있는 숫자가 나오면 배포 여부 최종 결정.

CSV: `logs/pinn_correction_gust_sweep_20260823_21*.csv`(crosswind_gust만 10회),
`logs/aggregate_summary_pinn_correction_gust_sweep_20260823_215857.csv`(10회 집계).
코드: `pinn_wind_correction_gust_sweep.py`에 `--condition` 옵션 추가.

---

## 12-38. light_gust/default_gust도 10회 재검증 — 둘 다 중립 확인, SITL 재시작
정상종료(MAVSDK) 보강 (2026-08-23~24)

**light_gust 10회**: 개선율 -4.7%±17.2% (범위 -38.3%~+28.9%) — 12-36절 5회 결과
(-8.1%±17.5%)와 방향·크기 비슷, 극단값 없음. **중립 확정.**

**default_gust 10회**: 개선율 +1.6%±10.9% (범위 -16.2%~+17.7%) — 12-36절 5회 결과
(-1.7%±8.3%)와 마찬가지로 방향·크기 비슷, 극단값 없음. **중립 확정.**

**부수 작업 — SITL 재시작 시 정상종료 보강**: `parameters.bson` 손상 예방을 더
확실히 하기 위해 `sim_scripts/graceful_sitl_shutdown.py` 신설(MAVSDK
`Action.shutdown()` - OS 시그널이 아니라 진짜 MAVLink 정상종료 명령이라 파라미터
flush를 보장함) — 기존 `SIGTERM`/`SIGKILL`은 그대로 두고 그 앞 단계로 3개
오케스트레이션 스크립트 전부에 추가. 2회 반복으로 실제 동작 확인(SITL 없을 땐
스킵, 있을 땐 정상 종료 명령으로 즉시 종료됨).

**4개 gust 조건 현재까지 정리(전부 신뢰 가능한 표본, N≥5)**:

| 조건 | N | 개선율 평균±SD | 판정 |
|---|---|---|---|
| light_gust | 10 | -4.7%±17.2% | 중립 |
| default_gust | 10 | +1.6%±10.9% | 중립 |
| crosswind_gust | 10 | +0.8%±6.9% | 중립 |
| strong_gust | 5(12-36절, 미재검증) | +26.4%±22.6% | 개선(아직 10회 미확인) |

**다음에 볼 것**: strong_gust도 10회 재검증해서 일관성 확인 → 4개 조건 전부
정리되면 432조건 재학습 모델의 배포 여부 최종 결정.

CSV: `logs/pinn_correction_gust_sweep_20260823_23*.csv`/`_20260824_00*.csv`
(light_gust/default_gust 10회씩), 대응 `aggregate_summary_*.csv` 2개.

---

## 12-39. strong_gust 10회 재검증 — 이것도 뒤집힘, 4개 조건 전부 중립으로 최종
결론, 432조건 재학습 모델 배포 안 함 (2026-08-24)

**결과**: 개선율 +1.0%±2.1% (범위 -2.5%~+4.1%) — 12-36절 5회 결과(+26.4%±22.6%,
확실한 개선으로 봤던 유일한 조건)가 전혀 재현 안 됨. **완전히 중립.**

**4개 gust 조건 최종 정리 (전부 N=10, 신뢰 가능)**:

| 조건 | 개선율 평균±SD | 범위 | 판정 |
|---|---|---|---|
| light_gust | -4.7%±17.2% | -38.3%~+28.9% | 중립 |
| default_gust | +1.6%±10.9% | -16.2%~+17.7% | 중립 |
| crosswind_gust | +0.8%±6.9% | -8.2%~+12.2% | 중립 |
| strong_gust | +1.0%±2.1% | -2.5%~+4.1% | 중립 |

**해석**: 12-36절 5회 결과에서 나왔던 두 "극단"(crosswind의 심한 악화 -69.1%,
strong의 확실한 개선 +26.4%) 둘 다 재현이 안 됨 — 방향이 반대인 두 극단이 같은
세션에서 같이 나왔다는 건, 그 세션 자체에 (원인은 특정 못 했지만) 측정을
흔드는 무언가가 있었다는 정황을 더 강하게 뒷받침함. N=10으로 제대로 측정하면
4개 조건 전부 평균이 표준편차 안에 들어가는(strong_gust는 특히 표준편차 자체가
2.1%로 작아서 "정말로 효과가 없다"에 가까움) 깨끗한 중립.

**결론 — 432조건 재학습 모델은 배포 안 함**: 12-35절에서 이 모델의 오프라인
gust 추정 정확도가 20% 개선(1.517→1.218m/s)된 건 사실이지만, 그 개선이 실제
비행 제어 효과로는 **4개 조건 어디에서도 신뢰할 수 있게 나타나지 않음** —
추정이 정확해진 것과 그 추정으로 만든 가속도 보정이 실제로 도움이 되는 것은
별개 문제라는 12-30절의 교훈이 이번에도 재확인됨. 원본(12-30절) 모델을 그대로
유지. 재학습 자체(432조건 데이터, 코드)는 저장소에 남아있으니 나중에 다른
방식(파라미터 재튜닝, 모델 구조 변경 등)으로 재도전 가능.

**이번 조사 전체의 방법론적 교훈**: N=5는 이 정도 규모의 A/B 테스트에서
"극단값 하나가 결론을 뒤집을 수 있는" 수준으로 부족한 표본이었음 — 앞으로 gust
A/B는 기본을 N=10으로 잡을 것. 5회로 결론을 냈던 과거 결과(12-19/20/24 등)도
시간 나면 재검토 가치 있음(다만 그것들은 이번처럼 방향이 극단적으로 갈린 사례는
아니었음).

**다음에 볼 것**: gust 쪽은 일단락 - `ACCEL_GAIN`/`WIND_DEADBAND_MPS`를 이
(미배포) 모델 기준으로 재튜닝하면 상황이 바뀔 수도 있으나 우선순위 낮음. 대신
README/EXPERIMENTS.md "남은 것" 목록의 다른 항목(`TAU_FF_GAIN` 캘리브레이션,
실기 이관, 캡스톤 보고서)으로 넘어가는 게 나을 듯.

CSV: `logs/pinn_correction_gust_sweep_20260824_01*.csv`(strong_gust 10회),
`logs/aggregate_summary_pinn_correction_gust_sweep_20260824_005338.csv`(10회 집계).

---

## 12-40. `TAU_FF_GAIN` 물리 단위 캘리브레이션 — 계산은 성공했지만 그 값을
그대로 쓰면 발산함 (2026-08-24)

**배경**: `TAU_FF_GAIN=0.2`는 12-17절 스윕으로 찾은 경험값이지, "N·m를 PX4의
정규화 토크 스케일([-1,1])로 바꾸는 진짜 변환 배율"이 얼마인지는 몰랐음.

**계산 방법**: 새 비행 없이 기존 더더링 학습 데이터(15개 파일, 81만 샘플)를
재활용 — 더더링 자체가 매 스텝 **알려진 크기의 정규화 토크**(`dither_x/y/z`)를
같은 `TAU_FF` 채널로 주입하고 있었고, 그 결과 로터 회전속도(`actuator0~3`)도
같이 로깅돼 있었음. `wind_pinn_model.py`의 이미 검증된 모터 물리모델
(`motor_torque()` - x500 SDF 실측 관성모멘트·모터상수·로터 배치 사용)로 로터
회전속도를 실제 N·m로 환산한 뒤, 그 값을 주입한 `dither_x/y/z`(정규화값)에
선형회귀 — 기울기가 "정규화 1단위 = 몇 N·m인지"를 직접 알려줌.

**결과** (95% 신뢰구간 매우 좁음, n=810,000):

| 축 | 정규화 1.0 = 실제(N·m) | 역산한 진짜 게인(N·m→정규화) |
|---|---|---|
| roll | 0.256 ± 0.005 | 3.90 |
| pitch | 0.159 ± 0.004 | 6.30 |
| yaw | 0.238 ± 0.001 | 4.20 |

roll이 pitch보다 게인이 작아도 되는 건 물리적으로 타당함(로터 배치상 roll 쪽
모멘트암(~0.21m)이 pitch 쪽(~0.13m)보다 길어 토크 효율이 더 좋음). 기존
`TAU_FF_GAIN=0.2`는 이 값의 **1/20 수준**이었음.

**SITL 검증 — 물리값 근처에서 이미 발산**: 안전하게 낮은 값부터 단계적으로
테스트할 계획이었으나, 첫 단계(gain=1.0, 기존값의 5배, 계산된 물리값의 1/4~1/6
수준)에서부터 이미 명확히 악화됨 — default 조건 자세 피크편차 4.35°→8.49°
(개선율 -95.0%). 계산된 물리값(3.9~6.3)까지는 시도도 안 하고 중단.

**해석**: 물리적으로 계산한 배율 자체는 정확할 가능성이 높음(회귀 신뢰구간이
매우 좁고, 축간 차이도 물리적으로 설명됨) — 다만 그건 "노이즈 없는 완벽한
추정치를 완벽하게 보정에 쓴다면"의 정답이지, **실제로는 모델의 tau_disturbance
추정치 자체에 노이즈가 있고, 그 노이즈가 게인만큼 그대로 증폭**돼서 피드포워드가
아니라 외란을 새로 만드는 꼴이 됨. 즉 12-30절에서 이미 나온 교훈("추정 정확도와
이 특정 게인으로 만드는 제어 효과는 별개 문제")이 게인 캘리브레이션에도 그대로
적용됨 — 기존 `TAU_FF_GAIN=0.2`가 낮았던 건 계산이 틀려서가 아니라, 노이즈
증폭을 막기 위해 경험적으로 필요했던 값이었던 것.

**결론**: `TAU_FF_GAIN`을 물리값(3.9~6.3)으로 바꾸지 않음 — 기존 0.2 유지.
이번 계산은 "왜 0.2가 물리값보다 훨씬 작아야 했는지"를 정량적으로 설명해준
것으로 의미를 둠(1/20 정도의 마진이 노이즈 증폭을 억제하는 데 필요했다는 뜻).

**다음에 볼 것**: 우선순위 낮음 - 0.2와 1.0 사이(예: 0.5)에서 더 나은 값이
있는지 탐색해볼 여지는 있으나, 이미 12-17절에서 스윕으로 0.2가 최선이라고
확인된 바 있어 실익이 크지 않음. 노이즈를 줄이는 쪽(추정치에 스무딩 추가 등)이
먼저면 물리 게인에 더 가깝게 갈 여지가 생길 수도 있음 - 별도 조사 필요.

CSV: `logs/pinn_rotation_correction_20260824_020638.csv`(gain=1.0 스팟체크).

---

## 12-41. 병진/회전 5조건 A/B를 N=10으로 재검증 — 병진은 방향 유지(과대추정만 시정),
회전은 완전히 뒤집힘 (2026-08-24)

**배경**: 4.2/4.3절의 "재튜닝 후(gain=0.05)"·"5조건 확장 검증"·"첫 순이익" 수치들은
사실상 대부분 **1회 측정**이었다(`repeat_sweep.sh`로 5회 반복한 기존 매니페스트들은
전부 12-30 혼합 재학습 이전 구모델 기준). 12-37~39절에서 gust A/B가 N=5→N=10으로
뒤집힌 걸 겪은 뒤, 같은 검증을 현재 배포 모델·현재 게인(ACCEL_GAIN=0.05,
TAU_FF_GAIN=0.2) 기준 병진/회전 5조건에도 그대로 적용해봤다. `repeat_sweep.sh
--script pinn_wind_correction_sweep.py --repeats 10`과 `--script
pinn_rotation_correction_test.py --repeats 10`을 순차 실행(SITL 매회 재시작),
총 20회 반복 모두 에러 없이 성공.

**병진 결과** (매니페스트: `logs/repeat_sweep_20260824_025009_pinn_wind_correction_sweep_manifest.txt`):

| 조건 | N=10 평균±표준편차 | 기존(1회) | 비고 |
|---|---|---|---|
| calm | -9.2%±37.6% | - | 절대오차 0.04m, 노이즈 |
| light | +10.4%±10.6% | - | |
| default | +1.9%±4.7% | -2.2%(중립) | 방향 바뀜(음→미약한 양) |
| strong | +3.0%±5.1% | +38.2% | **방향은 유지, 크기는 대폭 과대추정이었음** |
| crosswind | +14.3%±4.7% | +17.2% | 대체로 일치 |

병진은 방향이 뒤집히진 않음 — 다만 strong의 "+38.2%"는 1회 측정의 과대추정이었고
실제로는 +3.0%±5.1% 수준. calm 빼고 전 조건 순이익 유지.

**회전 결과** (매니페스트: `logs/repeat_sweep_20260824_035625_pinn_rotation_correction_test_manifest.txt`):

| 조건 | N=10 평균±표준편차 | 기존(1회/소표본) | 비고 |
|---|---|---|---|
| calm | -6.7%±40.3% | -4.5% | 절대오차 0.4도, 노이즈 |
| light | -6.5%±6.8% | (미측정) | 음수, CI가 0을 넘지 않음 |
| default | **-7.9%±3.5%** | +2.8~3.8% | **완전히 뒤집힘, 표준편차 작아 신뢰도 높음** |
| strong | **-3.6%±3.4%** | +3.2% | **뒤집힘, 경계선이지만 양수 아님** |
| crosswind | +3.3%±4.0% | (미측정) | 유일하게 양성이나 CI가 0에 걸침 |

3.2/4.3절에서 "첫 순이익"이라 결론 냈던 회전 피드포워드가 **N=10에서는 5조건 중
확실히 양성인 게 하나도 없다** — default/light/strong은 오히려 자세오차를 키우고
있고(그것도 표준편차가 작아 노이즈로 치부하기 어려움), crosswind만 미약하게
양성(CI가 0에 걸침). gust 때와 달리 이번엔 "과거의 극단값이 사라지는" 패턴이
아니라 **부호 자체가 반대**로 나왔다.

**원인 가설**: `TAU_FF_GAIN=0.2`는 12-17절에서 스윕으로 확정된 값인데, 그건
**12-30절 혼합 재학습 이전** 구모델 기준이었다. 병진 쪽은 이 문제를 12-31절에서
이미 겪고 `ACCEL_GAIN`을 0.15→0.05로 재스윕해서 해결했는데, **회전 쪽은 같은
재스윕을 한 번도 안 했다** — 3.4절 교훈("게인이 특정 모델의 출력 스케일에 맞춰
튜닝돼 있으면 모델이 바뀔 때 재튜닝이 필요하다")이 회전 축에는 아직 적용되지
않은 상태였던 것. 12-40절에서 `TAU_FF_GAIN` 물리값(3.9~6.3)을 계산하고 gain=1.0
스팟체크에서 발산을 확인했지만, "그 사이 어딘가(0.2~1.0)에 새 모델 기준 최적값이
있을 수 있다"는 가능성은 그때 "우선순위 낮음"으로 미뤄뒀었다(12-40 "다음에 볼 것")
— 이번 N=10 결과로 그게 더 이상 낮은 우선순위가 아님이 드러남.

**결론**: 현재 배포 설정(`TAU_FF_GAIN=0.2`, 12-30 혼합 재학습 모델)에서 회전
피드포워드는 **신뢰할 수 있는 순이익이 없다** — 3개 조건에서 오히려 자세오차가
커짐. 실비행 배포 판단이 있다면 재검토 필요. 다음 조치로 `TAU_FF_GAIN`을 현재
모델 기준으로 재스윕(12-31절 `ACCEL_GAIN` 재스윕과 같은 패턴)한 뒤, **처음부터
N=10 반복으로 검증**할 것 (1회 측정으로 판단 안 함).

---

## 12-42. `TAU_FF_GAIN` 재스윕 착수 — 12-30 재학습 모델 기준, 처음부터 N=5/N=10으로
검증 (2026-08-24, 진행 중)

**배경**: 12-41절에서 회전 피드포워드가 배포 게인(0.2)으로는 N=10 기준 신뢰할 수
있는 순이익이 없다는 게 드러났다. `TAU_FF_GAIN=0.2`는 12-17절에서 스윕으로
확정된 값인데, 그건 **12-30 혼합 재학습 이전** 모델 기준 — `ACCEL_GAIN`이 이미
겪었던 "구모델 기준 게인" 문제(3.4/3.5절)가 회전 쪽엔 아직 안 걸려 있었을
가능성이 높다.

**계획**: (1) 코스 스윕 — gain ∈ {0.05, 0.10, 0.15, 0.20, 0.30}, 조건은 12-17절과
같은 default+strong(효과가 가장 크게 나타나는 대역), 각 조합 **N=5 반복**(12-17절
원래 스윕은 1회 측정이었음 — 이번엔 스윕 단계부터 반복측정 적용). 값 범위를
0.2 아래로 넓게 잡은 이유: 12-17절 원래 스윕도 "gain을 낮출수록 좋아지다 0.2
근방에서 최선"이었던 패턴이 있었고(gain=0.5→-4.1%, gain=0.2→+2.8~3.8%,
0.2 미만은 미탐색), `ACCEL_GAIN`도 재학습 후 0.15→0.05로 더 낮아진 전례가 있어
같은 방향을 예상함. (2) 코스 스윕에서 가장 좋은(평균 개선율 높고 표준편차도
작은) gain을 골라 5조건 전체를 N=10으로 검증.

스크립트: `sim_scripts/correction_experiments/repeat_sweep.sh --script
pinn_rotation_correction_test.py --repeats 5 --timeout 300 -- <gain> <condition>`
(target script가 위치인자로 gain/condition_label을 받는 기존 CLI 그대로 사용,
새 코드 작성 없음). 총 10개 조합(5 gain × 2 condition) × 5회 = 50회 실행,
1회당 실측 ~1.3~1.5분 페이스 기준 예상 70~90분(실제로는 조합당 SITL 재시작
포함 ~9.5분, 총 97분 소요 — 단일조건이라도 restart_sitl()의 안정화 대기(15s)+
재시작 오버헤드가 비행시간(약 48s)보다 커서 예상보다 느렸음).

**결과** (N=5, `logs/repeat_sweep_20260824_0[5-6]*_pinn_rotation_correction_test_manifest.txt`
10개):

| gain | default 평균±표준편차 | strong 평균±표준편차 | 비고 |
|---|---|---|---|
| 0.05 | -1.0%±3.0% | -0.4%±2.3% | 둘 다 사실상 0(노이즈 수준) |
| 0.10 | -2.1%±2.8% | **+1.9%±2.5%** | 조합 평균이 가장 0에 가까움(-0.2%) |
| 0.15 | -1.9%±2.1% | -0.6%±2.1% | |
| 0.20(현재 배포값) | -0.9%±1.4% | -1.3%±2.2% | 표준편차는 가장 작지만 둘 다 음수 |
| 0.30 | **-5.1%±2.3%** | **-2.6%±0.9%** | 확실히 나쁨(CI가 0을 안 넘음) |

**정직한 결론**: 이 범위(0.05~0.20) 안에서는 **default·strong 둘 다 확실히
양성인 gain이 하나도 없다** — 전부 평균이 0 근처에서 흔들리고 표준편차가
평균보다 커서(0.30 제외) 통계적으로 0과 구분 안 됨. 12-17절의 "gain=0.2에서
+2.8~3.8%"는 그 자체가 1회 측정의 우연이었을 가능성이 높다 — 이 코스 스윕
결과가 3.9/12-41절의 "N=10 재검증에서 뒤집힘"과 같은 방향으로 다시 한 번
확인해주는 셈. 유일하게 명확한 신호는 **0.30은 확실히 나쁘다**는 것뿐.

**다음 조치**: 그래도 확인차 5조건 전체를 N=10으로 검증할 gain을 하나 고른다면
`gain=0.10`(조합 평균 -0.2%로 가장 0에 가깝고, strong의 평균이 유일하게 양수) —
다만 이건 "유망한 후보"라기보다 "가장 덜 나쁜 후보"에 가깝다는 걸 분명히 해둠.

**5조건 N=10 검증 결과** (`logs/repeat_sweep_20260824_065909_pinn_rotation_correction_test_manifest.txt`):

| 조건 | gain=0.10 (N=10) | gain=0.20, 배포값 (N=10, 12-41) | 방향 |
|---|---|---|---|
| calm | -5.8%±13.5% | -6.7%±40.3% | 둘 다 노이즈(절대오차 작음) |
| light | +0.4%±5.0% | -6.5%±6.8% | **개선(음수→거의 0)** |
| default | -3.1%±2.8% | -7.9%±3.5% | **개선(그러나 여전히 확실히 음수**, 1-표준편차 구간이 0을 안 넘음) |
| strong | -0.6%±2.3% | -3.6%±3.4% | **개선(음수→거의 0)** |
| crosswind | +4.9%±3.4% | +3.3%±4.0% | **개선(양수 폭 커짐**, 1-표준편차 구간이 0을 안 넘음) |

**결론**: gain을 0.20→0.10으로 낮추면 **5개 조건 전부 방향이 개선**된다 —
`ACCEL_GAIN`이 재학습 후 더 낮은 값(0.15→0.05)을 필요로 했던 것과 같은 패턴이
회전에도 실제로 존재한다는 증거. 다만 이게 "배포해도 되는 수준"은 아직 아님 —
`default`가 여전히 확실하게 음수(-3.1%±2.8%, CI가 0에 안 걸림)라 5조건 중
1개는 여전히 손해. `calm`/`light`/`strong`은 노이즈~중립, `crosswind`만 확실한
순이익. **정직한 최종 결론**: 0.05~0.30 범위에서 스윕한 5개 gain 중 5조건
전부에서 신뢰할 수 있는 순이익을 내는 값은 없었다. gain=0.10이 가장 유망한
방향(더 낮출수록 나아지는 추세)이지만, 이 스윕 범위 안에서 "배포할 만한 값"을
찾지 못했다 — 회전 피드포워드는 **현재 미해결 상태로 남겨둔다**(배포 안 함).

**다음에 볼 것**: (1) 0.05 아래(예: 0.02~0.05 사이)로 더 낮춰보는 미세 스윕 —
추세가 이어지는지 확인. (2) 12-40절에서 제안했던 추정치 스무딩(EMA 등)으로
노이즈를 먼저 줄인 뒤 게인 탐색을 다시 하는 게 게인만 만지는 것보다 근본적일
수 있음. (3) 혹은 회전 피드포워드 자체를 이 프로젝트 스코프에서는 "시도했으나
현재 미해결"로 정리하고 실기 이관 등 다른 우선순위로 넘어가는 것도 합리적 선택.

---

## 12-43. correction_experiments 텔레메트리/오프보드 진입부 보일러플레이트 정리 (2026-08-24)

**배경**: 12-33절에서 `set_wind()`/`PINNCorrector`/`EmaSmoother`/`apply_deadband`를
`correction_common.py`로 통합할 때, "텔레메트리 모니터·오프보드 송신 보일러플레이트"는
스크립트마다 페이로드가 달라 위험 대비 이득이 작다고 보고 일부러 안 건드렸었음
(README/EXPERIMENTS.md "남은 것" 목록에 계속 남아있던 항목). 이번에 4개 스크립트를
다시 비교해보니, "페이로드가 다른" 부분(가속도/토크를 실제로 흘려보내는
`offboard_sender` 루프 자체)과 "완전히 동일한" 부분(연결·GPS확인, arm+이륙,
pv/att/gyro 모니터, 초기 offboard 진입)이 섞여 있었다는 걸 재확인 — 후자만 골라서
정리.

**작업**: `correction_common.py`에 4개 함수 추가:
- `connect_and_wait_ready(drone)` — SITL 연결 + GPS/홈 위치 확인
- `arm_and_takeoff(drone, safe_altitude_m, takeoff_timeout_s)` — arm+이륙+고도
  대기, 타임아웃 시 착륙 후 False
- `start_telemetry_monitors(drone)` — pv/att/gyro 모니터 태스크 3개 시작 + 첫 값
  대기, `(latest_pv, latest_att, latest_gyro, pv_task, att_task, gyro_task)` 반환
- `start_offboard_hold(drone, nominal)` — 현재 위치로 offboard 진입, 실패 시
  착륙 후 False

4개 스크립트(`pinn_wind_correction_sweep.py`, `pinn_wind_correction_gust_sweep.py`,
`pinn_correction_param_tuning.py`, `pinn_rotation_correction_test.py`) 전부 이
함수들을 쓰도록 교체 — 순수 리팩터링, 판정 로직·타이밍·CSV 데이터는 전부 동일.
회전 스크립트는 원래 다른 3개보다 print 문구가 약간 간략했던 부분(예: "-> Armed",
"안전 고도 도달 대기 중..." 줄 없음)이 있었는데, 이번에 더 자세한 쪽으로
통일함 — 콘솔 로그 문구만 살짝 달라지고 동작은 동일. `offboard_sender` 루프
자체(실제 가속도/토크 송신, 더더링, 적응형 게인)는 스크립트마다 여전히 달라
그대로 둠.

4개 스크립트 총 328줄 삭제, `correction_common.py`에 145줄 추가 — 순감소 183줄.

**검증**: SITL에서 두 스크립트를 실제로 돌려 확인 — `pinn_rotation_correction_test.py`
(gain=0.10, calm 단일조건, 개선율 -3.6%로 정상 범위) 및
`pinn_wind_correction_sweep.py`(5조건 전체, 4/5 개선, 송신 간격 통계 정상
50.0ms±4.5ms) 둘 다 연결→GPS확인→arm→이륙→모니터→offboard진입→트라이얼→착륙까지
에러 없이 완주. 나머지 2개 스크립트(`pinn_wind_correction_gust_sweep.py`,
`pinn_correction_param_tuning.py`)는 똑같은 공유 함수를 그대로 호출하는 구조라
별도 실행 검증은 생략(리스크 낮음으로 판단).

---

## 12-44. 회전 tau 추정치 EMA 스무딩 탐색 — N=3 프로브로는 결론 못 냄 (2026-08-24)

**배경**: 12-42절에서 게인 재스윕만으로는 회전 피드포워드를 못 고쳤고, "다음에 볼 것"으로
추정치 자체의 노이즈를 줄이는 접근(스무딩)이 제안됐었음. 병진 스크립트는 이미
`EmaSmoother`(시상수 0.4초)로 풍속 추정치를 스무딩한 뒤 게인을 곱하는데, 회전
스크립트(`pinn_rotation_correction_test.py`)는 지금까지 모델 원시 출력을 스무딩
없이 그대로 게인에 곱해 쏘고 있었음을 코드로 재확인 — 12-40절 "노이즈가 게인만큼
증폭된다" 가설과 정확히 맞아떨어지는 구조였음.

**작업**: `pinn_rotation_correction_test.py`에 4번째 위치인자 `tau_smooth_s`
추가(기본값 0.0 = 스무딩 없음, 기존 동작과 완전히 동일해 하위호환). 0보다 크면
`correction_common.EmaSmoother`(병진과 동일 클래스, 트라이얼마다 새로 생성)를
tau_x/y/z 각 축에 적용한 뒤 게인을 곱함. CSV에 `tau_x_raw/y_raw/z_raw` 진단용
컬럼 추가(스무딩 전 원시값 - 스무딩 전후 비교용). `TARGET_YAW_DEG` 파싱이
빈 문자열("")에서 죽던 사소한 버그도 이 작업 중 발견해 같이 고침(4번째 인자를
쓰려면 3번째를 비워야 하는 CLI 사용 패턴에서 드러남).

**프로브**: gain=0.10(12-42 최선값 고정), default 조건(N=10에서 가장 확실히
나빴던 조건, 기준값 -3.1%±2.8%) 에서 시상수 {0.0, 0.1, 0.2, 0.4}초를 각각
**N=3**으로 빠르게 스캔(총 12회, 약 24분):

| tau_smooth_s | N=3 평균±표준편차 | 범위 |
|---|---|---|
| 0.0 (스무딩 없음, 기준) | +1.9%±3.4% | -2.4%~+6.0% |
| 0.1 | -1.5%±0.8% | -2.5%~-0.6% |
| 0.2 | -4.7%±3.4% | -9.5%~-1.8% |
| 0.4 | -1.1%±1.4% | -3.0%~+0.0% |

**정직한 결론**: 이 프로브는 **판단이 안 선다**. 결정적인 문제: `tau_smooth_s=0.0`
(스무딩 없음) 조합은 12-42절의 N=10 기준값(-3.1%±2.8%)과 완전히 같은 설정인데도
이번 N=3 표본은 +1.9%±3.4%로 나옴 — 부호까지 다르다. 이건 스무딩 없음 자체가
개선됐다는 뜻이 아니라, **N=3이 이 정도 크기(표준편차 ~3%대)의 신호와 노이즈를
구분하기엔 턱없이 부족하다**는 뜻 — 오늘 밤 이미 두 번(gust N=5→N=10, 회전
N=5→N=10) 겪은 교훈이 세 번째로, 이번엔 실시간으로 재현된 셈. 0.2가 제일
나빠 보이지만(-4.7%±3.4%) 이것도 N=3 표준편차 안에서 다른 값들과 통계적으로
구분되지 않음. **스무딩이 도움이 되는지 안 되는지조차 이 프로브로는 알 수 없다.**

**다음에 볼 것**: 뭔가 결론을 내려면 최소 N=10씩(4값×N=10=40회, ~70분)은 돌려야
함 — 이번엔 사용자가 "이거도 해봐" 한 건으로 스코프를 좁게 요청했으므로, 그
후속 스윕은 별도 승인 후 진행. 혹은 4개 값 다 도긴개긴이라면 스무딩 시상수
범위(0.1~0.4초) 자체가 틀렸을 수도 있음 - 더 짧은(0.05초) 값이나 훨씬 긴(1초+)
값도 검토 대상.

CSV: `logs/repeat_sweep_20260824_120347_pinn_rotation_correction_test_manifest.txt`
포함 4개 매니페스트(시상수별), 요약 CSV들도 `logs/aggregate_summary_pinn_rotation_correction_*.csv`.

---

## 12-45. tau 스무딩 시상수 4개를 N=10으로 제대로 재검증 — 약한 개선 신호, 확정은 아님 (2026-08-24)

**배경**: 12-44절의 N=3 프로브가 스스로의 N=10 기준값조차 재현 못 해 판단 불가로
끝남 - 같은 4개 시상수({0.0, 0.1, 0.2, 0.4}초, gain=0.10, default 조건)를
이번엔 N=10으로 정식 재검증.

**결과** (`logs/repeat_sweep_20260824_1[3-4]*_pinn_rotation_correction_test_manifest.txt`
4개):

| tau_smooth_s | N=10 평균±표준편차 | 범위 |
|---|---|---|
| 0.0 (스무딩 없음) | -1.9%±5.1% | -10.2%~+6.9% |
| 0.1 | -0.6%±3.0% | -5.6%~+4.7% |
| 0.2 | **+0.5%±3.8%** | -4.6%~+6.3% |
| 0.4 | -3.1%±3.0% | -8.6%~+1.3% |

**신뢰도 체크**: 이번 `tau_smooth_s=0.0` 결과(-1.9%±5.1%)는 12-42절의 동일 설정
N=10 기준값(-3.1%±2.8%)과 점추정치는 다르지만 신뢰구간이 크게 겹쳐 통계적으로
모순되지 않음(12-44절의 N=3 때처럼 부호가 아예 반대로 나온 것과는 다름) - N=10도
완전히 수렴한 표본은 아니지만, 최소한 이번 결과를 믿을 근거는 있음.

**해석**: 4개 값 다 표준편차(3~5%)가 평균보다 크거나 비슷해서 서로 통계적으로
확실히 구분되진 않는다. 다만 경향은 있음 — `default`가 12-42절 N=10 검증에서
**유일하게 확실히 나빴던 조건**(-3.1%±2.8%, CI가 0을 안 넘음)이었는데, 스무딩
0.2초를 넣으면 +0.5%±3.8%로 **CI가 0을 넉넉히 포함하는 "중립"** 상태로 바뀐다.
"확실한 손해"에서 "노이즈 수준"으로 바뀐 것 자체는 방향이 맞는 신호이지만,
"확실한 순이익"이라 부르기엔 아직 이르다(평균이 겨우 +0.5%, CI가 넓음).

**결론**: 스무딩(특히 0.2초)이 `default` 조건을 더 나쁘게 만들지는 않고, 오히려
가장 나빴던 조건을 중립 수준으로 끌어올리는 정도의 약한 개선 신호는 있다.
하지만 아직 "배포해도 될 만큼 확실"하다고 하기엔 부족 - 다음 단계는 5조건
전체를 gain=0.10 + tau_smooth_s=0.2로 N=10 검증해서 다른 조건(특히 이미 양호했던
light/strong/crosswind)을 스무딩이 훼손하지 않는지 확인하는 것. 이건 사용자
승인 후 진행.

---

## 12-46. 프로젝트 목표 재조정 — 실기 실측 제외, "웨이포인트 이동 정확도" 검증으로 전환 (2026-08-28)

**배경**: 원래 계획은 실제 하드웨어(Holybro X500 V2 + Pixhawk 2.4.8, 12-40절 이후
메모리에 기록된 실기 사양)로 직접 측정한 데이터와 시뮬레이션 결과를 합쳐서
검증하는 것이었음. 사용자가 이 계획을 변경 — **실기 실측은 스코프에서 제외**,
대신 "임의의 목표 좌표를 찍으면, 바람이 부는 중에도 PINN 보정을 켠 채로 그
지점까지 정확하게 도달하는지"를 SITL만으로 검증하는 쪽으로 목표를 조정.

**기존 실험들과의 차이**: 지금까지 4개 correction_experiments 스크립트는 전부
"제자리를 유지하는 동안 바람에 얼마나 안 밀리는지"만 쟀음 - 목표 위치(nominal)가
트라이얼 내내 고정이었음. 이번 목표는 목표 위치가 이동하는 시나리오(웨이포인트
커맨드)라 새 스크립트가 필요했음.

**새 스크립트**: `pinn_waypoint_correction_test.py`. 기존 `pinn_wind_correction_sweep.py`
구조를 재사용(`correction_common.py`의 connect/arm/monitor/offboard-hold 함수
그대로 사용, 새 공통 함수 추가 없음). 트라이얼 구조: 홈으로 복귀+안정화(무풍,
`RETURN_HOME_WAIT_S=10초` - 기존 `PHASE_GAP_S=4초`보다 넉넉하게 잡음, 실제로
~10m를 왕복 비행해야 하므로) → 바람 온셋 → 목표 웨이포인트(홈에서 동쪽으로
`WAYPOINT_OFFSET_EAST_M=10m`) 커맨드 → `TRIAL_DURATION_S=15초` 관찰. 이동
축을 동쪽으로 잡아서 기존 5개 바람벡터(calm/light/default/strong/crosswind) 중
`crosswind`(vx=0,vy=6)가 이 실험에서는 말 그대로 진행방향에 수직인 옆바람이 됨.

**지표 설계**: 이동 중 오차(peak)는 "목표까지 얼마나 남았나"를 재는 것뿐이라
의미가 없음(당연히 초반엔 ~10m) - 대신 **정상상태 위치오차**(트라이얼 마지막
`SETTLE_WINDOW_S=5초` 동안의 pos_error 평균, "도달 후 얼마나 정확히 머무는지")를
주 지표로 삼음.

**스모크테스트 결과** (각 조건 1회, SITL 실사):
- **calm**(무풍): OFF=0.096m, ON=0.087m — 둘 다 매우 정확, 이동 메커니즘 자체는
  정상 작동 확인(peak≈9.95m로 실제 10m 목표를 향해 이동했다가 잘 도달함)
- **strong**(8.5m/s): OFF=0.031m, ON=**0.066m** (개선율 -116.9%) — **PID만으로도
  이미 3cm까지 정확**, PINN 보정을 켜면 오히려 소폭 나빠짐(그래도 6.6cm로 절대치는
  무의미한 수준)

**중요한 발견**: PX4 내장 포지션 컨트롤러의 적분(integral) 항이 있어서, 목표
지점에 충분히 오래 머물면(이 실험의 SETTLE_WINDOW처럼) 바람이 얼마나 불든
정상상태 오차를 결국 거의 0으로 수렴시킨다 - "제자리 유지" 실험(4.2절)에서는
이런 적분 수렴이 일어나기 전(온셋 직후 과도구간)의 피크 오차를 쟀기 때문에
PINN 피드포워드가 그 과도구간을 줄이는 효과가 뚜렷했지만, "도달 후 정착 상태"를
재는 이 지표에서는 PID 자체가 이미 거의 완벽해서 PINN 보정이 개선할 여지가
거의 없고 오히려 미세한 추정 노이즈를 더할 뿐일 수 있음.

**미해결/다음에 볼 것**: 이 지표(정상상태 위치오차)로는 PINN 보정의 효과가 잘
안 보일 가능성이 있음. 대안: (1) 이동 중 궤적 정확도(경로 이탈량, 과도구간 자체를
지표로) - 4.2절의 "피크 오차" 철학과 더 비슷함, (2) settle window를 훨씬 짧게
잡아 "도달 직후"(적분이 아직 다 수렴하기 전) 오차를 보는 방법, (3) 이 지표
그대로 나머지 조건(light/default)까지 마저 스모크테스트해서 패턴을 더 보는 것.
사용자 의견 대기 중.

CSV는 스모크테스트 산출물이라 커밋 안 함(재현 가능, 저장 안 함).

---

## 12-47. 웨이포인트 지표를 "도달직후 피크오차"로 재설계 — 검증 도중 중단, 다음에 이어서 (2026-08-28)

**배경**: 12-46절에서 나온 세 가지 대안 중 (2)번(정상상태 대신 "도달 직후,
적분이 다 안 감긴 구간"의 오차를 재는 방향)으로 진행하기로 판단(사용자가
"알아서 해봐"로 위임). 먼저 실측으로 근거를 만듦: calm 조건에서 목표(10m)까지
transit이 t≈2.9초 만에 끝나는 것 확인. strong 조건에서는 도달 직후(t≈2.35초,
0.185m까지 근접) 바람이 다시 밀어내며 t≈2.9초에 **피크 0.524m**까지 벌어졌다가
서서히 줄어 t≈6.9초쯤 노이즈 바닥(0.02~0.03m)으로 수렴하는 걸 CSV로 직접 확인 -
기존 실험들(3.1/3.3절)의 "바람 온셋 직후 피크" 패턴이 여기도 그대로 있었음.

**지표 재설계**: `pinn_waypoint_correction_test.py`에 `ARRIVAL_THRESHOLD_M=2.0`(이
거리 안에 처음 들어온 시점을 "도달 시작"으로 봄), `ARRIVAL_WINDOW_S=5.0`(그
시점부터 이 기간 동안의 peak pos_error가 새 주 지표 `arrival_peak_error`) 추가.
기존 정상상태 오차(`steady_state_error`)는 참고용으로 계속 같이 출력.
`run_trial()`이 이제 `(arrival_peak_error, steady_state_error, transit_peak)`
3개를 반환하도록 변경.

**진행 상황**: strong 조건으로 재검증을 시작했으나 **사용자 요청으로 중단**
(진행 중이던 SITL 프로세스·백그라운드 대기 전부 정리함). 중단 직전 OFF 트라이얼
결과만 확보: `arrival_peak_error(OFF)=1.923m`(정상상태=0.040m, 이동중peak=9.592m) -
직접 CSV로 확인했던 앞선 1회(0.524m)와 값이 꽤 다름. 같은 조건·같은 코드인데도
이렇게 편차가 크다는 것 자체가, 오늘 밤 내내 겪은 "N=1은 못 믿는다"는 교훈이 이
지표에도 똑같이 적용될 거라는 신호 - 나중에 반드시 여러 번(N≥5) 반복해서
확인할 것. ON 트라이얼은 실행 전에 중단됨. CSV는 미완성 산출물이라 커밋 안 함.

**다음에 이어서 할 일**:
1. strong 조건 OFF/ON 재검증 마무리 (새 지표로 최소 몇 회는 반복해서 편차 확인)
2. 나머지 4개 조건(calm/light/default/crosswind)도 새 지표로 스모크테스트
3. 패턴이 괜찮아 보이면 `repeat_sweep.sh`로 N=10 반복 검증 — 단, `aggregate_repeats.py`는
   "전체 트라이얼 peak"를 계산해서 이 실험엔 안 맞음(트라이얼 초반 이동거리
   자체가 peak로 잡힘) - 이 지표 전용 집계 스크립트를 새로 만들거나
   `aggregate_repeats.py`를 확장해야 함

---

## 12-48. 문턱 기반 "도달 시점" 정의의 버그 발견·수정 — SITL 재검증 안 된 채로 세션 종료 (2026-08-28 밤)

**재개**: 12-47절에서 중단됐던 strong OFF/ON을 마저 끝내고 나머지 4개 조건까지
5조건 전체를 한 번씩 도는 전체 스윕으로 재개(사용자 지시로 N=10 반복검증은
이번엔 빼고 나머지만).

**버그 발견**: 결과가 이상했음 — calm/light/default/strong/crosswind **5개
조건 전부 arrival_peak_error가 1.7~2.0m로 몰려나옴**(calm 1.733m, light 1.772m,
default 1.957m, strong 1.878m, crosswind 1.870m). 무풍(calm)인데도 1.7m나
되는 게 말이 안 됨 - 바람 세기와 무관하게 다 비슷한 값이라는 것 자체가 이
지표가 바람이 아니라 다른 걸 재고 있다는 신호.

**원인**: `ARRIVAL_THRESHOLD_M=2.0`을 "도달 시작" 기준으로 삼았는데, 드론이
목표에 접근하는 중(아직 감속 안 끝남)에 이 문턱을 통과하는 순간의 pos_error
값 자체가 항상 ~2.0m 근처 - 그리고 이 값이 문턱 통과 직후 window의 첫 샘플이라
사실상 항상 그 window의 최댓값이 돼버림(실제 바람에 의한 되돌아옴은 그보다
훨씬 작은 값, 실측 0.52m 수준). 즉 "바람 때문에 얼마나 밀렸는가"가 아니라
"감속이 덜 끝난 시점에 문턱을 넘었다는 사실 그 자체"를 재고 있었던 것.

**수정**: 문턱 기반 정의를 버리고 "최근접 시점"(트라이얼 전체에서 pos_error가
가장 작았던 순간 - 이미 감속이 끝난 뒤라 신뢰 가능) 기준으로 교체.
`pinn_waypoint_correction_test.py`가 이제 트라이얼 전체 pos_error를
`all_errors` 리스트에 모아뒀다가, 트라이얼이 끝난 뒤 최솟값 인덱스를 찾아
그 시점부터 `ARRIVAL_WINDOW_S`(5초) 동안의 peak를 `arrival_peak_error`로
계산함(사후 처리 방식 - 스트리밍 중엔 "지금이 최소인지" 미리 알 수 없어서
어차피 이렇게 해야 함). `ARRIVAL_THRESHOLD_M` 상수는 제거.

**상태**: 코드 수정·컴파일 확인만 하고 **SITL로는 아직 재검증 못 함** - 사용자가
잠들기 전 "돌리지 말고 md만 반영"으로 마무리 지시. 다음 세션 시작할 때 이
수정판으로 strong 등 몇 조건 먼저 스모크테스트해서 (1) 5조건이 이제 서로 다른
값으로 갈리는지, (2) 무풍(calm)이 확실히 제일 작은 값으로 나오는지부터 확인할 것
- 이 두 가지가 확인되면 지표가 드디어 제대로 작동한다는 뜻.

이번 세션에서 얻은 CSV(5조건 전체 스윕 1회분, 버그 있는 지표 기준)는 커밋 안 함
(재현 가능한 스모크테스트 산출물이고, 지표 자체가 바뀌어서 의미 없어짐).

---

## 12-49. 재검증 재개 — 최근접 시점 정의의 두 번째 버그 발견·수정, 지표 자체는 검증 완료 (2026-08-29)

**재검증 1차**: 12-48절 수정판(트라이얼 전체에서 pos_error 최솟값을 "최근접
시점"으로 찾음)으로 5조건 전체 스윕. 값은 이제 서로 갈리긴 함(0.041~0.258m)
- 문턱 버그(12-47)처럼 전부 몰리진 않음 - 하지만 **calm(무풍)이 오히려 제일
큰 값(0.258m)**으로 나와 이상함.

**원인**: CSV를 직접 뜯어보니, `all_errors`에서 전역 최솟값을 찾다 보니 진짜
도달 시점(calm t≈2.85초, strong t≈2.35초)이 아니라 **트라이얼 후반(t≈8~13초)의
자연 진동(PX4 위치제어의 미세한 노이즈 바닥, 진폭 ~0.02~0.07m - 3.3절/12-47절
에서도 이미 본 현상) 중 우연히 더 작은 값을 찍는 순간**이 "최근접"으로 잡히고
있었음. strong 조건 실측: 진짜 도달 직후 피크는 t=2.90에 0.509m(손으로 확인한
12-47절 수치 0.52m와 일치)인데, 지표는 t=8.20~13.20 구간의 노이즈 피크
0.068m를 보고하고 있었음(전역 최솟값이 t=8.20~8.45의 0.015m였기 때문).

**수정**: `EARLY_SEARCH_WINDOW_S=6.0`초 추가 - "최근접 시점" 탐색을 트라이얼
초반 6초 이내로 제한(실측 transit 시간 2.3~2.9초 기준 넉넉한 여유). 그 이후의
노이즈 바닥 저점은 더 이상 후보에 안 들어감.

**재검증 2차 (수정 확인)**: 다시 5조건 스윕 - **strong OFF=0.539m로 손 검증한
값(0.509~0.524m)과 잘 일치** - 이제 지표가 진짜 도달 직후 구간을 재고 있다는
근거 확보. 전체 결과:

| 조건 | OFF | ON | 개선율 |
|---|---|---|---|
| calm | 0.343 | 0.163 | +52.4% |
| light | 0.117 | 0.150 | -28.3% |
| default | 0.074 | 0.275 | -274.0% |
| strong | 0.539 | 0.398 | +26.2% |
| crosswind | 0.196 | 0.195 | +0.1% |

**정직한 결론**: 지표 자체는 이제 기계적으로 올바르게 작동하는 것으로 확인됨
(2번의 버그를 다 잡음). 다만 **calm에도 바람과 무관한 PID 자체의 정상적인
오버슈트가 존재**한다는 걸 이 과정에서 알게 됨 - 목표 지점을 새로 커맨드하면
관성 때문에 약간 지나쳤다가 되돌아오는 게 정상적인 2차 시스템 스텝응답이라,
"calm이 제일 작아야 한다"는 애초의 사전 기대(12-48절에서 세운 검증 기준)
자체가 틀렸음. 그래서 조건별 패턴이 풍속에 비례하지 않고 들쭉날쭉한 것도
이상 신호가 아니라 원래 그럴 수 있는 것 - 다만 default의 -274.0%처럼 극단적인
값은 오늘 밤 내내 본 "N=1은 못 믿는다"는 패턴과 정확히 같음. **결론 내리려면
N≥5, 가급적 N=10 반복측정이 필요** - 아직 안 함.

**다음에 볼 것**:
1. 5조건 N=10 반복 검증(`aggregate_repeats.py`는 peak-over-whole-trial이라
   이 지표엔 안 맞음 - 이 CSV 스키마 전용 집계 로직 필요, 최근접시점+arrival
   window 재계산을 반복 실행 결과에 대해서도 해야 함)
2. 혹은 calm의 "기저 오버슈트"를 베이스라인으로 놓고 각 바람 조건에서
   "오버슈트가 그것보다 얼마나 더 커지는지"(초과분)를 재는 방향으로 지표를
   한 번 더 다듬는 것도 고려할 만함 - 아직 시도 안 함

CSV: `logs/pinn_waypoint_correction_20260829_144444.csv`(수정판 검증, 커밋함).

---

## 12-50. 경로 이탈량(cross-track error) 지표 추가 (2026-08-29)

**배경**: 사용자 질문("최단거리 루트에서 벗어나는 정도를 줄이는 방향 맞나?")을
계기로, 지금까지의 `arrival_peak_error`(도착 후 정착 오차)와는 다른 관점의
지표가 있다는 걸 확인 - 홈→목표 직선 경로에서 **이동 중에** 얼마나 옆으로
벗어났는지(cross-track error). 이미 매 스텝 실제 위치·목표 위치를 CSV에
기록하고 있어서 SITL을 새로 더 돌릴 필요 없이 사후 계산만 추가하면 되는
구조 - "둘 다 하는 게 오버냐"는 질문에 "거의 공짜"라고 답하고 추가.

**장점**: `arrival_peak_error`가 두 번이나 버그를 냈던 이유(12-47/48) 전부
"언제 도착했다고 볼 것인가"를 정의해야 했기 때문인데, cross-track error는
그 정의가 아예 필요 없음 - 트라이얼 시작(홈, 직선 위)과 끝(목표, 직선 위)
둘 다 자연히 0에 가까워서 트라이얼 전체 peak를 그냥 쓰면 됨.

**구현**: `pinn_waypoint_correction_test.py`에 2D 외적 기반 수선거리 계산
(`cross_track_error()`) 추가, `run_trial()`이 이제 4개 값
`(arrival_peak_error, steady_state_error, transit_peak, peak_cross_track)`을
반환. CSV에 `cross_track_error_m` 컬럼 추가.

**5조건 검증 결과** (`logs/pinn_waypoint_correction_20260829_145751.csv`, N=1):

| 조건 | 도달OFF | 도달ON | 개선율 | 경로OFF | 경로ON | 개선율 |
|---|---|---|---|---|---|---|
| calm | 0.290 | 0.201 | +30.7% | 0.427 | 0.098 | +77.0% |
| light | 0.198 | 0.177 | +10.6% | 0.056 | 0.081 | -43.6% |
| default | 0.368 | 0.300 | +18.4% | 0.137 | 0.122 | +10.5% |
| strong | 0.499 | 0.437 | +12.5% | 0.195 | 0.164 | +16.0% |
| crosswind | 0.171 | 0.200 | -17.1% | 0.427 | 0.625 | **-46.4%** |

도달직후 4/5, 경로이탈 3/5 개선. crosswind(0,6 - 이 실험에서 유일하게 진행방향과
정확히 수직인 바람)가 경로이탈 기준선(OFF)도 가장 크고(0.427m, calm과 동률),
보정을 켜면 오히려 더 나빠짐(-46.4%) - 옆바람이 궤적을 옆으로 미는 효과가
가장 뚜렷하게 보여야 할 조건에서 지표가 반대로 움직인 셈이라 눈에 띄는 결과지만,
**이것도 N=1**이라 아직 아무 결론도 못 냄 - 오늘 밤 내내 본 패턴 그대로.

**상태**: 지표 2개(도달직후/경로이탈) 다 기계적으로 정상 작동 확인, 코드
커밋 완료. **N=10 반복 검증은 아직 안 함** - 다음 세션 과제.

---

## 12-51. 웨이포인트 전용 집계 스크립트 작성 (2026-08-29)

**배경**: 12-50절에서 남긴 과제 - 기존 `aggregate_repeats.py`는 phase 전체의
"peak pos_error"를 그대로 쓰는데, 웨이포인트 트라이얼은 초반 이동거리 자체가
그 peak가 돼버려 안 맞음(12-47절에서 이미 겪은 문제와 동일 구조). N=10 반복
검증을 하려면 전용 집계 로직이 필요해서 먼저 코드만 작성(SITL은 안 돌림 -
사용자가 "코드만 생성해봐"로 범위를 명시).

**구현**: `aggregate_waypoint_repeats.py` 신설. `pinn_waypoint_correction_test.py`의
`run_trial()`과 동일한 두 지표를 CSV 원시 시계열(`t_s`/`pos_error_m`/
`cross_track_error_m`)에서 재계산:
- `arrival_peak_error`: `EARLY_SEARCH_WINDOW_S` 이내 최근접 시점부터
  `ARRIVAL_WINDOW_S` 동안의 peak `pos_error_m`
- `peak_cross_track`: phase 전체의 `cross_track_error_m` 최댓값(도착 시점
  정의가 필요 없어 그대로 전체 peak면 됨)

**검증 중 발견한 버그**: 처음엔 "최근접 시점 탐색"을 `t_s <= EARLY_SEARCH_WINDOW_S`
(시간값 비교)로 구현했는데, 기존 12-49절 검증 CSV로 대조하니 crosswind 조건에서
`arrival_peak_error`가 0.171m(원본 스크립트)과 0.165m(집계기)로 어긋남 - 원본은
"행(row) 개수" 기준 슬라이스(`all_errors[:early_search_steps]`, 즉 t<6.00)를
쓰는데 집계기는 `t<=6.00`을 써서 경계의 t=6.00 행 하나를 더 포함시켰기 때문
(그 지점에서 값이 0.171→0.165로 계속 감소 중이었음 - crosswind 조건은 6초
안에 최근접점에 도달하지도 못했다는 뜻이기도 함, 별도로 살펴볼 만한 신호).
시간값 비교 대신 원본과 동일한 "행 개수" 기준 슬라이스로 고쳐 정확히 일치시킴
(`logs/pinn_waypoint_correction_20260829_145751.csv`로 대조 검증, 5조건 전부
±0.1%p 이내 반올림 차이로 일치).

**다음에 볼 것**: 이 집계기로 실제 N=10 반복 검증(`repeat_sweep.sh --script
pinn_waypoint_correction_test.py --repeats 10`) 실행 - 아직 안 함. crosswind가
6초 안에 최근접점을 못 찾은 것도 확인되면 `EARLY_SEARCH_WINDOW_S`를 더
넉넉하게(예: 8~10초) 늘리는 것도 검토할 만함.

---

## 12-52. 웨이포인트 N=10 반복 검증 첫 결과 — crosswind 경로이탈 악화가 확실한 신호로 확인 (2026-08-29)

**실행**: `repeat_sweep.sh --script pinn_waypoint_correction_test.py --repeats 10
--timeout 600` (5조건 전체를 한 트라이얼 세트로 묶어 10회 반복, 매회 SITL 재시작),
에러 없이 10/10 성공. `repeat_sweep.sh`가 자동으로 돌리는 기본 집계
(`aggregate_repeats.py`)는 이 CSV 스키마에 안 맞아 조건별 ~9.5~10m(그냥 초반
이동거리)만 찍고 끝남 - 무시하고 12-51절에서 만든 `aggregate_waypoint_repeats.py`로
매니페스트를 다시 집계.

**결과**:

| 조건 | 도달직후 OFF | ON | 개선율(도달직후) | 경로이탈 OFF | ON | 개선율(경로이탈) |
|---|---|---|---|---|---|---|
| calm | 0.433 | 0.165 | **+60.8%±6.5%** | 0.733 | 0.109 | **+84.7%±2.5%** |
| light | 0.130 | 0.163 | -28.5%±22.2% | 0.070 | 0.087 | -25.5%±22.1% |
| default | 0.332 | 0.273 | -2.0%±79.5% | 0.133 | 0.131 | +0.8%±10.8% |
| strong | 0.524 | 0.307 | **+41.2%±25.5%** | 0.199 | 0.195 | +2.2%±5.8% |
| crosswind | 0.186 | 0.193 | -4.2%±8.7% | 0.421 | 0.593 | **-41.1%±4.3%** |

**정직한 결론**:
- **calm의 두 지표 다 확실한 개선**(도달직후 +60.8%±6.5%, 경로이탈
  +84.7%±2.5%, 표준편차 둘 다 작아 노이즈 아님) - 다만 calm은 무풍이라
  "바람 보정 효과"라기보다는, 보정 가속도가 우연히 PID의 자연 오버슈트
  (12-49절에서 발견)를 댐핑하는 부수효과로 추정됨 - 별도 조사 필요.
- **strong의 도달직후도 확실한 개선**(+41.2%±25.5%, 범위가 +19.6~+83.5%로
  전부 양수 - 표준편차는 크지만 부호는 신뢰 가능).
- **crosswind의 경로이탈이 확실히 악화**(-41.1%±4.3%, 신뢰구간
  [-47.1%,-34.8%]로 전부 음수) - N=1 때 봤던 -46.4%(12-50절)가 노이즈가
  아니라 재현되는 진짜 신호였음이 확인됨. crosswind는 이 실험에서 유일하게
  진행방향과 정확히 수직인 바람 조건이라, "옆바람이 경로를 밀어내는 걸
  보정이 못 막고 오히려 더 심하게 만든다"는 뜻으로 읽힘 - 병진 보정
  게인(ACCEL_GAIN=0.05)이 "제자리 유지" 시나리오 기준으로 튜닝된 값이라
  "이동 중" 시나리오에는 안 맞을 가능성(3.4/3.5절의 "게인은 특정 시나리오
  기준으로 튜닝된다" 교훈이 여기도 적용될 수 있음).
- light/default는 표준편차가 평균보다 커서(default는 특히 79.5%로 극단적)
  판단 불가 - default 조건 중 최소 1회는 -236.7%라는 큰 발산이 있었던 것으로
  보임(원인 미조사).

**요약**: 5조건 중 2개(calm/strong)는 확실한 개선, 1개(crosswind)는 확실한
악화(그것도 물리적으로 가장 설명 가능한 조건에서), 나머지 2개(light/default)는
표본 부족이 아니라 **표준편차 자체가 커서** 판단 불가 - 이건 N을 더 늘린다고
꼭 해결될지도 불확실함(default의 극단값이 진짜 이변인지 시스템적 문제인지
먼저 봐야 함). "웨이포인트 이동 정확도"라는 새 목표는 조건에 따라 결과가
완전히 갈리는, 아직 정리 안 된 상태.

CSV: `logs/repeat_sweep_20260829_151309_pinn_waypoint_correction_test_manifest.txt`,
`logs/aggregate_waypoint_summary_pinn_waypoint_correction_20260829_151345.csv`.

**다음에 볼 것**: default 조건의 -236.7% 이상치 원인 조사, crosswind
경로이탈 악화가 게인 문제인지 다른 구조적 문제인지 확인(게인 낮춰서
재시도해볼 만함), calm의 "우연한 오버슈트 댐핑" 가설 검증.

---

## 다음 단계

전체 서사는 [CAPSTONE_REPORT_DRAFT.md](CAPSTONE_REPORT_DRAFT.md) 참고. 아래는 절
번호 색인 겸 남은 일 목록.

**완료** (절 번호로 색인, 자세한 내용은 해당 절 참고):
- 병진 파이프라인·5조건 A/B 5/5 (12-1~5) · gust "구조적 한계" 오판 정정, 5배
  개선 (12-6, 18, 19) · 파라미터 튜닝 0.15→0.05 재확정 (12-8, 14, 20, 31)
- yaw 일반화 데이터 기반 재설계 (12-10, 11) · 회전 피드포워드 발산→원인규명
  (자기입력 지름길)→더더링으로 해결→첫 순이익 (12-16, 17)
- 코드 셀프 리뷰 → 반복측정 인프라 → settle-time 부족이 분산의 근본 원인이었음
  확정 (12-21~23) · 펌웨어 패치 유실·복구 (12-24) · `gz topic pub` 침묵실패 완화 (12-25)
- RAMP-Net 파라메트릭 불확실성 스윕, 효과 없음 (12-26) · 더더링 의존성 발견·수정,
  비더더링 성능 대폭 개선 (12-27~30)
- `set_wind` 반복재발행 버그 발견·수정 2건 (12-31, 32) · 전환구간 조사 완료 (12-32)
- correction_experiments 코드 중복 정리 + 스팟체크 (12-33, 34)
- 비더더링 gust 192조건 수집 완료, 재학습은 대조군이 작아 판단 보류·배포 안 함 (12-34)
- 큰 gust 대조군(32조건) 확보 + 240조건 추가 수집, 재학습 재개해 오프라인 20%
  개선 확인했으나 SITL A/B 5회 반복에서 배포 기준 미달(crosswind 역효과) 확인·
  배포 안 함 (12-35, 36) — **그 crosswind 역효과를 10회 재검증하니 사라짐(+0.8%±6.9%,
  극단값 없음) → 5회 결과는 그 세션의 일시적 문제였던 것으로 결론, 배포 판단
  재검토 필요 (12-37)**. light_gust(-4.7%±17.2%)/default_gust(+1.6%±10.9%)도
  10회 재검증 완료 - 둘 다 중립 확정. SITL 재시작 시 MAVSDK 정상종료 보강 (12-38)
  — **strong_gust도 10회 재검증하니 뒤집힘(+26.4%→+1.0%±2.1%) → 4개 조건 전부
  중립으로 최종 결론, 432조건 재학습 모델 배포 안 함(오프라인 20% 개선이 실제
  제어 효과로는 이어지지 않음) (12-39)**
- `TAU_FF_GAIN` 물리 단위 캘리브레이션 완료 — 더더링 데이터로 회귀해서 진짜
  N·m↔정규화 변환배율(roll 3.90/pitch 6.30/yaw 4.20)을 계산해냈지만, SITL에서
  그 근처 값(1.0)을 시도해보니 이미 발산(-95.0%) — 추정 노이즈가 게인만큼
  증폭되는 게 원인으로 보여 기존 0.2 유지, 배포 안 함 (12-40)
- 병진/회전 5조건 A/B를 N=10으로 재검증 — 병진은 방향 유지(strong의 "+38.2%"가
  과대추정이었을 뿐, 실제 +3.0%±5.1%). **회전은 완전히 뒤집힘**: default
  -7.9%±3.5%, strong -3.6%±3.4%, light -6.5%±6.8% — "첫 순이익"이라던 12-17절
  결론이 재현 안 됨, crosswind만 미약하게 양성(+3.3%±4.0%, CI가 0에 걸침).
  `TAU_FF_GAIN=0.2`가 12-30 혼합 재학습 이후 한 번도 재스윕 안 된 게 원인으로
  추정(`ACCEL_GAIN`은 12-31에서 이미 재스윕함) (12-41)
- `TAU_FF_GAIN` 재스윕 완료 — {0.05,0.10,0.15,0.20,0.30} × {default,strong}을
  N=5로 코스 스윕한 결과 **5개 값 전부 default·strong 동시 양성 없음**(0.30만
  확실히 나쁨, 나머지는 전부 노이즈 수준). 가장 덜 나쁜 0.10을 5조건 N=10으로
  검증하니 **5개 조건 전부 방향은 개선**(gain=0.20 대비 light/strong 거의 중립화,
  crosswind 순이익 확대)됐지만 **default는 여전히 확실한 손해**(-3.1%±2.8%) —
  스윕 범위 안에 "배포할 만한 값"은 없었음. 회전 피드포워드는 게인 튜닝만으로는
  해결 안 되는 것으로 잠정 결론, **미해결 상태로 배포 보류** (12-42)
- correction_experiments 4개 스크립트의 텔레메트리/오프보드 진입부 보일러플레이트
  (연결·GPS확인, arm+이륙, pv/att/gyro 모니터, 초기 offboard 진입) `correction_common.py`로
  통합 완료 - 4개 파일 총 328줄 삭제, SITL 실행으로 검증(회전/병진 각 1회 완주,
  결과 정상 범위). 실제 바람/토크를 흘려보내는 `offboard_sender` 루프 자체는
  스크립트마다 페이로드가 달라 그대로 둠 (12-43)
- 회전 tau 추정치 EMA 스무딩 기능 추가(`tau_smooth_s` CLI 인자, 기본값 0=기존과
  동일) 후 시상수 {0.0,0.1,0.2,0.4}초를 N=3으로 빠르게 프로브 — **판단 불가**로
  결론. tau_smooth_s=0.0(스무딩 없음) 결과가 이번 N=3 표본에서 +1.9%±3.4%로
  나왔는데, 이건 12-42절 N=10 기준값(-3.1%±2.8%)과 완전히 같은 설정 - 부호까지
  다른 걸 보면 N=3은 이 정도 노이즈 수준을 구분하기엔 부족함(오늘 밤 세 번째로
  겪은 "표본 부족" 사례). 스무딩이 도움되는지 여부는 여전히 미해결 (12-44)
  — **N=10으로 정식 재검증하니 약한 개선 신호 확인**: tau_smooth_s=0.2에서
  `default`가 -3.1%±2.8%(확실한 손해)→+0.5%±3.8%(중립)로 바뀜. "확실한 손해"는
  없어졌지만 "확실한 순이익"이라 부르기엔 아직 이름(CI가 0을 넉넉히 포함) - 5조건
  전체 검증은 사용자 승인 대기 (12-45)
- **프로젝트 목표 재조정(2026-08-28)**: 실기 실측을 스코프에서 제외, "웨이포인트
  이동 정확도" 검증으로 전환. 새 스크립트 `pinn_waypoint_correction_test.py`
  작성·스모크테스트 완료 - 이동 메커니즘 자체는 정상 작동하지만, "정상상태
  위치오차" 지표는 PX4 PID의 적분 항이 이미 오차를 거의 0으로 수렴시켜서 PINN
  보정의 효과가 잘 안 보임(strong 조건에서 OFF=0.031m→ON=0.066m, 둘 다 절대치는
  작지만 방향은 악화) - 지표 설계 재검토 필요, 사용자 의견 대기 (12-46)
- 웨이포인트 지표를 "도달직후 피크오차"(`arrival_peak_error`, 문턱 통과 후
  5초간의 peak)로 1차 재설계 - strong 조건 실측으로 근거 확보(도달 직후 피크
  0.52m, 7초 뒤엔 노이즈 바닥 0.02~0.03m로 수렴, 3.1/3.3절과 같은 패턴). 코드
  반영 완료했으나 재검증은 사용자 요청으로 중단(12-47, strong 1회차 OFF만
  확보) — **재개해서 5조건 전체 스모크테스트하니 문턱 기반 정의 자체에 버그
  발견**: 5조건 전부 1.7~2.0m로 몰려나옴(무풍 calm도 1.7m) - "문턱 통과 순간의
  값 자체가 항상 window 최댓값이 되는" 설계 결함. "최근접 시점" 기준으로 재수정
  했으나 **SITL 재검증은 다음 세션으로 미룸**(사용자가 잠들기 전 코드만 고쳐
  두라고 지시) (12-48, 미완료)
- 재검증 재개 - 최근접 시점을 트라이얼 전체(15초)에서 찾다 보니 진짜 도달
  시점(2.3~2.9초)이 아니라 후반부(8~13초) 노이즈 바닥의 우연한 저점을 잘못
  집는 **두 번째 버그** 발견(strong 조건: 진짜 도달 피크 0.509m인데 지표는
  노이즈 구간 피크 0.068m를 보고). `EARLY_SEARCH_WINDOW_S=6초`로 탐색 범위를
  제한해 수정, 재검증하니 strong OFF=0.539m로 손 검증치(0.509~0.524m)와 일치
  - **지표 자체는 이제 기계적으로 정상 작동 확인**. 다만 calm에도 바람과
  무관한 PID 자체의 정상 오버슈트가 있다는 걸 알게 돼 "calm이 제일 작아야
  한다"던 애초 검증 기준 자체가 틀렸었음이 드러남 - 조건별 결과는 여전히
  들쭉날쭉(default -274.0%처럼 극단값도 있음, N=1이라 못 믿음) - N=10 반복
  검증 필요 (12-49)
- 경로 이탈량(cross-track error) 지표 추가 - 홈→목표 직선에서 이동 중 얼마나
  벗어났는지, "도착 시점" 정의가 필요 없어 arrival_peak_error가 겪은 버그
  구조를 원천적으로 피함. 5조건 검증(N=1): 도달직후 4/5, 경로이탈 3/5 개선,
  crosswind(진행방향과 정확히 수직인 유일한 조건)만 경로이탈이 -46.4%로
  악화 - 눈에 띄지만 이것도 N=1이라 결론 못 냄 (12-50)
- 웨이포인트 전용 집계 스크립트 `aggregate_waypoint_repeats.py` 작성(코드만 -
  SITL은 안 돌림, 사용자 지시). 검증 중 "행 개수 기준 vs 시간값 기준" 슬라이스
  경계 불일치 버그를 하나 더 발견·수정(crosswind에서 0.171m vs 0.165m로 어긋남).
  crosswind가 `EARLY_SEARCH_WINDOW_S`(6초) 안에 최근접점을 못 찾고 있었다는
  부수 발견도 있음 - 나중에 그 값을 늘려야 할 수도 있음 (12-51)
- 웨이포인트 N=10 반복 검증 첫 실행 완료 - **calm/strong은 확실한 개선**
  (도달직후 +60.8%±6.5%/+41.2%±25.5%), **crosswind는 경로이탈이 확실히
  악화**(-41.1%±4.3%, N=1의 -46.4%가 노이즈 아니었음이 확인됨 - 진행방향과
  정확히 수직인 유일한 조건에서 나온 결과라 게인이 "이동 중" 시나리오에
  안 맞을 가능성 시사), light/default는 표준편차가 너무 커서(default는
  79.5%p) 판단 불가 - default 중 1회는 -236.7% 이상치도 있었음, 원인 미조사 (12-52)

**남은 것** (우선순위순):
1. default 조건 -236.7% 이상치 원인 조사, crosswind 경로이탈 악화가 게인
   문제인지 확인(낮은 게인으로 재시도), calm의 "우연한 오버슈트 댐핑" 가설
   검증 (12-52)
2. 캡스톤 보고서 시각화(텍스트 초안은 완료, 다만 12-46~52절 목표 전환을 반영해
   서사 자체도 갱신 필요)
3. (낮은 우선순위) 회전 피드포워드 미해결 문제 후속 — tau_smooth_s=0.2를
   5조건 전체 N=10으로 검증(12-45, 승인 대기), 또는 0.05 아래로 더 낮춰보는
   게인 미세 스윕 (12-42) · 미배포 gust 모델을 `ACCEL_GAIN`/`WIND_DEADBAND_MPS`
   재튜닝 기준으로 다시 시도해볼지 검토 (12-39) · `ACCEL_GAIN_MIN`/`MAX`(적응형 게인,
   `pinn_wind_correction_sweep.py --adaptive`)는 12-31절 재튜닝(0.15→0.05)
   이후로 한 번도 재검증 안 됨 - 만들어만 두고 방치된 상태(12-8/20절에서 고정
   게인보다 나쁘다고 결론 났던 것도 구모델 기준이라 재확인 필요)

**스코프 제외(2026-08-28)**: 실기(Jetson/Pixhawk 2.4.8) 이관 — 원래 계획엔
있었으나 사용자 요청으로 제외, SITL 검증만으로 마무리.

**교훈**: 학습 스크립트처럼 CPU를 많이 쓰는 작업은 SITL 비행 테스트와 동시에
돌리지 말 것. SITL을 반복 재시작하면 GPS 락이 안 될 수 있음 — **이건 "빌드
손상"이 아니라 `rootfs/parameters.bson`(런타임 설정 파일) 손상임**, `pkill -9`
반복이 원인. `make distclean` + 재빌드(10~25분)로 "해결"돼 온 게 12-16/30/34/36절
등에 반복 기록돼 있는데, 이건 재빌드가 부수적으로 이 파일도 같이 지워서 나는
효과일 뿐 — 진짜 해결책은 `mv rootfs/parameters.bson{,.bak}` 후 재시작(즉시
해결, setup_guide.md 참고). 다음에 이 증상 나오면 재빌드 전에 이것부터 시도할 것.
`gust_sweep.py`류를 `repeat_sweep.sh`로 돌릴 땐 `gz topic pub` 1회 호출이 실측
~3.5초 걸리는 걸 감안해 `--timeout 900` 이상 지정할 것(기본 600초로는 부족, 12-36).
